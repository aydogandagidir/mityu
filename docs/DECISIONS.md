# Architecture Decision Records (ADR)

Append a short ADR whenever you make a decision that shapes structure, dependencies, security, or the tenancy/local-first model. Format: Context → Decision → Consequences → Status.

---
## ADR-0001 — Build on the Tauri/Rust core, treat the Python backend as legacy
**Context:** Meetily's supported app is the Tauri (Rust) + Next.js client; the Python/FastAPI `backend/` is archived with unauthenticated CORS.
**Decision:** Build the product on the Tauri core. Do not depend on the legacy backend at runtime; use it as reference (schema/prompts) only. The future server is a NEW clean service.
**Consequences:** One authoritative capture/transcribe/summarize path; a clean, secure server later; some reference code stays unused.
**Status:** Accepted.

---
## ADR-0002 — Local-first, server-optional, tenant-aware-by-design
**Context:** We want enterprise + eventual multi-tenant SaaS without losing the local-first (privacy) value prop.
**Decision:** Ship single-tenant/local first; introduce the four seams (AuthContext, tenant_id on all entities, repository layer, dormant sync) from Phase 1. Server is additive.
**Consequences:** Phase 2 is additive, not a rewrite; a little upfront discipline (tenant_id, repositories) in Phase 1.
**Status:** Accepted.

---
## ADR-0003 — Server language: Rust/Axum
**Context:** Options: Rust/Axum (cohesion with the core, shared models) vs clean FastAPI (velocity, reuse legacy schema/prompts as reference). Decided 2026-07-02 at BOOTSTRAP Step 3, per the default recommendation in docs/ARCHITECTURE.md.
**Decision:** **Rust/Axum** for the Phase-2 sync/collaboration server. Rationale: one language across core+server (the docs/CONTRACTS.md types — AuthContext, Repository, sync protocol — map 1:1 to shared Rust types instead of being re-modeled in Python), one toolchain (already proven on this repo), no second runtime to secure/operate, and the archived FastAPI backend stays reference-only for schema/prompts. FastAPI remains the sanctioned fallback ONLY if Rust server iteration proves measurably slow.
**Consequences:** `server/` (Phase 2, task D1) scaffolds as Axum + Postgres (RLS) with OIDC at the gateway; shared entity types live in a workspace crate reusable by client and server; hiring/onboarding assumes Rust.
**Status:** Accepted (re-confirm briefly at D1 kickoff before the first `server/` commit; server work does not start before gate C8).

---
## ADR-0004 — Audio module: `audio/` is authoritative; `audio_v2/` is dead code
**Context:** Base has both `audio/` and `audio_v2/`. Read-only investigation (audio-pipeline-engineer, 2026-07-02, Meetily v0.4.0) gathered wiring/runtime/history evidence.
**Decision:** **`frontend/src-tauri/src/audio/` is the authoritative module.** `audio_v2/` is an abandoned v0.1.1 rewrite scaffold, **not compiled into the binary**: no `mod audio_v2;` anywhere (`lib.rs:40` declares only `pub mod audio;`), zero `crate::audio_v2::` references (vs **59** refs to `crate::audio::` across 21 files), zero `#[tauri::command]` entry points (all ~30 registered audio commands come from `audio::`), and it is bit-rotted (imports `crate::audio::core::*` which no longer exists; duplicate `MixingMode`; undefined `AudioStream`). Git: `audio/` 125 commits (active through v0.4.0); `audio_v2/` exactly 1 commit (2025-10-23, v0.1.1) then never touched; 19 TODO/"Phase N" stubs in 1,393 LOC. Live-session log targets (`app_lib::audio::pipeline`, `::vad`, `::transcription::worker`) confirm `audio/` is the executing pipeline. The improvements audio_v2 promised (simplified pipeline, EBU R128) were since implemented inside `audio/` itself (`audio/mod.rs:18-26`, `audio_processing.rs`).
**Consequences:** No convergence flag is needed — nothing reachable to converge. `audio_v2/` is FROZEN (no new code, no imports). Delete it in a dedicated audio-cleanup ticket **after the Phase-0 validation gate passes**, together with the other verified-unreferenced dead files (`src/lib_old_complex.rs`, `src/audio/core-old.rs`, `src/audio/recording_saver_old.rs`), followed by `cargo build` + one recording smoke test per platform. All audio work targets `audio/` and routes to audio-pipeline-engineer.
**Status:** Accepted.

---
**Amendment (2026-08-07, out of a codebase-cleanliness sweep).** The deletion this ADR scheduled has already happened: `2b99ed6` (2026-07-07) removed `audio_v2/` together with `lib_old_complex.rs`, `audio/core-old.rs` and `audio/recording_saver_old.rs`, and is an ancestor of `main`. Recording it because the text above still reads as pending work under a condition that was **not** met — the Phase-0/A5 gate it was made conditional on remains unpassed to this day (ADR-0027), so the files went a month before their stated trigger. Nothing argues for restoring them: the evidence in the Decision above (unreferenced, uncompiled, bit-rotted) was what made them safe to remove, and it did not depend on Phase-0. What Phase-0 gated was the *risk appetite* for touching the audio tree at all. The `cargo build` half of the follow-up is continuously satisfied by CI; the **per-platform recording smoke test is not** — the macOS smoke remains owed here as it does elsewhere (`CLAUDE.md` §4).

---
## ADR-0005 — Audio retention default
**Context:** Storing raw audio increases cost + privacy risk.
**Decision:** Default to **transcript-only** (delete audio after transcription); raw-audio retention is an explicit per-tenant policy.
**Implementation status (2026-07-02, BOOTSTRAP Step 3):** the v0.4.0 base does NOT yet implement this — it unconditionally saves `audio.mp4` per meeting under `~/Music/mityu-recordings/` (verified live). The decision stands; enforcement (configurable retention, default delete-after-transcription) lands with BACKLOG **C6**. Until C6 ships, surface to pilot users that raw audio is kept locally.
**Status:** Accepted (revisit if a customer requires raw audio).

---
## ADR-0006 — Product name (provisional): "Mityu"
**Context:** The base (Meetily) is MIT but its name/brand is used commercially by Zackriya Solutions; MIT does not grant trademark rights, so we must rebrand. We need a working name to proceed with development.
**Decision:** Use **Mityu** as the working codename and bundle identifier `com.bluedev.mityu`. Short, easy to pronounce in both Turkish and English, and trademark-distinct from "Meetily". Because it is short, it is more likely to collide with existing marks/domains, so treat as **provisional** until a TÜRKPATENT/EUIPO/USPTO + domain/app-store availability check clears it before public launch + trademark filing.
**Consequences:** Development unblocked; name is a low-cost, config-level change to swap later; keep the MIT copyright notice (Zackriya Solutions) regardless of our brand.
**Status:** Accepted (provisional).

---
## ADR-0007 — Authoritative repo root: `mityu-app` (fork clone); pack folder archived
**Context:** BOOTSTRAP Step 0 (2026-07-02) found the Claude Code pack in a code-less staging folder (`../mityu`: docs + `.claude/` only, not a git repo) while the sibling `mityu-app` is the real project: a clone of fork `aydogandagidir/mityu` (upstream `Zackriya-Solutions/meetily` @ v0.4.0, branch `main`, whisper.cpp submodule initialized) with the pack already copied on top, uncommitted. The copy, on Windows' case-insensitive filesystem, overwrote two upstream files in the working tree: `CLAUDE.md` (upstream engineering notes) and `docs/architecture.md` (case-collision with the pack's `ARCHITECTURE.md`). Upstream originals are intact at `HEAD`.
**Decision:** `mityu-app` is the single authoritative repo root: all BOOTSTRAP steps, agent work, and Claude Code sessions run here; the `../mityu` pack folder is a frozen archive — do not edit it further. Overwritten upstream files are preserved as reference docs: `docs/UPSTREAM_CLAUDE.md` and `docs/UPSTREAM_ARCHITECTURE.md`. The pack's architecture doc keeps the git path `docs/architecture.md` (optional case-only rename at commit time: `git mv docs/architecture.md docs/ARCHITECTURE.md`). The pack overlay should be committed on a branch (e.g. `chore/claude-pack`) before feature work.
**Consequences:** One source of truth; upstream engineering notes remain available to agents; future Claude Code sessions must be opened in `mityu-app` so `.claude/` settings, hooks, and agents bind to the correct project dir.
**Status:** Accepted.

---
## ADR-0008 — Docs corrected to match the Meetily v0.4.0 base (Step 0 contradiction sweep)
**Context:** Step 0 verification of docs against the actual code found drift: (1) app transcription uses **whisper-rs** (builds whisper.cpp from source; features cpu/cuda/vulkan/metal/coreml/openblas/hipblas) — the `backend/whisper.cpp` submodule serves only the archived Python backend; (2) an embedded local-LLM path exists: workspace crate `llama-helper` (wrapping `llama-cpp-2`) plus an in-app model manager — local summarization is not Ollama-only; (3) actual Tauri plugins are `dialog, fs, log, notification, process, single-instance, store, updater` (docs claimed an `os` plugin); (4) `pnpm run tauri:dev` routes through `scripts/tauri-auto.js` (GPU auto-detect) and Windows scripts exist (`clean_run_windows.bat`, `build.ps1`, `build-gpu.ps1`); (5) stray heredoc `EOF` artifacts ended `docs/BACKLOG.md` and `docs/PHASE0_VALIDATION.md`; (6) the MIT license file is `LICENSE.md` (not `LICENSE`) — keep it intact under that name.
**Decision:** Corrected CLAUDE.md (§2 diagram/table, §3 stack, §8 commands) and docs/SETUP.md accordingly; removed the stray lines. `docs/UPSTREAM_CLAUDE.md` is the detailed upstream build/run reference.
**Consequences:** Agents orient on accurate ground truth; no unnecessary submodule build in the app path; Windows dev flow documented.
**Status:** Accepted.

---
## ADR-0009 — Rebrand to Mityu executed (BOOTSTRAP Step 2 / BACKLOG A3)
**Context:** Working name Mityu (ADR-0006) applied to the fork on 2026-07-02.
**Decision:** `productName`/`identifier`/window title → `Mityu` / `com.bluedev.mityu` / `Mityu`; `frontend/package.json` name → `mityu`; `src-tauri` crate name → `mityu`; user-facing strings & notification titles → Mityu; NEW recordings go to `~/Music/mityu-recordings` (old absolute paths in DB stay valid); data dirs migrated by copy (`com.meetily.ai` → `com.bluedev.mityu`, 3.2 GB incl. models+DB; `Roaming/Meetily` → `Roaming/Mityu`) — old dirs kept as rollback, delete after stabilization; **updater endpoint switched** from Zackriya releases to `github.com/aydogandagidir/mityu` releases (prevents self-update into upstream brand) — upstream updater `pubkey` retained temporarily and **must be regenerated before first release**; placeholder icon (blue "M") generated for tauri icons + public logos — replace with final brand assets before public launch.
**Deliberately kept as "meetily":** legacy-import UI/paths (HomebrewDatabaseDetector, LegacyDatabaseImport, homebrew db path) — they refer to the legacy product's data; IndexedDB `MeetilyRecoveryDB` (rename needs a data migration); model CDN `meetily.towardsgeneralintelligence.com` (upstream model hosting — supply-chain review before GA); macOS CoreAudio tap label `meetily-audio-tap` (audio module — separate audio-pipeline ticket); `lib_old_complex.rs` (legacy, do-not-extend).
**Consequences:** App runs branded as Mityu; `LICENSE.md` (MIT © Zackriya Solutions) untouched. Pre-existing QA tooling debt surfaced and tracked separately: `next lint` unconfigured / `eslint` missing; `tests/lib/blocknote-markdown.test.ts` (bun:test) breaks `tsc --noEmit`.
**Status:** Accepted.

---
**Amendment (2026-08-07).** **Two** entries in the "deliberately kept" list above are obsolete; the others were re-checked in the tree rather than assumed.

- `lib_old_complex.rs` no longer exists — removed by `2b99ed6` (2026-07-07) as dead code under ADR-0004.
- The model CDN `meetily.towardsgeneralintelligence.com` was retired the same day. `parakeet_engine.rs` now builds both the v2 and v3 URLs from `huggingface.co/istupakov/parakeet-tdt-0.6b-v{2,3}-onnx`, and `RELEASE_CHECKLIST.md` §3 records the switch as verified byte-for-byte against the old CDN (ADR-0020, whose Status already notes the repoint). So the "supply-chain review before GA" follow-up attached to this entry is **closed, not pending** — the domain now survives only in this ADR's prose.
- Still present and still deliberately "meetily", each verified on 2026-08-07: `HomebrewDatabaseDetector.tsx` and `LegacyDatabaseImport.tsx` with the homebrew path in `database/commands.rs`; `MeetilyRecoveryDB` (`indexedDBService.ts:11`, frozen so the v1 database can be upgraded in place); and the CoreAudio tap label (`audio/capture/core_audio.rs:147`).

---
## ADR-0010 — Tenant-scoped repositories are the only storage access path (BACKLOG B2 phase 2)
**Context:** Phase 1 (migration `20260702000000`) added `workspace_id` + sync columns to every domain table; B1 added the `AuthContext` seam. The repositories still ran unscoped SQL, and stray SQL existed outside `database/` (`api/api.rs` `open_meeting_folder`; `audio/import.rs` meeting+segment inserts and settings read; `audio/retranscription.rs` transcript delete/insert and two settings reads).
**Decision:** Every public method of the five repositories (`meeting`, `transcript`, `summary`, `transcript_chunk`, `setting`) takes `ctx: &AuthContext`; every SELECT/UPDATE/DELETE is scoped by `workspace_id = ctx.tenant_id`; every INSERT sets `workspace_id` and populates `created_at`/`updated_at` (nullable on `transcripts`/`transcript_chunks`/settings tables); every write to a synced table bumps `rev` and stamps `updated_by`. Commands and background tasks resolve identity **only** via `crate::context::current()` (never from the frontend — `AuthContext` is deliberately not `Deserialize`). Stray SQL was routed through repository methods; two methods were added for the audio flows (`create_meeting_with_segments` — preserves caller segment ids; `replace_meeting_transcripts` — transactional delete+insert with a scoped meeting-existence check) and one projection (`get_transcript_provider_model`). Timestamp writers standardized on binding `chrono::DateTime<Utc>` (RFC 3339); the one `naive_utc()` outlier (`update_meeting_title`) was fixed. Reads do **not** filter `deleted_at` yet — the app has only hard-delete flows today; soft-delete semantics land with sync.
**Settings caveat:** `settings`/`transcript_settings` keep the legacy single-row `id = '1'` PRIMARY KEY, which collides across workspaces. Until a Phase-2 migration widens the key to `(id, workspace_id)`, their upserts guard `DO UPDATE ... WHERE workspace_id = excluded.workspace_id`, so a foreign workspace's write degrades to a no-op instead of clobbering another workspace's row (proven by test). Single-workspace local mode is unaffected.
**Consequences:** `grep` for `sqlx::query` outside `database/` is now a valid review gate (`/tenant-check`); local flows are behavior-identical (everything is workspace `local`); cross-workspace isolation is proven by `frontend/src-tauri/tests/repository_tenant_scoping.rs` (6 integration tests, both directions). `rev` now increments on local edits — correct "modified since baseline" state for future sync.
**Phase-2 rules (guardian review, 2026-07-02):** (a) when `context::current()` becomes request-scoped, spawned background tasks must receive the `AuthContext` **captured at command time** — they must not re-resolve `current()` inside the task (today's constant resolver makes both equivalent; the spawn site in `summary/commands.rs` carries a `// Phase 2:` marker). (b) Repository no-match results (`Ok(false)`/0-rows on cross-workspace access) map to **404** at Phase-2 server endpoints, and status-writer methods (`update_process_completed/failed/cancelled`) must check `rows_affected` server-side rather than returning unconditional success.
**Status:** Accepted.

---
## ADR-0011 — BYOK LLM/STT API keys live in the OS credential store, not SQLite plaintext
**Context:** CLAUDE.md §0.7/§3 and `docs/SECURITY_PRIVACY.md` ("Secrets") require BYOK keys to live in the OS keychain / secure store — never SQLite plaintext, source, or logs. The Meetily base stored provider keys in plaintext `*ApiKey` columns of `settings` / `transcript_settings` (flagged by the guardian security review). This is a standalone secrets fix; **it is not** the SQLCipher at-rest DB-encryption work (BACKLOG B3, deferred — that will be a later ADR and encrypts the whole DB file; the two are independent and complementary).
**Decision:** Introduce a `secrets` module (`frontend/src-tauri/src/secrets.rs`, registered in `lib.rs`) wrapping the `keyring` crate (v3.6.3; native backends only — Windows Credential Manager via `windows-native`/`windows-sys` pure-winapi, macOS Keychain via `apple-native`, Linux keyutils via `linux-native`; no dbus/openssl build-time toolchain, works fully offline). Service name = `com.bluedev.mityu`; entry name = `{tenant_id}:{summary|transcript}:{provider}:api_key` — **workspace-scoped** (tenant-aware-by-design; a future non-`local` workspace cannot read another workspace's key, identity from `AuthContext` only) **and domain-scoped** (so the two settings tables' shared provider names `openai`/`groq` do not collide). All secret functions take `&AuthContext`, return `anyhow::Result` with context, **never log key values**, and **fail closed** — a locked/unavailable store yields a user-facing error with **no silent plaintext fallback**.
`SettingsRepository::save_api_key`/`save_transcript_api_key` now write the secret to the credential store first (abort before touching the DB if it fails), then persist only the non-secret marker `keychain:v1` in the legacy column; `get_api_key`/`get_transcript_api_key` read from the credential store; `delete_api_key` clears both. The columns are **kept** (not dropped) for schema/older-binary compat and to carry the reference marker. The existing Tauri command signatures/return types are unchanged, so the Next.js UI keeps working through the same `invoke()` calls with no `frontend/src` edits.
A one-time migration `migrate_plaintext_keys_to_keychain` runs inside `DatabaseManager::new` (covers fresh/legacy-import/recovery init paths) and lazily on first read: for any column still holding a real key (present, non-empty, not the marker) it moves the value into the credential store and overwrites the column with the marker. It is **idempotent**, receives the resolved `AuthContext` and touches **only that authorized workspace**; a future authenticated workspace switch reruns the same scoped migration/lazy read instead of enumerating or synthesizing identities for other workspaces. It runs **offline** and is **non-fatal** for keychain availability — if the credential store is unavailable, plaintext is scrubbed and that provider requires user re-entry, so the app still opens the DB and works offline (local-first invariant).
**Consequences:** No provider key is ever persisted in SQLite again; local single-`local`-workspace behavior is byte-identical for the user (a previously-saved key still calls providers after migration). The `geminiApiKey` column (schema-present but not yet wired to a provider) is swept defensively. `keyring` is now a dependency (and dev-dependency, so integration tests can install a persistent in-memory credential store — keyring's built-in `mock` is `EntryOnly` and cannot model separate `Entry::new` set→get calls). Tests never touch the real machine credential store. **For the SQLCipher (B3) agent:** `keyring 3.6.3` is already in `Cargo.toml`; B3 encrypts the DB file at rest and is orthogonal to this — the `*ApiKey` columns will by then hold only the marker, so no plaintext secret is exposed even in the pre-encryption DB.
**Status:** Accepted.

---
## ADR-0012 — Dormant sync client seam (BACKLOG B4): typed, disabled, no network, apply-remote bypasses the Phase-1 repositories
**Context:** ADR-0002 mandates a **dormant sync client** as one of the four Phase-1 seams so Phase 2 (the optional `server/`, ADR-0003 Rust/Axum) is additive, not a rewrite. `docs/CONTRACTS.md` §5 pins the wire shapes; `docs/DATA_MODEL.md` defines the sync semantics (monotonic `rev`, `updated_by`, soft-delete `deleted_at`; client SQLite authoritative for a user's own captures) and the never-synced baseline (`rev = 1` + `updated_by IS NULL`); `docs/MULTITENANCY.md` classifies data as local-only vs synced (`provider_credential` secrets NEVER sync). The seam must exist and be typed **without** breaking the local-first invariant: the app must behave byte-identically with sync off, and **no** network dependency may be added (no `reqwest`/`tokio-tungstenite`).
**Decision:** Add `frontend/src-tauri/src/sync/` (`mod.rs` / `protocol.rs` / `client.rs`, per `docs/SCAFFOLD.md`), registered as `pub mod sync;` in `lib.rs`. It is **inert**: no Tauri command is registered, nothing on the capture→transcript→summary→store path calls it, and there is no UI toggle. Reuses only already-present deps (serde, serde_json, uuid, chrono, thiserror, anyhow).
- **`protocol.rs`** — the §5 wire types with serde: `SyncEntity { Meeting, Transcript, Summary, ActionItem }` (`#[serde(rename_all = "snake_case")]` ⇒ tokens `meeting|transcript|summary|action_item`), `PushItem { tenant_id, entity, id, rev: u64, updated_by: Option<String>, updated_at, deleted: bool, payload: serde_json::Value }`, and `ServerAck { id, server_rev: u64, conflict: bool }`. Struct field order matches §5 so the serialized key order matches the documented sample; round-trip/shape tests assert the exact key SET, ORDER, and value types against JSON literals transcribed from §5, and parse the §5 doc samples back. `updated_by` is `Option` so the never-synced baseline serializes as `updated_by: null` (no sentinel user id).
- **`client.rs`** — `SyncConfig { enabled: bool }` (`Default` derived ⇒ `false`), a `SyncClient<T: Transport = NoopTransport>` whose `push`/`pull` **guard on `enabled` FIRST and return `SyncError::Disabled` (thiserror) performing NO I/O** while disabled (proven by an atomic-flag spy transport that stays untouched). A `Transport` trait abstracts the wire; the ONLY Phase-1 impl is `NoopTransport` (reports non-operational, returns `SyncError::NoTransport` — never fabricates acks), so the Phase-2 HTTPS transport slots in as a new impl behind a flag without changing this file. `SyncError` also pre-defines `NoTransport`/`Transport(String)`/`Protocol` for a stable Phase-2 surface.
- **CRITICAL seam — `RemoteApply` (guardian-flagged):** applying a PULLED remote change MUST NOT go through the Phase-1 repositories (ADR-0010), because those bump `rev = rev + 1` and stamp `updated_by = ctx.user_id` on **every** write. Replaying a remote change through them would (1) masquerade it as a local edit (`updated_by` = local user), (2) re-bump `rev` and destroy the server-assigned `rev` and the `rev = 1 / updated_by NULL` never-synced baseline, and (3) make the row look "modified since sync" again → the next push re-sends it, the server re-acks, forever (a ping-pong / infinite echo). So `apply_remote` is a **distinct trait** (defined, not wired) whose contract — captured in rustdoc — is to write `rev`/`updated_by`/`updated_at`/`deleted_at` **VERBATIM** from the `PushItem` payload (a future dedicated "remote-apply" repository path with its own `... ON CONFLICT DO UPDATE SET rev = :remote_rev, updated_by = :remote_updated_by, ...` SQL), passing `AuthContext` for **tenant scoping only**, never to derive identity/rev. A unit test encodes this as an executable check (a recording `RemoteApply` impl preserves a remote author + rev, distinct from `ctx.user_id`).
- **Classification (local-only vs synced):** `SyncEntity` enumerates only the four `Synced? = yes` domain entities. There is deliberately **no** `Settings`/`Credential` variant, so a `PushItem` for `settings`/`transcript_settings` or a `provider_credential` secret cannot even be constructed — the local-only/secret boundary is structural, not disciplinary. Only a non-secret label may ever sync (a future, separate decision).
**Consequences:** The seam is typed and reviewable; Phase 2 fills in the `push`/`pull` bodies, adds an HTTPS `Transport`, and implements `RemoteApply` as a separate repository path (never a method on the existing `database/repositories/` types). Local-first holds: with sync off (always, today) behavior is byte-identical and offline; no network crate was added. DoD verified: `cargo build -p mityu` green; `cargo clippy -p mityu --all-targets` unchanged at the 142-warning baseline (the one initial `derivable_impls` finding on `SyncConfig::Default` was resolved by deriving `Default`); `cargo test -p mityu` passes (12 new sync tests + existing suites); sync files `rustfmt`-clean.
**Status:** Accepted (re-confirm the wire shapes against `server/` at D1 before the first sync endpoint).

---
## ADR-0013 — On-device AI agents: local-first, draft-only (HITL), no autonomous actions (EPIC F seam)
**Context:** The About screen advertises "Coming soon: a library of on-device AI agents — automating follow-ups, action tracking." That was marketing ahead of the plan: BACKLOG had **source-linked summaries (C1)** and **action-item extraction (C2)** but no "agents" epic and no "automated follow-up" capability. Building agents naively (autonomous send, cloud round-trips, auto-approval) would violate `CLAUDE.md` §0.1 (local-first), §0.5 (HITL for every AI output), and §10 (no autonomous irreversible actions). Separately, the About "Works everywhere — Google Meet / Zoom / Teams" is **not** an API integration and is intentionally absent from the plan: the app captures **system audio** (WASAPI loopback / BlackHole), recording any call online or offline — `CLAUDE.md` §1 "not a meeting bot that joins calls" stands.
**Decision:** Introduce **BACKLOG EPIC F** and scaffold a **dormant `agents/` seam** now (`frontend/src-tauri/src/agents/` = `mod.rs` + `draft.rs` + `engine.rs`, `pub mod agents;`), following the ADR-0012 dormant-`sync/` precedent (typed, disabled, no network dependency, tests pin the invariants). Reuses only already-present deps (serde, serde_json, thiserror). Four rules, encoded structurally (not merely documented):
1. **Local-first / dormant:** no LLM/HTTP client in the seam; Phase-F agents reuse the existing summary providers (`summary/` + anthropic/openai/groq/ollama/openrouter, BYOK, offline) behind the `Agent` trait (Phase-1 impl = `NoopAgent`, no I/O). Off by default (`AgentsConfig { enabled: false }`, `Default` derived) ⇒ a build is byte-identical to one without the module.
2. **HITL:** an agent run yields an `AgentDraft` whose status `AgentRunner::run` forces to `Draft`; approval is a separate human action. A self-approving agent cannot bypass it (pinned by `run_forces_hitl_never_returns_approved`). `run` guards on `enabled` FIRST and returns `AgentError::Disabled` doing no work (an atomic-flag spy proves the agent is never touched — the same pattern as sync).
3. **No autonomous external actions:** agents only *propose* — `ProposedAction` is data; there is **no** execute/send method anywhere in the seam. "Send"/"create" stays a manual user step (`CLAUDE.md` §10).
4. **Source-linked + tenant-scoped:** every output carries `SourceRef` (→ `source_chunk_id`, C1) and `ctx: &AuthContext`; `AgentRun` carries the §6 sync-ready fields (`id`, `tenant_id`, `created_at`/`updated_at`, `updated_by: Option`, `rev`, soft-delete) so a Phase-2 `agent_runs` table + sync are additive — with no `SyncEntity` coupling yet (agents don't sync until a separate decision).
Sequenced **after gate C8**, dependent on **C1 + C2**; trigger model manual/on-demand first, scheduling only at F5 behind explicit opt-in.
**Consequences:** The "Coming soon" copy is now backed by a tracked, gated plan (BACKLOG F0–F5) and an inert, tested seam that ships today without changing behavior or adding a runtime dependency. Verified here with `rustfmt` (all three files parse clean & formatted); the full `cargo build -p mityu` / `clippy` / `test` gates run in an environment with the LLVM/CMake toolchain (BOOTSTRAP Step 1). The seam's `#[cfg(test)]` invariant tests mirror `sync/`'s and only compile under `cargo test`, so a test issue cannot break `tauri:dev`. Revisit trigger/automation policy at F5 before any scheduled run ships.
**Status:** Accepted (seam merged dormant; F0 re-confirms scope at C8 kickoff).
**Amendment (2026-07-08, out of the ADR-0022 audit):** Three internal inconsistencies were found in the dormant seam. Two are corrected **now** rather than at F0, because they are internal-consistency defects rather than product scope, and because leaving them would let the future `agent_runs` migration bake a wrong shape in. The seam stays dormant, off by default, no network dependency, no caller.
1. **`Agent::draft` is now `async`** (`#[async_trait]`, and `AgentRunner::run` with it). Every real Phase-F agent reaches an LLM through the `summary/` provider layer, whose methods are async; the synchronous signature would have forced the first real agent to block a runtime thread or bolt on an ad-hoc executor. `async-trait` is **already a direct dependency** (`frontend/src-tauri/Cargo.toml:112`) and the identical `#[async_trait]` + `Box<dyn _>` pattern already ships in `audio/transcription/provider.rs`, so ADR-0013's "reuses only already-present deps" promise holds.
2. **`AgentRun.deleted: bool` → `deleted_at: Option<String>`.** `CLAUDE.md` §6 mandates `deleted_at` on every domain table, and the shipped tables use exactly that (`database/repositories/action_item.rs`: `SET deleted_at = ?` / `WHERE deleted_at IS NULL`). The `bool` mirrored the *sync envelope* (`sync/protocol.rs`: `"deleted": false`), not the *row*: a bool cannot answer "when", so tombstone retention and pruning would have had nothing to read.
3. **A new invariant, enforced structurally rather than documented:** `AgentRunner::run` now rejects a draft that proposes actions while carrying no `SourceRef` (`AgentError::Ungrounded { actions }`). `CLAUDE.md` §0.5 requires every AI-generated item to be bound to its source transcript segment; previously an agent could return ungrounded proposals and the runner would hand them to a reviewer with no evidence to check them against — the HITL test itself used such a draft. An *empty* draft (no actions) still needs no sources. Pinned by `run_rejects_proposals_with_no_source_link` and `empty_draft_needs_no_sources`.
**Left open for F0, deliberately not guessed:** `AgentInput` is single-meeting (`meeting_id: String`) and carries only `summary_text`. That fits **F2** (follow-up drafter) but not **F3** (action-item tracker), whose entire job is aggregating open items *across* meetings; and it omits the approved action items F2 must draft from. Widening it (an enum, or `Vec<MeetingRef>` + `action_items`) is additive and breaks no caller today, because there are none. F0 decides the shape at C8 kickoff.
**Verification:** `cargo test -p mityu --lib` → **287 passed, 0 failed, 2 ignored** (12 of them in `agents::`); `rustfmt --edition 2021 --check` clean on `agents/{draft,engine}.rs`; no warning in the build output originates in `agents/`. Not verified: nothing calls this module, so none of it has ever executed outside its own tests.

---
## ADR-0014 — Encryption at rest (SQLCipher): key in OS keychain, one-time plaintext→encrypted conversion
**Context:** `docs/SECURITY_PRIVACY.md` ("Encryption — At rest (client): SQLCipher (encrypted SQLite) for sensitive data; OS-protected key") and BACKLOG B3 require the local SQLite DB to be encrypted at rest with an OS-protected key, while the app must **still open existing data** (documented migration path) and stay **local-first** (works fully offline; a key/store failure must never silently downgrade to plaintext). ADR-0011 already moved BYOK provider secrets out of SQLite into the OS keychain and explicitly deferred whole-file DB encryption to B3 (the two are orthogonal and complementary — after B3 the `*ApiKey` columns still hold only the `keychain:v1` marker, so even the pre-encryption backup exposes no provider secret). Feasibility was proven in a prior Step-1 spike: `sqlx 0.8.6` → `sqlx-sqlite` pulls `libsqlite3-sys 0.30.1`, which ships a `bundled-sqlcipher-vendored-openssl` feature, and sqlx treats the SQLCipher `key` pragma as a reserved first-executed slot.
**Decision:** Encrypt the whole `meeting_minutes.sqlite` file with SQLCipher (AES-256), keyed by a device-scoped 256-bit key held in the OS credential store.
- **Cargo (two dep changes):** (1) declare `libsqlite3-sys = { version = "0.30.1", features = ["bundled-sqlcipher-vendored-openssl"] }` as a **direct** dependency purely to enable the feature — Cargo unifies it with sqlx's transitive `bundled` on the same crate version, so sqlx links a SQLCipher-capable libsqlite3; no system OpenSSL/SQLCipher toolchain is needed (the vendored OpenSSL + SQLCipher amalgamation compile from source, one long native build). (2) declare `zeroize = { version = "1.8", features = ["alloc"] }` (already in-tree transitively at 1.8.2, so it unifies with no new crate) to scrub the DB key hex from memory (security-review hardening, below).
- **Key management (`secrets::db`, reuses ADR-0011's `keyring` 3.6.3):** a new `db` submodule in `secrets.rs` stores/reads the DB key under service `com.bluedev.mityu`, entry `db-key`. Unlike BYOK keys it is **device-scoped, not workspace-scoped** (one encrypted file spans all workspaces). `get_or_create_hex()` generates 32 bytes from the OS CSPRNG (`rand` 0.8, already in-tree) on first launch, stores them as 64 lowercase hex chars, and reads them back on every startup — fully offline. The key value is **never logged**; a malformed stored value is rejected (never silently regenerated — that would strand decryptable data); the value is passed to SQLCipher as a **raw** key `PRAGMA key = "x'<hex>'"` (no KDF). The key hex is returned as `zeroize::Zeroizing<String>` and held that way in `database::manager` (dropped right after the pool opens), so the material is scrubbed from freed heap rather than lingering.
- **Keyed pool (`database/manager.rs`) — GUARDRAILED fallback (revised 2026-07-03):** the main pool moved from `SqlitePool::connect(path)` to `connect_with(SqliteConnectOptions…)` applying `.pragma("key", x'<hex>')` (sqlx executes the reserved `key` slot first, before any statement), and preserves the prior behavior explicitly with `.journal_mode(Wal)` + `.foreign_keys(true)` + `create_if_missing(true)`, so the existing WAL checkpoint/recovery paths (`cleanup`, orphaned-WAL retry) keep working and a fresh install is created already-encrypted. The key path is now **state-dependent, decided from the on-disk file BEFORE the key is touched** (via `encryption::is_plaintext_db`, the shared plaintext-header probe reused from `ensure_encrypted`): `absent` = no file, `plaintext` = the `"SQLite format 3\0"` header is present, `encrypted` = a file with no such header.
  - **Encrypted file ⇒ FAIL CLOSED.** The key is read with the **non-creating** `secrets::db::get_hex()` — a missing entry must **never** mint a fresh key over live ciphertext (that would strand the data behind a key that cannot decrypt it). `Ok(Some(key))` ⇒ open keyed (`ensure_encrypted` is a no-op on an already-encrypted file). Key **missing (`Ok(None)`) or store error (`Err`)** ⇒ return a `sqlx::Error::Configuration` ("local database is encrypted but its key is unavailable … refusing to open — retry, or restore the key") and **do not open** — never a keyless open of ciphertext, never a regenerated key. This error is **distinct from corruption** (it contains neither "malformed" nor "corrupt"), so the `new_from_app_handle` WAL-recovery path is **not** entered and cannot delete the WAL of a healthy-but-locked encrypted DB.
  - **Plaintext or absent file ⇒ fail-open is permitted** (CLAUDE.md §0.1 local-first: never lock the user out of a still-plaintext file). The key is read with `secrets::db::get_or_create_hex()` (mint-on-first-run is correct here) and the file is converted via `ensure_encrypted`. If the key store is unavailable, **or** the one-time conversion fails (e.g. a SQLCipher-less runtime where `sqlcipher_export` is missing), we log **loudly and accurately** (the DB is opening **UNENCRYPTED / plaintext at rest**, will retry next launch — the log does **not** claim it is "encrypted"/"safe") and open **without** the `key` pragma. This is safe because the file was plaintext anyway, so no confidentiality is lost relative to the pre-B3 state, and **no ciphertext is ever opened keyless**.
  - This **replaces** the earlier strict "any locked/unavailable keychain aborts" wording (and reverts an interim blanket **fail-open** edit that regressed confidentiality by opening ciphertext keyless / minting a key over an encrypted file). `Zeroizing<String>` still scrubs the key hex from memory once the pool opens.
- **One-time conversion (`database/encryption.rs`, before migrations):** `ensure_encrypted()` runs in `DatabaseManager::new` **before** the keyed open and **before** `sqlx::migrate!`. It detects a plaintext DB by the `"SQLite format 3\0"` header (absent on a cipher file), then: (1) **checkpoints any plaintext `<db>-wal`** (`PRAGMA wal_checkpoint(TRUNCATE)`) so committed WAL-only rows fold into the main file and no stale plaintext `-wal` can collide with the encrypted DB's WAL; (2) opens the **encrypted destination** as the main keyed connection (create) **in rollback-journal (DELETE) mode** so the cipher DB is a single self-contained file (no `<dst>-wal`/`-shm` orphans a single-file rename would leave), `ATTACH DATABASE '<plaintext src>' AS plaintext KEY ''`, `SELECT sqlcipher_export('main','plaintext')`, `DETACH`; (3) an atomic file swap moves the original to a **temporary** `<db>.pre-encryption` backup and promotes the cipher file; (4) **verifies** the promoted file opens with the key (`SELECT count(*) FROM sqlite_master`) — and **only then deletes** the `.pre-encryption` backup (scrub-then-unlink) and any stale plaintext `<db>-wal`/`-shm`, so **no full-database plaintext lingers on disk**. If verification fails, the plaintext backup is restored and startup aborts (fail closed, recoverable). (The reverse direction — encrypted-main + plaintext-attach — is required because this `libsqlite3-sys 0.30.1` build returns `SQLITE_CANTOPEN` when `ATTACH` is asked to *create* a new encrypted file; it also mirrors exactly how the live pool later opens the keyed file, so a successful+verified conversion guarantees the pool opens it.) No row is rewritten, so every `workspace_id`/`rev`/`created_at`/`updated_at` and the **`_sqlx_migrations` ledger survive byte-for-byte** (asserted in tests). It is idempotent (no-op on an already-encrypted or missing file). The legacy `.db`→`.sqlite` import still lands plaintext first and is then encrypted by the same path.
- **Migration path for existing users:** on the next launch after upgrade, an existing plaintext `meeting_minutes.sqlite` is transparently converted once. The `.pre-encryption` backup is a **transient** safety copy during conversion and is **deleted after the encrypted DB is verified readable** — so afterwards no plaintext copy remains. Because the plaintext backup is gone and the key lives only in the OS keychain, deleting/rotating the `db-key` entry or restoring on a new machine without it makes the DB **permanently unrecoverable** — this is the intended at-rest posture (an attacker with the file but not the keychain gets nothing). A user-facing key export/rekey affordance is future work (below); until then, protecting the OS keychain is the recovery story (documented in `docs/SECURITY_PRIVACY.md`).
- **Keyless-still-reads-plaintext invariant:** a SQLCipher build with **no** `key` pragma still opens ordinary plaintext SQLite files, so the existing integration suites (`migration_workspace_sync`, `repository_tenant_scoping`) that open plain temp DBs with no key keep passing unchanged (pinned by a dedicated test).
**Consequences:** The local DB is encrypted at rest with an OS-protected key; local-first holds (offline, no network); no provider secret is exposed at any point (ADR-0011). **After a verified conversion no full-database plaintext remains on disk** (the temporary `.pre-encryption` backup and stale plaintext WAL/SHM are removed), and the DB key hex is zeroized from memory after the pool opens. Cost: one long first native build (vendored OpenSSL + SQLCipher), and the deliberate loss of any plaintext fallback for **already-encrypted** data — **the OS keychain `db-key` entry is the sole recovery path for an encrypted DB; losing it = unrecoverable data** (correct at-rest posture). Security/guardian review (2026-07-02) passed with no blocker; the three fixes applied then were: delete the plaintext backup after a verified keyed re-open (HIGH), checkpoint the plaintext WAL before conversion + remove stale plaintext WAL/SHM after (MED), and zeroize the key hex (LOW hardening).
**Guardrailed-fallback amendment (2026-07-03):** a follow-up security review found an interim edit had relaxed the keyed-open into a **blanket fail-open** — which (a) does not help an already-encrypted DB (a keyless open of ciphertext just fails or, worse, regenerating the key over ciphertext strands the data) and (b) regresses confidentiality. It is replaced by the **state-dependent** posture above: **fail-open only on a plaintext/absent file** (via `get_or_create_hex`), **fail-closed on an encrypted file whose key is unavailable** (via the non-creating `get_hex`). **Accepted local-first tradeoff:** brand-new **plaintext** data at rest is now possible **only on a machine that was never successfully encrypted** (a first run where the OS keychain is broken/locked so the key can neither be read nor minted); such a file **heals on the next launch** once the keychain is available (it is then detected as plaintext and converted). An already-encrypted DB is **never** downgraded and is **never** opened keyless. **Follow-up (tracked, NOT in this change): an in-app downgrade WARNING** surfaced to the user when the DB opens UNENCRYPTED (so a silent plaintext-at-rest state is visible), alongside the existing key export/rekey affordance and the sync/backup denylist for the encrypted file + key (deferred to B4). New tests: `frontend/src-tauri/tests/db_encryption_sqlcipher.rs` — fresh encrypted DB + migrations; plaintext→encrypted conversion preserving row counts **and** the `_sqlx_migrations` ledger (seeded via the public `Migrator`) **and** asserting the `.pre-encryption` backup is deleted; a populated-`-wal` conversion that preserves committed rows and leaves no stale plaintext `-wal`/`-shm`; wrong-key-fails-closed; the keyless-plaintext invariant; an env-gated (`MITYU_TEST_ENCRYPT_DB`) real-copy conversion proof; **and (added 2026-07-03) three `DatabaseManager::new` fallback tests driving a persistent in-memory keyring — ENCRYPTED+key-present opens & reads; ENCRYPTED+key-MISSING fails closed with the ciphertext file byte-for-byte unmodified, no new key minted, not opened as plaintext; PLAINTEXT+store-unavailable fails open (opens plaintext, rows readable, file stays plaintext).** Touched: `Cargo.toml` (the two features + the `keyring`/`tempfile` dev-deps enabling the mock keyring in integration tests), `secrets.rs` (the `db` submodule incl. the non-creating `get_hex`), `database/manager.rs`, `database/mod.rs`, `database/encryption.rs` (added the `pub fn is_plaintext_db` shared probe), the test, and this ADR + the SECURITY_PRIVACY "Encryption" note.
**Status:** Accepted (SQLCipher at-rest encryption merged; no plaintext residue after conversion; **guardrailed fallback amended 2026-07-03** — fail-open only on plaintext/absent, fail-closed on encrypted-but-keyless; revisit the in-app downgrade warning + key export/rekey + sync/backup denylist when sync (B4) or a second device lands).

---
## ADR-0015 — Opt-in PII/keyword redaction at the persist + summary boundaries (BACKLOG C6, redaction half)
**Context:** `docs/SECURITY_PRIVACY.md` ("Privacy & data lifecycle — Retention/redaction: ... redaction rules for PII/sensitive terms") and the OWASP LLM02 (sensitive-info-disclosure) note require per-workspace redaction of PII/sensitive terms, and `docs/DATA_MODEL.md` already lists `redaction` as a `settings` concern. `docs/SCAFFOLD.md` anticipated a `policy.rs` for per-workspace redaction. The BACKLOG C6 **retention/audio-deletion half is a separate, later change** and is explicitly out of scope here — this ADR covers **redaction only** and does **not** touch `audio/` capture internals or `audio.mp4` saving. The hard constraint: must be **non-breaking / local-first** — existing local data and flows are byte-identical unless a user opts in.
**Decision:** Add a pure, DB-free `frontend/src-tauri/src/redaction.rs` (`pub mod redaction;`) exposing `RedactionConfig { enabled, use_default_patterns, custom_terms }` (serde; `Default` = **disabled**) and `redact(text, cfg) -> String`. **OFF by default:** when `!cfg.is_active()` (the default) `redact` is a verbatim, allocation-cheap no-op. When enabled it applies built-in PII patterns and case-insensitive custom terms, each match replaced with a **typed placeholder**.
- **Default patterns (conservative — precision over recall, documented per-pattern):** email → `[EMAIL]`; IBAN (2 letters + 2 check digits + 11..30 alnum) → `[IBAN]`; credit-card (13..19 digit candidate **gated by a Luhn check**) → `[CARD]`; Turkish TC Kimlik No (11-digit candidate **gated by the official TC checksum**) → `[ID]`; phone (candidate **gated by a 7..=15-digit + leading-`+`-or-separator guard**) → `[PHONE]`; custom term (escaped, case-insensitive) → `[REDACTED]`. Fixed pass order **email → IBAN → card → TC → phone → custom** so structured numeric PII claims its digits before the looser phone pattern (a Luhn-valid card is never mangled into `[PHONE]`). The Luhn/TC/phone Rust-side validators keep ordinary long integers (ids, timestamps, non-card digit groups) from being over-redacted. Patterns compile once via `once_cell::Lazy` (both `regex` and `once_cell` were already direct deps). **No raw text or redacted-away value is ever logged.**
- **Config storage (per-workspace, tenant-scoped):** stored as a small JSON blob in a new **additive** `settings.redactionConfig TEXT` column via `SettingsRepository::{get,set}_redaction_config(ctx)` (mirrors the existing `customOpenAIConfig` JSON-column pattern; scoped by `workspace_id = ctx.tenant_id`; `get` returns the disabled `Default` when absent/unparseable so a missing/corrupt blob can never *enable* redaction). Migration `frontend/src-tauri/migrations/20260704000000_add_redaction_config.sql` is a single nullable-column `ALTER TABLE` (additive, forward-only, idempotent-by-construction like the `geminiApiKey`/`customOpenAIConfig` precedents); the `Setting` struct is untouched (sqlx `FromRow` ignores the extra column; targeted `SELECT redactionConfig` reads it). The `settings` table is PER-SCOPE and **NOT synced**, so this policy is client-local for now (a future policy-sync is a deliberate separate change).
- **Two enforcement boundaries (only when enabled; config fetched via `AuthContext`):** (1) **before persistence** — each transcript segment's `text` is redacted at the call boundary just above the repository write, keeping the repository a **pure writer** (policy is not buried in the repo). Covered persist entry points: the **main recording save** (`api::api_save_transcript`), **audio import** (`audio/import.rs`, at the `create_meeting_with_segments` text-persist boundary — redacted once so the DB rows and the on-disk `transcripts.json` stay consistent), and **retranscription** (`audio/retranscription.rs`, at the `replace_meeting_transcripts` boundary). Only `text` is rewritten; segment ids/timestamps are preserved. (2) **before the summary LLM** — `summary/service.rs::process_transcript_background` redacts the assembled `text` immediately after resolving `ctx`, before language detection, the cache fingerprint, and the provider call. This is the **key privacy boundary**: for a cloud provider it stops PII leaving the device. Because the redacted text feeds the summary cache fingerprint, toggling redaction correctly invalidates the cache (no cross-contamination of redacted/unredacted summaries).
- **Fail-safe:** if the redaction config **cannot be read** at a boundary, the operation fails with a clear, content-free error rather than persisting/summarizing potentially-unredacted PII (the persist commands return an error; the summary background task marks the process failed).
- **HITL / source-linking intact (`docs/CONTRACTS.md` §4):** redaction changes stored text only; it does not alter `source_chunk_id` linkage or the human-approved draft flow.
- **Tauri commands:** `api_get_redaction_config` / `api_set_redaction_config` (typed, registered in `lib.rs`), with a typed TS wrapper `configService.getRedactionConfig()` / `setRedactionConfig()` in `frontend/src/services/configService.ts`. The **config UI is a separate frontend task** (not built here). `RedactionConfig` is not identity, so — unlike `AuthContext` — the value legitimately round-trips through the frontend.
**Consequences:** Enterprises get an opt-in, on-device PII/keyword scrubber at exactly the two seams where sensitive text would otherwise leak (local DB at rest, and the summary provider — especially a cloud BYOK provider), with no new dependency and no change to the offline/local-first path when disabled. Conservative patterns favour missing an exotic PII shape over shredding ordinary transcript content; the placeholder taxonomy lets a reviewer see *what kind* of value was removed. Cost: one additive column/migration on `settings`; redaction is a lightweight regex pass applied only when enabled. **Out of scope / follow-ups:** the C6 retention/audio-deletion half (separate change); a config UI (separate frontend task); optional expansion of the default pattern set (e.g. IDN emails, more national-ID formats) and a future policy-sync decision. Verified: `cargo build -p mityu` green; `cargo clippy -p mityu --all-targets` at the 140 baseline with **zero new warnings**; 15 new `redaction::tests` + the full 246-test lib suite pass; rustfmt applied to the fully-owned `redaction.rs` and to only the added blocks of the shared files (no crate-wide reformat).
**Amendment (2026-07-04, commit 3b9a8ff):** the post-merge security review found a **fourth persist entry point** missing from the boundary list above: `summary/commands.rs::api_process_transcript` writes the raw frontend `text` into `transcript_chunks` (durable full-text, on the B4 sync allowlist) before the background summary task runs — so with redaction ON, un-redacted transcript could still land at rest via that path. The same redact-at-the-boundary treatment (same fail-safe) is now applied at the top of `api_process_transcript`, covering both the `transcript_chunks` write and the text handed to the summary task. Rule of thumb going forward: transcript text persists via **four** paths — `api_save_transcript`, audio import, retranscription, and `api_process_transcript` — and any transcript-wide policy must cover all four.
**Status:** Accepted (redaction half of C6 merged, OFF by default; retention half tracked separately).

---
## ADR-0016 — Telemetry (PostHog) key is build-time injected; no vendor keys in source
**Context:** `analytics/commands.rs::init_analytics` hardcoded upstream Meetily's PostHog project key (inherited with the v0.4.0 base), so a user who opted into Mityu's analytics shipped telemetry to the upstream vendor's PostHog project — contradicting `PRIVACY_POLICY.md` (bluedev as the data recipient) and CLAUDE.md §3 (telemetry opt-in, content-free, disableable). The 2026-07-04 repo audit flagged this as a pre-GA blocker; it was not covered by `docs/RELEASE_CHECKLIST.md`.
**Decision:** The PostHog project key is **compile-time injected** via `option_env!("MITYU_POSTHOG_API_KEY")` in `frontend/src-tauri/src/analytics/commands.rs`; no telemetry key may be committed to source. Unset / empty / whitespace-only (an unset GitHub secret renders as an empty string) ⇒ `init_analytics` builds the disabled `AnalyticsConfig::default()` — the opt-in preference is still recorded and the UI flow is unchanged, but `AnalyticsClient` constructs with no inner PostHog client and every track/identify call is a local no-op (the pre-existing `enabled && !api_key.is_empty()` guard, now pinned by a test). The opt-in posture is untouched: default remains OFF (`AnalyticsConfig::default().enabled == false`; frontend `analyticsOptedIn` defaults false), and the `SENSITIVE_ANALYTICS_KEYS` strip keeps meeting content out of events. Release builds obtain the key from the `MITYU_POSTHOG_API_KEY` GitHub secret, wired into the `env:` of every `build*.yml` tauri-action step. The frontend `identify()` payloads now report the real app version via `getVersion()` (AnalyticsProvider + AnalyticsConsentSwitch) instead of a hardcoded `'0.4.0'`.
**Consequences:** No Mityu build can send telemetry to the upstream vendor's project; local/dev/CI builds are telemetry-silent by construction; enabling GA telemetry becomes a deliberate release step (create a bluedev-owned PostHog project + set the secret — `docs/RELEASE_CHECKLIST.md` §6). Cost: forgetting the secret silently ships telemetry-off binaries (accepted: fail-private). Tests: `effective_api_key` unit tests (commands.rs) + an `AnalyticsClient` empty-key no-op test (analytics.rs).
**Status:** Accepted (2026-07-04).

---
## ADR-0017 — CI gates made real: triggers cover working branches, native deps installed, clippy advisory-on-warnings
**Context:** `.github/workflows/ci.yml` (added with the pack, 2a89cf4) was decorative: push triggers covered only `main|feat/**|fix/**`, so the active `chore/bootstrap-phase0` branch never ran CI across the first 26 fork commits; the ubuntu `rust` job installed none of the native deps the workspace needs (webkit2gtk/appindicator/rsvg/alsa/openblas — see `build.yml`'s ubuntu install step), so `cargo clippy`/`cargo test` could not even compile; `clippy -- -D warnings` was guaranteed red against the ~140-warning upstream baseline (recorded in ADR-0015); and checkout pulled the `backend/whisper.cpp` submodule recursively although the app builds whisper via the `whisper-rs` crate (the submodule serves only the archived legacy backend, ADR-0001).
**Decision:** (1) Push triggers now include `chore/**` and `docs/**`, plus `workflow_dispatch:` for manual runs (the PR trigger already existed). (2) The rust job installs the ubuntu native deps — the unpinned superset of `build.yml`'s 22.04 list; CI compiles and tests, it does not ship binaries, so webkit version pinning is unnecessary here — and adds `swatinem/rust-cache` so repeat runs are incremental. (3) The clippy gate drops `-D warnings` to plain `cargo clippy --all-targets`: deny-by-default (correctness-level) lints still fail the job, warnings are advisory until the upstream baseline is burned down. Restore `-D warnings` after the post-Phase-0 dead-code deletion (ADR-0004), which removes a large share of those warnings. (4) Checkout no longer fetches submodules. (5) **Discovered on the first live run:** the file had never even parsed — GitHub rejected it outright with "workflow file issue" on every push (including non-matching branches), because `hashFiles()` is a **steps-context-only** function (evaluated on the runner) and the `server-isolation` job used it in a **job-level** `if`; additionally `with: { components: rustfmt, clippy }` is a YAML flow mapping, so `clippy` parsed as a stray null-valued input instead of a second component. Fixed by moving the hashFiles guard onto the step and quoting `components: "rustfmt, clippy"`. So the pre-fix CI was doubly decorative: wrong triggers AND an invalid file.
**Consequences:** Every push to a working branch now compiles the workspace, runs correctness-level clippy, and executes the full test suite on Linux. The first uncached run is long (whisper.cpp/llama.cpp source builds on a 2-core runner); cached runs are much faster. Known cost: new warnings are not blocked until `-D warnings` returns (tracked follow-up tied to the ADR-0004 cleanup).
**Status:** Accepted (2026-07-04).

---
## ADR-0018 — Opt-in Integrations hub: connectionless core, calendar metadata as light opt-in, meeting bot as future heavy opt-in
**Context:** ROADMAP fixed "Google Meet/Zoom/Teams *API* integrations" as out-of-scope and CLAUDE.md §1 positioned the product as "not a meeting bot"; capture works integration-free via system audio. The owner (2026-07-05) wants these capabilities available **as user-chosen options** without changing the default posture: the app ships deliberately connectionless; an **Integrations** section lets a user consciously enable a connection after reading its terms; keeping everything off (fully manual operation) remains a first-class, permanently supported mode.
**Decision:** Adopt a three-tier capture/enrichment model — all integration state per-workspace and OFF by default:
- **Tier 0 — Local capture (the default and the product):** mic + system audio, zero external connections. Unchanged; every feature must keep working with all integrations off (local-first invariant, CLAUDE.md §0.1). Manual meeting naming/management stays fully supported forever.
- **Tier 1 — Calendar metadata (light opt-in):** read-only Google Calendar / Microsoft 365 access to enrich meetings with title/time/attendees (and optionally prompt "meeting starting — record?"). Runs entirely **on-device**: the client calls the vendor API directly, nothing transits bluedev infrastructure; OAuth tokens live in the OS keychain (ADR-0011 pattern); read-only scopes only; disconnect deletes local tokens. **KVKK assessment:** attendee names/emails are third-party personal data, but processing is client-side, for the user's own purpose, and bluedev never receives it — manageable with in-app disclosure + a privacy-policy section. **Placement decision:** although KVKK-manageable, it ships **in the Integrations section behind explicit consent**, not silently in the core flow — any external connection contradicts the "deliberately connectionless" promise unless the user chose it. (The owner may later promote it into the core flow; this ADR records the conservative default.) Earliest implementation slot: post-C8 (the ROADMAP 60–90 "one export/integration" window).
- **Tier 2 — Meeting bot (heavy opt-in, Phase 2+):** a call-joining bot for Zoom/Teams/Meet offered as an Integrations option behind an explicit terms-of-service acceptance. Hard constraints acknowledged now: it cannot be purely local (requires server-side or vetted third-party bot infrastructure → depends on EPIC D); vendor OAuth app reviews / marketplace approvals apply; the bot must announce itself in-call (vendor policy + transparency); KVKK is materially heavier — bluedev or its infra provider becomes a **data processor** for meeting audio (DPA, retention, EU residency per E2, effective consent of all participants). Honest value over Tier 0: capturing meetings the user does **not** attend, capture without the desktop app running, and per-participant server-side diarization. **This ADR fixes only the direction and the consent frame; Tier 2 requires its own detailed ADR + /security-review before any code.**
**Consequences:** CLAUDE.md §1 is refined to "not a meeting bot **by default**". ROADMAP's out-of-scope line now points here. BACKLOG gains **EPIC G** (G1 integrations hub + consent framework, G2 calendar metadata, G3 meeting bot) gated on C8 (G3 additionally on D5). Phase-1 sequencing (A5, C1–C8) is untouched. The privacy policy needs an Integrations section when G1 ships. Product story: connectionless by birth; connecting is always the user's conscious, revocable choice — competitors' convenience stays reachable without giving up the differentiator.
**Status:** Accepted (2026-07-05 — direction + consent frame; each tier's implementation still requires its own design + security review).

---
## ADR-0019 — C1 foundations started ahead of the A5 gate; summaries + action_items schema
**Context:** The owner explicitly instructed (2026-07-04/05) starting BACKLOG C1 ahead of the A5 Phase-0 validation gate. Scope discipline for this exception: only **transcription-quality-independent foundations** may land — the §4 draft types (C1.1, `summary/draft.rs`), the schema (this ADR / migration `20260706000000`), the repositories, and the HITL review/approve mechanics; **C1 completion and gate C8 remain gated on A5** (no structured-summary feature ships as done until Phase-0 passes). The C1 requirement itself is unchanged: every AI-generated summary block and action item is a DRAFT carrying a mandatory `source_chunk_id` back to transcript evidence (CLAUDE.md §0.5, `docs/CONTRACTS.md` §4), and `docs/DATA_MODEL.md` already promised a first-class action-item table as "a future migration".
**Decision:** Create two new SYNCED-class client tables in migration `20260706000000_add_summaries_and_action_items` (additive, forward-only, idempotent-by-construction — `IF NOT EXISTS` everywhere; documented DOWN in the header):
- **`summaries`** — ONE row per meeting (`meeting_id` UNIQUE, CASCADE on meeting delete): `status` ('draft'|'approved'), `model`, `template_id`, `sections` TEXT NOT NULL holding the §4 JSON (array of `Section{title, blocks}` with per-block `{id, type, content, source_chunk_id, status, original_content}`), `generated_at`, HITL fields `approved_at`/`approved_by`, and the full common/sync column set (`workspace_id` DEFAULT 'local', `created_at`/`updated_at`, `updated_by`, `rev` DEFAULT 1, `deleted_at`).
- **`action_items`** — many rows per meeting (CASCADE on meeting delete): `text`, `assignee?`, `due?`, `status` ('draft'|'approved'|'edited'|'rejected'), **mandatory `source_chunk_id`**, `position` (display order), `original_text` (pre-edit preservation), same common/sync column set. Indexes: `idx_summaries_workspace_meeting`, `idx_action_items_workspace_meeting`, `idx_action_items_workspace_status`.
Rejected alternatives, one line each: **reuse `meeting_notes`** — it is the dormant per-meeting *user-notes* table (human content, no HITL/source-link semantics); **extend `summary_processes`** — upstream-churn surface whose process-status lifecycle (PENDING/COMPLETED/FAILED) is orthogonal to HITL draft→approved, and its `result` JSON is the legacy shape we are superseding; **per-block rows (`summary_blocks` table)** — there is no `summary_block` sync entity (`SyncEntity` has Summary/ActionItem only), and the 1-row-per-logical-unit mapping with embedded JSON follows the `transcripts`/`meeting_notes` precedent (1:1 table↔sync-entity preferred).
**No FK on `source_chunk_id` → `transcripts(id)` (deliberate):** retranscription (`replace_meeting_transcripts`) deletes+reinserts a meeting's segment rows, so ON DELETE CASCADE would silently destroy approved evidence links and RESTRICT would block retranscription; resolvability is instead enforced at the repository layer at write- and approve-time. `meeting_id` DOES cascade (house precedent).
Five adopted implementation decisions for the C1 pipeline built on this schema: (1) the structured `summaries` row is **authoritative when the structured-summary flag is ON**, with the legacy `summary_processes.result` still written as a rollback path, and C4 exports emit **approved content only**; (2) **retranscription downgrades** that meeting's blocks/action items to `draft` (evidence re-anchoring required) with a UI warning; (3) v1 structured mode **skips the translate/normalize passes** (they operate on the legacy text path); (4) provider **JSON mode is auto-selected per provider** capability, never a user-facing setting; (5) HITL accountability via `approved_by`/`approved_at` + `tracing` logs now — a local audit **table** is deferred to the server-side append-only audit log (D2). Data classification: **draft rows are local-only sync class; approved rows become sync-eligible in Phase 2** (wire promotion is policy, per docs/MULTITENANCY.md).
**Consequences:** Older binaries are untouched (purely additive tables they never reference — sync-compatibility note in the migration header and `docs/DATA_MODEL.md`); `docs/DATA_MODEL.md`'s client table map now includes both tables (summaries ↔ sync `summary`, action_items ↔ sync `action_item`) and the "future migration" promise is fulfilled. Proven by `tests/migration_c1_summaries.rs` (fresh-DB exact shape + indexes, populated-prior upgrade preserving data with EMPTY new tables, ledger-guarded re-run no-op, CASCADE-on-meeting vs no-FK-on-chunk semantics); `db_encryption_sqlcipher.rs` `ALL_TABLES` extended 8→10. Cost: two tables exist ahead of their repositories (next C1 step); until the approve flow lands, nothing writes them.
**C1 gate review (2026-07-05):** the full C1 chain (commits `9111412..152c43f`, all six steps C1.1–C1.6) passed both gate reviews with **no blockers**. `multitenancy-guardian`: PASS — every C1 SQL statement is `workspace_id = ctx.tenant_id`-scoped (reads, writes, and `resolve_source_chunk_ids`), identity is resolver-only (`AuthContext` not `Deserialize`), `source_chunk_id` is validated resolvable at write AND re-validated at approve (cross-tenant source-injection negative test passes), generation always forces `Draft`, the "AI-generated · review required" banner is non-dismissable, and the opt-in flag is default-OFF with a byte-identical legacy path. `security-privacy-auditor`: PASS, no HIGH/MED — **no path exists where un-redacted transcript text reaches an LLM provider when redaction is enabled** (two independent fail-closed redaction gates: per-segment re-redaction in the structured branch returning Terminal/FAILED on a config-read error, plus the redacted assembled-text on the degrade-to-legacy path); BYOK keys stay keychain→HTTP-header only, logs/errors are content-free, drafts are at-rest under SQLCipher.
**Accepted, tracked Phase-2 hardening (from the gate review — not C1 blockers):** (W1) the retranscription downgrade hook runs the summaries and action-items `downgrade_to_draft` as two non-atomic statements after the replace transaction commits; if the second errors the transient state is inconsistent, but the whole call returns `Err` (retranscription reported failed), `downgrade_to_draft` is idempotent so a retry re-runs both cleanly, and approve-time source re-resolution refuses any stale-approved item on the next approve — the backstop. Make the two downgrades one transaction when the repository executor is generalized in Phase 2. (W2) `approve_summary`/block-status write-backs are read-modify-write with no `AND rev = <read_rev>` guard; unreachable under the Phase-1 single-writer local SQLite model (documented in `summary_draft.rs`), add optimistic-concurrency `rev` predicates when the Phase-2 multi-writer server lands. (LOW, out of C1 scope) a user-configured plaintext `http://` custom/Ollama provider endpoint sends over cleartext — pre-existing provider-config behavior, not introduced by C1; surface a non-TLS warning at the provider-config settings surface.
**Status:** Accepted (2026-07-05). C1 code-complete and gate-reviewed (guardian + security PASS); **feature-level "done" and gate C8 remain A5-gated** — structured-summary output quality is unproven until the Phase-0 recordings run.

---
## ADR-0020 — Model licensing verified (whisper + Parakeet); GA-readiness audit recorded
**Context:** CLAUDE.md §9 requires verifying and recording the license/terms of any STT model before commercial distribution; no prior ADR had done this for whisper large-v3 or NVIDIA Parakeet-TDT despite both being core to the product. A 2026-07-07 commercial-launch-readiness audit (5 parallel read-only agents + direct verification against this repo @ `b28848d`) flagged this as a Tier-0 blocker alongside several other gaps; this ADR closes the licensing sub-item with an actual verification pass (web research + repo cross-check), not a restated TODO.
**Decision (verified, not assumed):**
- **Whisper models** (`tiny` through `large-v3`/`large-v3-turbo`, all quantized variants): downloaded from `huggingface.co/ggerganov/whisper.cpp` (`whisper_engine.rs:1111-1126`) — the canonical, actively-maintained ggml-conversion repo for OpenAI's Whisper. OpenAI's Whisper (code + weights) is **MIT licensed**: commercial use, modification, and redistribution are permitted with no fee, provided the copyright/permission notice is retained. No action needed beyond the existing MIT notice already in `LICENSE.md`. Supply-chain risk on this path is low (well-established third-party mirror, not a single-vendor CDN).
- **Parakeet model** (default `parakeet-tdt-0.6b-v3`): the official NVIDIA release is **CC-BY-4.0** (`huggingface.co/nvidia/parakeet-tdt-0.6b-v3`), permitting commercial use **with mandatory attribution**. Mityu currently downloads it from `meetily.towardsgeneralintelligence.com` (Zackriya-controlled CDN, `parakeet_engine.rs:646`) rather than directly from Hugging Face — that is a supply-chain concern (RELEASE_CHECKLIST.md §3, unchanged by this ADR), not a license concern: whatever host serves the file, the CC-BY-4.0 terms travel with the model itself. The community ONNX conversion RELEASE_CHECKLIST.md already names as the mirror target, `huggingface.co/istupakov/parakeet-tdt-0.6b-v3-onnx`, is **independently confirmed CC-BY-4.0** and consistent with NVIDIA's original — repointing the download URL there (matching the pattern already used for whisper) both fixes the supply-chain item and keeps a clean, verifiable license chain. **Attribution already exists in-repo**: `README.md:126` credits "NVIDIA for the Parakeet model, and istupakov for its ONNX conversion." Not yet confirmed in-app (About screen) — a shipped binary's end users may never see the GitHub README, so in-app attribution is recommended before GA even though it is not strictly required by CC-BY-4.0's "reasonable to the medium" standard.
- **FFmpeg** binaries are a separate, non-ML-model supply-chain item (own software license — LGPL/GPL depending on build flags — not audited in this pass) already tracked in RELEASE_CHECKLIST.md §3; not resolved by this ADR.
**Consequences:** The Tier-0 "model licensing unresolved" item from the 2026-07-07 audit is downgraded from a legal blocker to two small, well-scoped engineering tasks: (1) repoint `parakeet_engine.rs` to `istupakov/parakeet-tdt-0.6b-v3-onnx` on Hugging Face instead of the Zackriya CDN (closes RELEASE_CHECKLIST §3's Parakeet half), and (2) add an in-app credits/acknowledgments surface (e.g. the About screen) mirroring README.md's existing attribution. Neither blocks a sale on its own; both are cheap to close before GA. The full audit this ADR originated from is recorded in `docs/GA_READINESS.md`.
**Status:** Accepted (2026-07-07). Both follow-ups landed same-day: Parakeet URL repointed + About-screen credits added.

**Amendment (2026-07-15, summary models):** Built-in Qwen 3.5 GGUF presets are Apache-2.0; built-in Gemma 3 GGUF presets remain subject to Google's Gemma Terms and incorporated prohibited-use policy. Mityu does not bundle these weights: a user initiates each download directly from a revision-pinned Hugging Face repository. `MODEL-NOTICES.txt` ships with the installer, About exposes the terms, and the Mityu Terms incorporate the Gemma restrictions. All Qwen/Gemma downloads now require an exact manifest byte length and SHA-256 before discovery or load. This closes the engineering notice/integrity gate; counsel approval of Mityu's Terms remains external.
**Execution-test DONE (2026-07-09, the check ADR-0021 left owed):** the Windows cached binary was moved aside (to `frontend/src-tauri/binaries/ffmpeg-x86_64-pc-windows-msvc.exe.pre-lgpl-backup`, not deleted) and `build.rs` was forced to re-run. It downloaded `win64-lgpl.zip` from the `aydogandagidir/mityu` `ffmpeg-deps-8.1-lgpl` release and cached `ffmpeg version n8.1.2-22-g94138f6973-20260706` (the gyan.dev string is gone). Running that binary's `-version` proves the LGPL claim from **observed output**, not from BtbN's labelling: `configuration:` contains **`--enable-version3`** and **no** `--enable-gpl` / `--enable-nonfree`, and the GPL-only encoders are explicitly `--disable-libx264 --disable-libx265 --disable-libxvid --disable-libxavs2 --disable-libfdk-aac`. The path the app actually uses still works: `-c:a aac -f mp4` encoded 1 s of `anullsrc` to a valid 1228-byte MP4. Windows LGPL is now verified end-to-end. Still owed: the same for the **macOS** targets, which this ADR left on the Zackriya GPL mirror (no Mac available), and re-mirroring on any future FFmpeg bump. The `.pre-lgpl-backup` file should be deleted once a human is satisfied.

---
## ADR-0021 — FFmpeg supply chain: self-hosted LGPL builds (Windows, Linux); macOS still open
**Context:** `docs/GA_READINESS.md` (Tier 0 #5) flagged FFmpeg as the one remaining supply-chain dependency on a third-party host, `github.com/Zackriya-Solutions/ffmpeg-binaries` — a bare mirror repo (18-byte README, no LICENSE file, no documented source) whose release body just maps platform to filename. Inspecting the actual bundled Windows binary directly (`ffmpeg -version`, run against the already-cached local copy, not a freshly downloaded one) showed `configuration: --enable-gpl --enable-version3 ... --enable-libx264 --enable-libx265 ...` and the version string `8.0.1-essentials_build-www.gyan.dev` — a **GPLv3** build (gyan.dev's naming/config match exactly), not the lighter LGPL. Grepping every FFmpeg invocation in `frontend/src-tauri/src/audio/{encode,decoder,incremental_saver}.rs` found only `-c:a aac -f mp4` (raw PCM to AAC-in-MP4) — no `-c:v`, no GPL-only codec used anywhere in this codebase. Continuing to redistribute (and now self-host under this project's own name, per the owner's explicit instruction to mirror to "my own GitHub release") a GPL build would place a real source-availability obligation on Mityu as a commercial distributor, for functionality it never uses.
**Decision:** Repoint Windows and Linux (x64 + arm64) FFmpeg downloads to a new GitHub Release, `aydogandagidir/mityu` tag `ffmpeg-deps-8.1-lgpl`, hosting unmodified re-uploads of the pinned `n8.1` **LGPL-only** static builds from `BtbN/FFmpeg-Builds` (a widely-used, actively-maintained community CI build of upstream FFmpeg, chosen over `evermeet.cx` for macOS parity-checking because BtbN explicitly labels and separately publishes LGPL-only artifacts; evermeet.cx's build also bundles libx264/libx265). Archive layout (`<name>/bin/ffmpeg[.exe]`) is byte-identical in shape to what `find_ffmpeg_in_extracted_dir` already searches for, so only the URL constant in `get_ffmpeg_url_for_target` changed — zero extraction-logic changes. Using the LGPLv3 build avoids the GPL-only codec/build terms, but it does **not** eliminate redistribution duties: the release still needs the applicable notices/license, corresponding source for the exact build (including its enabled LGPL dependencies), and reproducible build/source-offer material as required by the distribution model. **macOS (Intel + Apple Silicon) is intentionally NOT changed by this ADR** — no equivalently-licensed LGPL-only static macOS build was identified in this pass; those two targets still resolve to the original Zackriya-mirrored URL pending a separate fix (own follow-up, not blocking this one).
**Verification performed vs. NOT performed (read this before shipping):** Checked *without executing anything*: `tar -tJf` archive listings confirming the expected `<name>/bin/ffmpeg` paths, `file` on the extracted Windows binary confirming a valid PE32+ executable, and the three re-uploaded GitHub assets' sizes matching the downloaded originals byte-for-byte. **NOT checked: the new binaries were never executed.** `ffmpeg -version` against the new download — the check that would actually prove the LGPL config (`--enable-gpl` absent) and that the binary runs at all — was blocked twice by this session's safety controls, correctly: first when the agent tried to run the extracted binary directly, then again when the agent cleared the local binary cache to force `build.rs` to re-download and run it. (The second attempt was, in effect, the same denied action by another route; the cache was restored and no further attempt made.) Note that a routine `cargo build` does **not** exercise the new URL at all: `ensure_ffmpeg_binary()` finds the cached `frontend/src-tauri/binaries/ffmpeg-x86_64-pc-windows-msvc.exe` — which still holds the OLD gyan.dev GPL build — passes `verify_ffmpeg_binary()` on it, and returns early. A build log line reading `✅ FFmpeg verification passed: ... 8.0.1-essentials_build-www.gyan.dev` therefore proves nothing about the new artifacts. **Action required before shipping a signed release (human sign-off needed):** delete the cached `frontend/src-tauri/binaries/ffmpeg-*` file, run one real `cargo build`, and confirm the fresh log line shows a `-version` banner *without* `--enable-gpl` and *without* `gyan.dev`. Until someone does that, the LGPL claim in this ADR rests on BtbN's published artifact labelling, not on observed output from the specific files now in our release.
**Consequences:** Resolves the binary-host provenance and GPL-only build problem for Windows+Linux, and removes their runtime dependency on Meetily's servers, but the FFmpeg distribution gate is not complete until the exact LGPLv3 corresponding-source/build-script/notices bundle is durably published beside the binary. Remaining open, tracked in `docs/GA_READINESS.md`: (1) that compliance bundle; (2) macOS needs its own approved binary/source path and hardware verification, or a deliberate decision to accept GPL there with the corresponding obligations; (3) these binaries have no automatic update mechanism if `n8.1` is superseded — matches the previous pinned-tag pattern, deliberately, for build reproducibility, but means a human must re-mirror and refresh the compliance bundle for every future FFmpeg bump. The Windows execution test was subsequently completed and is recorded in the dated amendment/evidence above.
**Status:** Accepted (2026-07-07).

**v1.0.4 release-hardening amendment (2026-07-14):** a later audit found the ignored local Windows cache had regressed to the old 99,264,000-byte `gyan.dev` GPL build because the build-script verifier accepted any executable whose `-version` command exited successfully. The release line now pins and verifies both `win64-lgpl.zip` (`e2757e…57db`) and its extracted `ffmpeg.exe` (`dd7570…912a`), rejects GPL/nonfree markers, and invalidates/re-downloads a mismatching cache. The obsolete `ffmpeg-sidecar` runtime downloader was removed because its fallback sources can reintroduce GPL builds and violate offline guarantees; packaged release builds accept only the bundled verified executable, while PATH/cwd fallbacks remain debug-only. The automatic replacement was observed end-to-end and the new binary again passed a 48 kHz AAC/MP4 encode/decode smoke (1,228 bytes). The v1.0.4 production matrix is Windows x64 only; macOS stays out until an approved binary/source/signing path and hardware smoke test exist. Windows binary verification is therefore green, while the LGPLv3 corresponding-source/build-script/notices publication remains a separate open distribution gate.

**v1.0.4 compliance-publication amendment (2026-07-15):** the Windows LGPLv3 corresponding-source/build-material publication gate is technically closed. The existing public `ffmpeg-deps-8.1-lgpl` release now contains the exact FFmpeg source (`41cc834ca4c8b63733b5ea9d215aed7b6cf8e29c4f96c8c61aff187d47e02937`), exact BtbN scripts (`319a39ec6585fe698544145700edf564c61b082c77d6c09dd9f49930f2567588`), recovered 2,019,293,207-byte dependency source cache (`5a9a32cdfce33c943193379fe6734cb27f6ea85083d41e230b123802d9b71b27`) and build-info/licenses/notices ZIP (`3f8b45084a966fa68ffd43b5bf798d07dc7942fde6a3de3d4b17816295b435a7`) beside the pinned binary archive. Local `SHA256SUMS`, archive inventory (230/230 entries), GitHub's stored asset digest/size, unauthenticated redirect-to-HTTP-200 HEAD requests and fresh unauthenticated full-download hashes all agreed. No tag or release metadata was created or replaced. This evidence does not constitute legal advice or counsel approval; the legal/commercial release gate remains open, as does the separate macOS provenance path outside the v1.0.4 Windows-only matrix.

---
## ADR-0022 — The workspace `[patch.crates-io]` was never applied: cpal's macOS fix restored, esaxx-rs pin dropped, system-audio platform claims corrected
**Context:** Auditing the About-screen claim "Works everywhere — Google Meet, Zoom, Teams — online or offline" (`About.tsx:120-121`, `README.md:43`) surfaced a silent build-configuration defect plus three factual errors in our own docs.
1. **The patch block was inert.** `frontend/src-tauri/Cargo.toml` declared `[patch.crates-io]` for `cpal` and `esaxx-rs`, but src-tauri is a **non-root member** of the root workspace, and Cargo honors `[patch]` only in the workspace-root manifest (root `Cargo.toml` had none). Cargo says so out loud: `cargo tree -p cpal` printed `warning: patch for the non root package will be ignored` and resolved `cpal v0.15.3` from `registry+https://github.com/rust-lang/crates.io-index` (checksum, no git source) in `Cargo.lock`. Both pins had been dead for the life of the fork.
2. **Windows never depended on that patch.** Released cpal 0.15.3 already implements WASAPI loopback: `src/host/wasapi/device.rs:568-570` ORs in `AUDCLNT_STREAMFLAGS_LOOPBACK` whenever `data_flow() == eRender`, documented at `src/host/wasapi/mod.rs:18-20` ("If you use a WASAPI output device as an input device it will transparently enable loopback mode"). That is exactly what `audio/devices/platform/windows.rs:196-251` (resolve an output endpoint) plus `audio/stream.rs:272` (`build_input_stream` on it) do. This ADR's author first concluded the opposite — that the ignored patch broke Windows system audio — and was **wrong**; the claim was disproven by reading the vendored crate source before any code was changed. Recorded because the wrong version is the intuitive one.
3. **The real cost was macOS.** Pinned rev `51c3b43` is `RustAudio/cpal#946` "Fix macos non-default audio output" (2025-02-16), a **single-file** diff to `src/host/coreaudio/macos/mod.rs`: `supported_configs()`/`default_config()` hardcoded `audio_unit_from_device(self, /*input=*/true)` even when querying `kAudioObjectPropertyScopeOutput`, so config resolution is wrong for a **non-default output device**. That is the path `audio/devices/configuration.rs` takes for macOS system capture, and precisely the Bluetooth-speaker-as-system-output scenario `audio/devices/fallback.rs:112-126` and `BLUETOOTH_PLAYBACK_NOTICE.md` fight with. The crate version at that rev is still `0.15.3`, so `[patch]` applies cleanly.
4. **The esaxx-rs pin is unresolvable.** `github.com/thewh1teagle/esaxx-rs` returns "Repository not found" (`git ls-remote`; a control repo resolves fine). Relocating the block wholesale would have broken the build.
5. **"BlackHole" is doc fiction.** `CLAUDE.md` §4, `docs/SETUP.md:13`, ADR-0013's context line, and two `.claude/` prompts all state that macOS system capture "needs a virtual device (BlackHole)". It does not: the default backend is ScreenCaptureKit (`audio/capture/backend_config.rs:76`) with a Core Audio tap alternative (`audio/capture/core_audio.rs`, cidre). BlackHole appears only as a *pattern* — classified as a virtual device and treated as wired (`audio/device_detection.rs:177-178`), and explicitly **excluded** when picking a built-in output (`audio/devices/speakers.rs:100-105`).
6. **Linux system-audio capture is broken while deb+appimage ship.** `tauri.conf.json:83-89` lists `deb`/`appimage`. But `audio/devices/platform/linux.rs:18` queries the **ALSA** host for names containing `"monitor"` (a PulseAudio/PipeWire concept ALSA does not surface) and registers them as `"{name} (System Audio)"`, while `AudioDevice::from_name` (`audio/devices/configuration.rs:66-88`) *hard-errors* on any name not ending in `(input)`/`(output)`, and `configuration.rs:155-166` matches raw cpal names exactly — the suffixed name can never round-trip. The auto path fails too: `speakers.rs:38-45` returns the ALSA **output** device, which `configuration.rs` then hunts for among ALSA **input** devices. Separately, `audio/capture/system.rs:99-103` still `bail!`s "System audio capture not yet implemented for this platform" on non-macOS; it is reachable only through `start_system_audio_capture_command` (registered `lib.rs:745`, invoked by the UI **zero** times), and `audio/system_audio_stream.rs` has no callers at all.
**Decision:** (a) Move `[patch.crates-io]` to the **root** `Cargo.toml`, carrying **cpal only**, rationale inline; leave a "do not re-add it here" note where the block used to be. (b) **Drop** the esaxx-rs pin — the target repo is gone and `esaxx-rs 0.1.10` from crates.io builds on the MSVC toolchain we ship (the existing CUDA build proves it). (c) Correct the product copy rather than the platform matrix: the About card becomes "Any meeting app"; it and the README name the supported platforms (macOS + Windows) and mark **Linux experimental**, keeping the honest mechanism sentence ("captures system audio … not a bot that joins your call", per ADR-0018). (d) Correct `CLAUDE.md` §4, `docs/SETUP.md`, and the two `.claude/` prompts to describe ScreenCaptureKit / Core Audio taps instead of BlackHole. (e) **Linux → honest mic-only** (owner's call, 2026-07-08, over the alternative of dropping the bundle targets): Mityu's field / in-person recording scenario works on Linux with the microphone alone, so `deb`/`appimage` keep shipping, but the app must stop *pretending* to offer system audio. Implemented: `devices/platform/linux.rs` no longer enumerates the unusable `"<name> (System Audio)"` monitor entries; `devices/speakers.rs::default_output_device()` returns an explicit `Err` on non-macOS/Windows targets — which `RecordingManager` already degrades correctly (`recording_manager.rs:227-236`: `Err` → `warn!` → `system_device = None` → microphone-only recording); and `devices/configuration.rs::get_device_and_config()` returns a named error for the Output scope on Linux instead of falling through to a generic "Device not found". A real PulseAudio/PipeWire capture backend remains unbuilt and is its own epic. The dead `audio/system_audio_stream.rs` / `start_system_audio_capture_command` surface is still **recorded, not fixed** (post-Phase-0 dead-code cleanup, ADR-0004).
**Verification performed vs. NOT performed (read this before merging):** *Performed:* `cargo tree -p cpal` no longer emits the ignored-patch warning and now prints `cpal v0.15.3 (https://github.com/RustAudio/cpal?rev=51c3b43#51c3b43c)`; `Cargo.lock:1106` flipped from `registry+…` to `git+…#51c3b43c54591203fe3edc9a31ff7595650f7103`. `cargo check -p mityu --lib` is **green** (1m51s; 10 warnings, all pre-existing dead-code, none new), so the post-0.15.3 rev is API-compatible with our call sites. `git show --stat 51c3b43` confirms the diff touches **one** file under `src/host/coreaudio/macos/`, therefore Windows and Linux codegen are unchanged and only macOS behavior moves. `cargo build -p mityu` green in 3m26s (full binary, not just `--lib`, not just `check` — `mityu.exe` links). `cargo test -p mityu` → **329 tests green, 0 failed** across the lib (287) and all 7 integration binaries, including `migration_workspace_sync` and `repository_tenant_scoping`. `npx tsc --noEmit` and `eslint About.tsx` clean for the copy edits (its one remaining warning, an unescaped apostrophe at line 137, predates this change — verified against `HEAD`). `git ls-remote` proves the esaxx-rs repo is gone. **Windows loopback proven empirically:** a standalone probe pinned to the same cpal rev, replicating `platform/windows.rs`'s config selection and `stream.rs`'s `build_input_stream` on the default WASAPI **render** endpoint, opened the stream and received **229 callbacks / 219,840 samples in 2 s (≈48 kHz, stereo, F32)** — **192 of them non-silent** while the probe rendered a 440 Hz tone on that same endpoint. Had cpal lacked loopback, `build_input_stream` would have failed with `AUDCLNT_E_WRONG_ENDPOINT_TYPE`; it did not. **The `CLAUDE.md` §4 Windows smoke test PASSED** (2026-07-08, dev build against the patched cpal): `RecordingManager` picked mic `Mikrofon Dizisi (Realtek(R) Audio)` and system `Hoparlör (Realtek(R) Audio)` — a **render** endpoint — opened both CPAL streams at 48 kHz / 2 ch / F32, recorded 111 s, and produced 5 transcript segments; `audio.mp4` (334 KB), `metadata.json` and `transcripts.json` were written, the meeting was persisted to SQLite and read back ("ZERO transcript chunks lost"), with **no ERROR, WARN or panic after recording start**. One segment came back as garbled Romanian-looking text: that is the known "Parakeet has no Turkish" limitation (a Phase-0 / A5 concern recorded elsewhere), not a regression from this change — the English segments transcribed correctly. `cargo clippy -p mityu --all-targets` attributes **zero** warnings to `src/agents` and none to the edited regions of `audio/devices` (its three hits there — `configuration.rs:37`/`:116`, `speakers.rs:35` — are pre-existing Windows-branch lines). *NOT performed:* **the macOS §4 smoke test.** That is the platform this patch actually changes: the #946 non-default-output bug was read in the diff but never reproduced on hardware, nor the fix observed working. No Mac was available. **The Linux changes in (e) were type-checked AND behaviourally tested on Linux (2026-07-09), in a `rust:1.83-bookworm` container** (Docker; there is no native Linux toolchain on this machine, and the C-dependency build scripts rule out cross-compiling the whole `mityu` crate). The three changed files were pulled into a tiny harness via `#[path = "/repo/.../*.rs"]` — the real committed sources, not copies, against the real cpal ALSA host with `libasound2-dev` installed and the same `[patch] cpal` pin. Results: `cargo build` clean (no warning from the repo files); `default_output_device()` returns the expected `Err` ("not supported on this platform … ADR-0022") **without touching cpal**; `AudioDevice::from_name` still rejects the old `"<name> (System Audio)"` synthetic name (the reason those entries could never round-trip); and `configure_linux_audio()` enumerates **0** devices named "(System Audio)". Only the isolated modules were built, not the whole crate; and a real end-to-end microphone record→transcript run on Linux hardware is still owed (the container has no `/dev/snd`).
**Consequences:** macOS system-audio config resolution for non-default output devices is fixed for the first time in this fork, which may interact with — or partly obsolete — the `devices/fallback.rs` built-in-speaker workaround; re-read that module during the macOS smoke test instead of assuming both are still needed. The first build after this change recompiles cpal from a git checkout (one-time). The product no longer claims a platform it does not support, and Linux users get a clear "microphone only" degradation instead of a system stream that silently records nothing. Follow-ups owed: **(1)** the §4 in-app smoke tests on macOS and Windows, **(2)** a Linux `cargo check` + mic-only smoke test, **(3)** a PulseAudio/PipeWire capture backend if Linux system audio is ever to work (own epic). Two cosmetic siblings, both harmless today and left alone: (a) `llama-helper/Cargo.toml` declares `[profile.*]` in a non-root member, which Cargo also ignores (`warning: profiles for the non root package will be ignored`) — the same class of bug as the `[patch]` one; (b) on Windows the app logs `Starting audio streams with backend: ScreenCaptureKit`, because `audio/capture/backend_config.rs`'s default is platform-independent. Nothing acts on it — `stream.rs` sets `use_core_audio = false` off-macOS and correctly falls through to CPAL, as the smoke-test log shows — but the line reads as if a macOS backend were driving Windows capture.
**Status:** Accepted (2026-07-08) — committed on `chore/bootstrap-phase0`, **not pushed, not merged**. The §4 **Windows** smoke test passed (above). Still owed before this merges: the §4 **macOS** smoke test (the platform this patch actually changes), and a **microphone-only record→transcript run on Linux hardware** (the code is type-checked and behaviourally tested on Linux in a container, but a container has no `/dev/snd`).

## ADR-0023 — Licensing & trial: Polar.sh (merchant of record) + client-side 14-day trial, with local-first guardrails

**Context:** The landing page sells Trial (14-day) / Pro ($79 one-time, 2 devices) / Business ($59/user/yr), but the binary contains zero enforcement: no payment link, no license concept, no trial clock. Any mechanism must not violate the constitution: local-first (§0.1 — the core path must work with no network), no plaintext secrets in SQLite (§7), no content to third parties (§10). Provider comparison (2026-07-11, owner decision): **Stripe** does not accept Türkiye-registered businesses; **PayPal** left Türkiye; **Gumroad** has no TR payout (and its name is banned from every user-facing surface by owner directive); **LemonSqueezy** is mid-migration to Stripe Managed Payments with TR payout unconfirmed; **Paddle** verifiably pays TR sellers but has no native license keys (would force a Keygen-class dependency or a self-hosted license server) and charges a $15 flat SWIFT fee per payout; **iyzico / PayTR** are gateways, not MoR — we would owe worldwide buyer-side VAT/sales-tax compliance plus the TR 1% e-commerce withholding, and they bring no license infrastructure (kept as a possible future TR-local channel in TRY with taksit). **Polar.sh** is a merchant of record (buyer-side tax is theirs), explicitly supports Türkiye-based sellers, is open source (polarsource/polar), and ships native license keys with activation limits, optional expiry, and a customer self-service deactivation portal. Fees: 5% + 50¢ (Starter tier for orgs created ≥ 2026-05-27), $15 per dispute; payout via Stripe Connect Express. For a TR seller the MoR model makes every sale a service export (KDV %0 + the 2026 "%100 kazanç indirimi" regime, CK 11257) — **to be confirmed with the accountant; this ADR is not tax advice.** Owner picked Polar.sh; launch version = 1.0.0.

**Decision:**
1. **Provider = Polar.sh** with hosted checkout: the landing page's buy buttons link to Polar checkout URLs. The app itself never touches payment data.
2. **Only Polar's public customer-portal endpoints are used** — they require no API secret, so nothing sensitive is baked into the binary: `POST api.polar.sh/v1/customer-portal/license-keys/activate` `{key, organization_id, label}` → 200 `{id, license_key{…}}` | 403 activation-limit | 404; `…/validate` `{key, organization_id, activation_id?}` → 200 `{status: granted|revoked|disabled, expires_at?, limit_activations, …}` | 404; `…/deactivate` `{key, organization_id, activation_id}` → 204. Sandbox host `sandbox-api.polar.sh` for tests/dev.
3. **Organization id via `option_env!("MITYU_POLAR_ORG_ID")`** (the ADR-0016 PostHog pattern): unset ⇒ the activation UI reports "licensing not configured" while trial mechanics run normally — the repo builds and runs before the Polar account exists; release builds inject the real id (GitHub secret, wired like `MITYU_POSTHOG_API_KEY`).
4. **Trial: client-side, 14 days, full-featured, no account, no card.** Anchor = first-launch timestamp stored twice — settings JSON (`licensingState`, per-workspace, the ADR-0015 `redactionConfig` pattern, **no schema migration**) and keychain entry `licensing:trial-anchor` — earliest wins, each heals the other if missing. Clock-rollback guard: a persisted high-water `last_seen_at`; effective now = max(system clock, high-water). **Honest-user stance, not DRM:** a client-side trial is bypassable by a determined user; accepted deliberately — the audience pays for updates, support and legitimacy, and the privacy promise (no phone-home) outranks enforcement strength.
5. **Expiry gates capture only — never data.** `TrialExpired`/`Revoked` blocks exactly two entry points: starting a new recording and importing audio. Opening the app, browsing/searching/exporting every existing meeting, playback, summaries of already-captured content, and settings remain available forever. This extends ADR-0014's "never lock the user out of their own meetings."
6. **Activation flow:** user pastes the key (Settings → License, or the paywall dialog) → `activate` with a stable random pseudonymous `label` (never the hostname; stored as `licensing:device-label` when the OS credential store is available) → persist key + activation_id in the **keychain only** (`licensing:key`, `licensing:activation-id`; §7 — never SQLite). Non-secret display state (masked key, plan, expires_at) is cached in the settings JSON.
7. **Validation: lazy, rate-limited, fail-open offline.** Re-validate at most once per 7 days, fire-and-forget in the background after status reads (never blocking a status read or startup on the network). Transport errors ⇒ remain `Licensed` (local-first). Explicit negatives act: `revoked`/`disabled` or past `expires_at` ⇒ `Revoked`/expired state with a re-activation path; 404 with our activation_id ⇒ the seat was freed via Polar's portal ⇒ clear local license and fall back to the trial state machine. Requests carry ids only — never content, never telemetry (§10).
8. **Deactivate** (Settings) calls the endpoint, then clears the keychain entries — frees the seat under the 2-device activation limit; Polar's customer portal offers the same self-service without us.
9. **States** (single source of truth in the Rust `licensing/` module): `Trial{days_left}` · `TrialExpired` · `Licensed{plan, expires_at?}` · `Revoked{reason}`. The UI consumes `get_licensing_status`; gated commands fail with a machine-readable `LICENSE_REQUIRED` error the UI maps to the paywall.
10. **Business ($59/user/yr) later:** identical key mechanics — `expires_at` is honored from day one — so post-launch it only needs the Polar product/benefit and copy.

**Consequences:** + fastest possible launch (no license server of ours), buyer-side global tax at the MoR, the 2-device promise enforced by Polar's activation limit, seat swaps self-service; − one new outbound endpoint (`api.polar.sh`) to document in `SECURITY_PRIVACY.md` (user-triggered activate/deactivate plus ≤1/7-day validate; ids only), *new* activations depend on Polar uptime (running installs don't), the client-side trial is bypassable (accepted above), the org id is public by design (it is in every checkout URL anyway). Gates live at the **command layer** — zero edits inside capture internals (§4). Implementation on `feat/licensing`: Rust `licensing/` module + 3 commands + 2 gates; frontend `licensingService` + trial banner + License settings section + paywall dialog; tests on both sides.

**Status:** Accepted (2026-07-11 — owner decisions: provider = Polar.sh, launch version = 1.0.0). Implemented on `feat/licensing`.

**Amendment (2026-07-11, at implementation):** The per-workspace `licensingState` JSON blob physically lives in the pre-existing — and otherwise dead — upstream `licensing` table, one marker row per workspace (`license_key = 'licensingState:<ws>'`, `signature_hash = 'licensingState:v1'`, JSON in `encrypted_key`), because the fixed-column `settings` table cannot host a new blob without the schema migration this ADR forbids; legacy upstream RSA rows are ignored via the marker filter, and the table is SQLCipher-encrypted at rest like the rest of the DB. The trial anchor is minted lazily at the first status read (the UI reads status at startup, so this coincides with first launch; pre-DB onboarding reads serve a keychain-only status that heals into the blob after DB init). Retranscription of already-captured audio is deliberately NOT gated (§5 — data is never hostage); the two gates are recording start and audio import, at the command layer only.

**Amendment (2026-07-15, privacy hardening):** Polar activation labels no longer contain the operating-system hostname. Mityu generates `mityu-<random UUID>` and stores it in the OS credential store for stable portal display; when that store is unavailable, the one-time random label is still used and activation never falls back to a hostname. This is a data-minimization change and does not alter seat validation semantics.

---
## ADR-0024 — Product Intelligence starts with local, source-resolvable Evidence Search

**Context:** The owner requested Mityu's product-intelligence layer while A5 real-audio validation and the C8 pilot gate remain open. The legacy search path uses unranked `LIKE`, returns meeting-level matches without a stable segment anchor, and can mix transcript results with legacy summary JSON that has no reliable HITL approval boundary. Starting with generative “Ask Mityu” would therefore lack a deterministic retrieval baseline and could present unreviewed AI text as authoritative.

**Decision:** The first Product Intelligence slice is Ranked Evidence Search v1. Migration `20260714000000_add_transcript_evidence_search_fts` creates an encrypted local FTS5 derived index plus a stable transcript-id routing map; it backfills existing rows and maintains the index across transcript and meeting lifecycle changes. The trusted `api_search_evidence` command accepts only a bounded literal token/prefix query, ranks with BM25, returns at most the best active segment per meeting, and joins every result back to active `transcripts` and `meetings` scoped by `AuthContext.tenant_id`. Every result includes a resolvable `source_chunk_id`, timestamp and original-text snippet. Raw rank is internal ordering metadata, not confidence. Query and snippet content are prohibited from logs and analytics. Legacy and unapproved summary text is excluded.

The FTS index remains a local-only derivative inside SQLCipher: it has no sync entity or wire representation and the multi-tenant server must not reuse it. The previous `api_search_transcripts` command remains for compatibility, but its content-bearing logging is removed and product UI uses the evidence command with debounce, stale-response suppression and exact-segment deep links.

**Consequences:** Mityu gains an offline deterministic retrieval foundation for later citation-first intelligence without an LLM or network dependency. Transcript text is duplicated inside the encrypted database, increasing disk use. ADR-0026 now defines and implements the app-controlled FTS/WAL/free-page cleanup boundary; it deliberately does not promise forensic erasure from storage layers or copies outside Mityu's control. The migration advances SQLx's forward-only ledger, so application downgrade after opening the database is unsupported. This quality-independent slice may land before A5 by owner direction, but does not complete A5 or C8.

**Status:** Accepted (2026-07-14; implementation authorized by owner, release claims remain A5/C8-gated).

---
## ADR-0025 — Approved Action Center is an approval projection, not a task tracker

**Context:** Evidence Search provides trustworthy retrieval, but users still have to open meetings individually to find reviewed follow-ups. `action_items.status` (`draft | approved | edited | rejected`) is a HITL review state, not real-world work progress. Treating `edited` as approved, adding a completion checkbox without a separate work-state contract, or interpreting free-text `due` values as deadlines would collapse distinct meanings. Repository review also found that producer-supplied status and inactive evidence needed stronger write-time enforcement before a cross-meeting view could claim “human approved.”

**Decision:** Product Intelligence slice 2 is the fully local, read-only `/actions` Approved Action Center. It returns only active `action_items.status = 'approved'` rows whose active meeting and active same-meeting transcript source all belong to the caller's tenant. Item approval is independent of whole-summary approval; `draft`, `edited` and `rejected` are excluded. Results preserve deterministic backend order and expose bounded pagination, source metadata, assignee and verbatim free-text due values. V1 adds no work-progress field, completion mutation, overdue inference, reminder, analytics content or external action.

The write boundary is hardened with mandatory Draft persistence for generated summary blocks and action items, removal of forged edit provenance, active meeting/source checks repeated after obtaining the SQLite writer transaction, and status+revision compare-and-swap updates. Approval additionally requires an active source in the same meeting and tenant. The Action Center UI reuses the exact transcript-segment jump/highlight path and describes items as AI-extracted and human-approved.

**Consequences:** Users gain an evidence-linked place for reviewed commitments without exposing drafts or inventing task semantics. A future task workflow requires a separate additive work-state contract. Offset pagination is intentionally visible and bounded; multi-writer sync may later require a cursor. The Home Dashboard's existing broader `list_open` review surface remains separate from the approved-only Action Center.

**Release-line amendment (2026-07-14):** implementation was rebased onto published `v1.0.3`. The owner subsequently approved `v1.0.4` as the release candidate for this work. Package, Tauri, Rust, lockfile and browser surfaces must remain synchronized at `1.0.4`; the published `v1.0.3` tag remains immutable.

**Status:** Accepted (2026-07-14; implementation authorized by owner, release claims remain A5/C8-gated).

---
## ADR-0026 — Verifiable local deletion covers Mityu-managed stores, not every physical replica

**Context:** Before this decision, a meeting hard-delete removed tenant-scoped SQLite rows and FTS routing/index entries logically, but SQLite ordinary pages, FTS5 merge segments and WAL have different remanence behavior. Recording folders and the WebView IndexedDB recovery cache were separate stores. Claiming physical or forensic erasure from only SQL row deletion would therefore be false. At the same time, recursively deleting an arbitrary `folder_path` would risk destroying user files or following a link outside Mityu's recording root. SSD wear-leveling, copy-on-write filesystems, snapshots, backups, swap, exports and browser/WebView storage also sit below or outside the app's authority.

**Decision:** Migration `20260714010000_enable_verifiable_local_deletion` persists FTS5 `secure-delete=1` and creates `local_privacy_maintenance`, a singleton content-free retry marker. Every pool connection enables SQLite core `PRAGMA secure_delete=ON`; the exact deletion connection verifies both controls before changing data. The tenant-scoped database delete and `required = 1` marker update commit atomically. That delete runs on a disposable `close_on_drop` connection with SQLite FK cascades disabled, explicitly deletes every child row with `workspace_id = AuthContext.tenant_id`, and never queries a foreign workspace; a malformed foreign child is left untouched instead of being discovered or cascaded away. A successful maintenance cycle then runs FTS `optimize`, requires a complete `wal_checkpoint(TRUNCATE)`, runs `VACUUM`, requires another complete WAL truncate and `freelist_count = 0`, records completion, and requires a final marker checkpoint. The marker clears only after success; startup reruns a pending cycle, so a crash after the logical commit cannot silently skip compaction.

Before the database commit, Mityu may scrub/unlink only its known recording, metadata, transcript and checkpoint artifacts in the meeting folder. New folders live below a collision-free hex-encoded workspace namespace and `metadata.json` carries the trusted `workspace_id` resolved in Rust. Erasure validates that ownership marker before touching files; a foreign marker fails closed without a database lookup. Legacy folders with a missing marker/field are accepted only for the implicit `local` workspace. The target and configured/default recording roots are canonicalized; outside-root targets, a root itself, same-workspace duplicate folder references and unsupported paths fail closed. Symlinks are unlinked but never followed. Unknown top-level user entries are retained, and the meeting directory is removed only when empty. This ordering leaves database data intact and retryable if app-managed artifact removal fails. The WebView recovery cache is logically deleted immediately after a recovered meeting is saved to SQLite, retried at startup for older/interrupted saves, and purged on explicit meeting deletion.

The automated `secure_local_deletion.rs` integration test plants a unique sentinel in domain rows, FTS, WAL-capable database storage, audio, metadata, transcript JSON and checkpoints. It proves a foreign delete is a no-op, a caller-scoped delete leaves a malformed foreign child untouched, and a same-workspace duplicate folder reference fails closed; unit coverage proves foreign ownership markers fail before artifact erasure and legacy unmarked folders are local-only. After the owner deletion and maintenance cycle it verifies all relevant caller-owned SQL/FTS rows are gone, FTS and core secure-delete are enabled, the free list is empty, WAL is absent or truncated, the sentinel is absent from SQLite/WAL artifacts and Mityu-managed files, and an unknown user file survives. Product deletion copy must always state that these checks establish the Mityu-controlled application boundary—not guaranteed physical erasure from SSD wear-leveling, COW layers, snapshots, backups, exports, swap or WebView/browser remnants.

**Consequences:** C6a's implemented/tested deletion-semantics gate is closed for v1.0.4, but A5, C8, legal, signing, updater-canary and FFmpeg source-compliance gates remain independent. Deletion can be slower because it compacts the whole local database, and it can return an error while WAL is busy; the content-free marker preserves a startup retry. Unknown user-added entries are intentionally not deleted. The migration is client-only and adds no sync entity or meeting content, but application downgrade is unsupported: FTS5 secure-delete requires SQLite 3.42+ and Mityu's bundled SQLCipher/SQLite is 3.45.3.

**Status:** Accepted and implemented (2026-07-14; C6a closed, release remains A5/C8-gated).

---

## ADR-0027 — v1.0.4 defers A5/C8 evidence without claiming that either gate passed

**Context:** On 2026-07-15 the product owner explicitly directed that the human-evidence phase should stop blocking the remaining v1.0.4 release work. A5 still has zero eligible quiet/field/multi/jargon pairs, and C8 has not been performed against an immutable signed candidate. An AI agent cannot manufacture participant permission, human-corrected references, a pilot judgment or a signature. At the same time, v1.0.4 is a Windows x64 patch built on the already-published v1.0.3 line; its implementation scope (source-resolvable search, approved-action projection, deletion hardening and release engineering) is independent of a claimed WER or field-performance result.

**Decision:** For **v1.0.4 publication only**, A5 and C8 are reclassified from pre-publication blockers to **DEFERRED / NON-BLOCKING evidence debt**. This is a sequencing exception, not a PASS, GO, CLOSE or waiver of the underlying acceptance criteria:

1. A5 remains `NOT EVALUATED (0/5)` and C8 remains `NOT PERFORMED`. No report, reviewer identity, consent record, pilot result or signature may be invented.
2. v1.0.4 must not claim measured WER/CER, verified Turkish/jargon accuracy, diarization quality, noisy-field suitability, an accuracy SLA, or pilot-proven product value. Product and release copy must say that accuracy varies with language, speaker, microphone, overlap and environment, and that users must verify transcripts and AI drafts against source evidence.
3. A5 remains mandatory before any target-environment/industrial-field positioning or measured accuracy claim. C8 remains the Phase 1 exit gate. Existing dependencies from Phase 2, on-device agents and integrations to C8 remain in place; this exception does **not** unlock D1, F0, G1 or any other post-C8 work.
4. Signing, updater-key continuity, exact-SHA protected CI, legal/commercial approval, legacy-lead disposition, signed-artifact inspection and the post-publish v1.0.3 updater canary are unchanged and remain fail-closed.
5. Public benchmarks, synthetic/TTS speech and AI review may be reported only as supplemental engineering evidence; they never count toward A5 or C8.
6. The current v1.0.4 retention behavior must be disclosed honestly: raw meeting audio is kept locally until the user deletes the meeting. ADR-0005's configurable transcript-only target remains open C6 work; this release exception does not pretend it was implemented. Because changing post-capture deletion touches the high-risk playback/retranscription pipeline and still needs the required Windows + macOS recording smoke matrix, it is not silently added to this Windows patch.

**Consequences:** Remaining v1.0.4 engineering and release steps can proceed without false human evidence, but transcription-quality and pilot-validation risk is explicitly accepted as unresolved product evidence debt. Support/refund risk is higher, and field/enterprise accuracy claims remain prohibited. Later patches may carry the same honest limitation, but no release may call A5/C8 complete or broaden validated scope without the real human artifacts.

**Status:** Accepted (2026-07-15; explicit product-owner sequencing direction). A5 and C8 are deferred for v1.0.4 publication, not passed.

---

## ADR-0028 — Remove the unsupported clinical-note template from v1.0.4

**Context:** The inherited `psychatric_session.json` template (including the filename typo) asked an AI system to produce psychiatric SOAP content, diagnoses, medication changes, safety/risk material and a confidence score. Mityu has no clinical validation, medical-device positioning, clinical safety case or target-environment evidence supporting that workflow. A5 and C8 are explicitly deferred under ADR-0027, so silently bundling a high-stakes clinical template would conflict with the release's claim limits and human-review safeguards.

**Decision:** Remove the psychiatric-session template from the shipped template allowlist and delete the inherited file for v1.0.4. Public release notes state that Mityu is not validated or positioned for clinical documentation or medical decision-making. General meeting templates remain available. Reintroducing a clinical workflow requires a separate product/legal/safety decision, domain-expert validation, appropriate consent/retention controls and explicit acceptance criteria; it cannot arrive as an ordinary prompt-template edit.

**Consequences:** Existing installations that independently created custom templates are not modified, but the unsupported built-in clinical choice is no longer shipped. This reduces high-stakes misuse risk without changing the core audio or database schema.

**Status:** Accepted (2026-07-15; v1.0.4 release-scope hardening).

---

## ADR-0029 — v1.0.4 ships updater-signed only; Windows Authenticode deferred (owner decision)

**Context:** The v1.0.4 hardened release workflow was authored to require Windows Authenticode code signing via DigiCert KeyLocker (five `SM_*` secrets) *in addition to* the Ed25519 Tauri updater signature, behind a fail-closed preflight that refuses to build a release unless all seven signing secrets are present. A read-only investigation on 2026-07-15 established that **no `SM_*` DigiCert secret has ever existed in this repository** — the account is a personal user (not an org, so no org secrets), the `Production` environment holds zero secrets, and repository scope holds only `TAURI_SIGNING_PRIVATE_KEY` + `TAURI_SIGNING_PRIVATE_KEY_PASSWORD`. Therefore v1.0.0–v1.0.3 were **never Authenticode-signed**; every prior release shipped with only the Ed25519 updater signature (the `.sig` assets, which are present on all four releases) and consequently triggers the Windows SmartScreen "unknown publisher" prompt. DigiCert Authenticode was thus a *new* v1.0.4 hardening that the fail-closed preflight made mandatory for the first time. The product owner (2026-07-15, via explicit decision) chose to ship v1.0.4 the same way as the already-published v1.0.x line — updater-signed only, no Authenticode — rather than acquire and provision a code-signing certificate now.

**Decision:** For v1.0.4, make Windows Authenticode **optional and off**, while keeping the Tauri updater signature **mandatory and fail-closed** so the v1.0.3 → v1.0.4 auto-update chain is preserved. Three surgical release-pipeline edits:
1. `release.yml` "Validate updater signing configuration" preflight now requires only `TAURI_SIGNING_PRIVATE_KEY` + `TAURI_SIGNING_PRIVATE_KEY_PASSWORD`; the five `SM_*` inputs are no longer required.
2. `release.yml` dispatches `build-windows` with `sign-binaries: false`.
3. `build.yml` "Validate release-mode contract" still requires a release id in production mode but no longer mandates platform signing.

No new secret and no updater-password recovery are needed: the updater key + password already live as **repository** secrets (they signed v1.0.3) and are readable by the `Production`-environment `create-release`/`build` jobs (an environment job can read repository secrets). With `sign-binaries: false`, every DigiCert step is skipped by its existing `&& inputs.sign-binaries` guard, `frontend/src-tauri/scripts/sign-windows.ps1` takes its documented no-op branch (`DIGICERT_KEYPAIR_ALIAS` unset **and** `MITYU_REQUIRE_WINDOWS_SIGNING` unset ⇒ "Skipping signing", exit 0), and the Authenticode-verification step is skipped — while the updater `.sig` + `latest.json` are still produced (they are gated on `production-release`, not `sign-binaries`) and still verified against the baked public key with `minisign-verify`, so an unsigned or key-mismatched updater artifact still fails the release **closed**.

**Consequences:** v1.0.4 installers carry no Windows publisher identity, so users continue to see the SmartScreen "Windows protected your PC / unknown publisher" prompt exactly as on v1.0.0–v1.0.3 (More info → Run anyway). Product, release and landing copy must not claim a verified publisher, Authenticode, or a "signed installer" in the publisher-identity sense; **"updater-signed" is the accurate claim.** This is a support/trust cost accepted for a paid product and should be revisited. Re-enabling Authenticode later (the deferred "option A") requires acquiring a code-signing certificate (OV/EV, identity validation, a DigiCert KeyLocker account), provisioning the five `SM_*` secrets in `Production`, and reverting these three edits (set `sign-binaries: true`, restore the `SM_*` preflight requirement, restore the production platform-signing requirement). The `Production` reviewer gate, `main`-only deployment policy, exact-SHA protected CI, legal/commercial sign-off, legacy-lead disposition, updater-artifact inspection, and the post-publish v1.0.3 updater canary are all unchanged and remain fail-closed. Migrating the updater secrets from repository scope to the `Production` environment (the RELEASE "Production key governance" item) is deferred together with Authenticode: it is not required for this release because environment jobs can read repository secrets, and it still needs the original updater-key password, which is unavailable locally.

**Status:** Accepted (2026-07-15; explicit product-owner signing-scope decision for v1.0.4). Updater chain preserved; Authenticode deferred, not waived.

---

## ADR-0030 — Local learning: the HITL correction signal becomes plain-language rules, not weights

**Context:** The owner asked (2026-07-16) whether to build a self-improving / learning agent system into Mityu. Auditing the code first changed the answer twice, so both wrong turns are recorded — in each case the intuitive version is the wrong one.

1. **The capture half already exists and is good.** Every AI-drafted block keeps `original_content` beside the human's `content` (`summaries.sections` JSON), every action item keeps `original_text` beside `text`, with a `draft|approved|edited|rejected` state machine (`summary_draft.rs:204-217`), 11 Tauri commands (`lib.rs:712-722`), and source-evidence anchors re-resolved at approve time (`summary_draft.rs:643-656`). **But no generation path has ever read it**: `original_content`/`original_text` appear only in the repositories that write them, the type definitions, the UI that displays them, and tests. The signal is captured and inert. The gap is consumption, not capture.
2. **Three defects make today's signal unusable as-is.** (a) `upsert_draft` rewrites `sections` **wholesale** (`summary_draft.rs:323-338`), so regenerating a summary destroys every prior `original_content` — the signal evaporates exactly when the user iterates most. (b) A rejection records `status='rejected'` and no rationale: "this was wrong" is not teachable, "wrong because X" is. (c) `original_content` is set only on the *first* edit (`summary_draft.rs:569-571`).
3. **"N=0 users ⇒ nothing to learn" was wrong.** That reasons about *aggregate* learning. Local-first learning is **per-user**: customer #1 learns from customer #1's own corrections — "1 user × repetition", not "many users". It also cannot be retrofitted onto usage that was never recorded, so the system must ship **active**, or every early customer's first month is lost.
4. **"A self-modifying policy violates §0.5 HITL" was mostly wrong, and the code says why.** `upsert_draft` **forces** `status='draft'` (`summary_draft.rs:324,347`) and `approve_summary` requires every non-rejected block approved (`:591-678`). A learned rule can therefore only change the shape of a **draft**; a human still approves every block. The output gate bounds the blast radius. What survives from that objection is not safety but **reproducibility**: EU AI Act Art.50 and the dispute-evidence claim require knowing which rules shaped a given summary.
5. **"We can't measure it" was mislocated.** It was tied to Phase-0, which is at **0 clips** — and which measures WER/CER/term-recall, i.e. **not summary quality at all** (ADR-0019 gates summary quality on a gate that does not measure it; that inconsistency is real and independent of this ADR). But the learning objective needs **no labeled data**: *correction burden* — normalized Levenshtein(`original_content` ↔ `content`) plus rejected-block rate — is computable from rows the app already writes.
6. **Two walls do not move.** **Weights** (fine-tune/LoRA/RL): KVKK/GDPR erasure is not satisfiable against a trained model — "machine unlearning" is a research topic, not a product feature — and under BYOK we do not own the model anyway. **Cross-tenant / cloud aggregation**: violates §0.1 and §10. Both are rejected on legal/architectural grounds, not preference.

**Decision:**
1. **Learning is data, not weights.** Learned state = plain-language rules in SQLite, retrieved and injected into the prompt at generation. Deterministic, inspectable, and erasable with one `DELETE`.
2. **`correction_events`** — a new **append-only** table (append-only enforced at the repository layer; SQLite cannot express it without a trigger, and a trigger would fight the CASCADE in §11). One row per human HITL action (`edit`/`reject`/`approve`/`restore`) carrying the draft text, the human text, and the generation context (`template_id`, `model`, `block_type`, `section_title`). It is a **separate table specifically to survive `upsert_draft`'s wholesale rewrite** — that is the fix for defect 2(a), not a convenience.
3. **`reason`** on correction events; the reject UI collects it, optionally, never blocking the reject. Fixes defect 2(b).
4. **`learned_rules`** — derived rules. `status`: `proposed|active|dismissed`. `origin`: `mined_deterministic|mined_llm|user_authored`. `rule_text` is **plain language**, user-editable and user-deletable. `evidence` = JSON id list of the correction events that produced it.
5. **`summaries.applied_rules`** — a snapshot of `{rule_id, rule_text, scope}` written at generation time. **The text, not just the ids**, because rules are editable: an id alone cannot reproduce a six-month-old summary. This is the Art.50 requirement and the precondition for §7.
6. **Injection at `build_structured_system_prompt`** (`summary/structured.rs:313-343`), deterministic ordering by rule id. **The same seam `custom_prompt` needs** — it is currently threaded to `api_process_transcript` but consumed only *after* the structured block ends (`service.rs:607/613/660`), so with `structuredSummaries` defaulting **true** the user's custom prompt is **silently dead on the default path** today. Fixing that is free once this seam exists, and the manual knob must work before the automatic one is credible.
7. **Auto-activation is permitted, behind `learningConfig.autoActivate` + `autoActivateMinSupport` (default 3)** — permitted *only* because §4's output gate bounds it and §5 makes it reproducible. Mined rules are otherwise born `proposed`. `user_authored` rules are born `active`: the user writing the rule *is* the approval.
8. **Miners: deterministic first** (term substitution, section rejection, length preference) — free, and the most legible to the user, so they carry the product story. **LLM miner** via the existing BYOK provider for subtler patterns: **no new privacy surface** (the transcript already goes to that provider for summarization), and Ollama users stay fully local. Both emit `ProposedRule` = data with no apply method, per **ADR-0013**.
9. **Objective = correction burden**, computed from `correction_events`, surfaced in-product ("Mityu learned 12 rules from your 47 corrections; your correction burden dropped 31%"). `eval-harness/src/metrics.rs` already holds Levenshtein + Turkish-aware normalization, and `eval-harness` already depends on the core crate (`eval-harness/Cargo.toml:28`), so moving that code **into** the core is a dependency-correct one-way refactor both sides consume. **This unblocks measuring the learning system without the Phase-0 clips.**
10. **Erasure asymmetry (the whole argument for data-over-weights).** `correction_events.meeting_id` **CASCADEs**: deleting a meeting deletes the corrections derived from it, so "delete my data" stays a real `DELETE` with no residue. The `learned_rules` derived from them **survive deliberately** — a rule is an abstraction the human approved ("call it 'takip'"), carries no transcript text, and is independently deletable. Evidence id lists may therefore dangle; readers **must** degrade to "kanıt silindi", never error.
11. **Both new tables are LOCAL-ONLY by policy** and get **no `SyncEntity`** in this ADR. `correction_events` stores raw meeting content (the draft and the human's rewrite of it), making it the most sensitive table in the schema after `transcripts`. Phase-2 sync policy for corrections and for rules is explicitly **out of scope and owed**.

**Verification performed vs. NOT performed (read this before relying on any of it):** *Performed.* Every code claim above was read at `HEAD` (`0a503be`, `main`) and is cited to file:line. The four new identifiers (`correction_events`, `learned_rules`, `applied_rules`, `learningConfig`) were confirmed absent from **every** schema lineage this app can encounter — all 13 pre-existing files in `migrations/` and the legacy `backend/app/db.py` import path — which is what makes the unguarded `ADD COLUMN`s safe by construction. **The full 14-file migration chain was executed in order against a scratch SQLite database and applied clean**; `PRAGMA table_info` confirms both new tables with the intended columns/defaults, `summaries.applied_rules` and `settings.learningConfig` present, all five indexes created, and `PRAGMA foreign_key_list(correction_events)` reporting `-> meetings on_delete=CASCADE`. **§10's erasure asymmetry was proven empirically, not asserted:** with `PRAGMA foreign_keys=ON`, inserting a meeting + 2 correction events + 1 derived rule and then `DELETE FROM meetings` took `correction_events` 2 → 0 (meeting content is really gone) while the `learned_rule` survived with its `evidence` list `["e1","e2"]` now **dangling** — which is precisely why readers must degrade to "kanıt silindi" rather than error — and the rule itself was then removed by a single `DELETE`. That contrast *is* the data-over-weights argument, now demonstrated. **The app's real migration path is green:** `cargo test -p mityu --test migration_workspace_sync` → **4 passed, 0 failed** with this file in the directory, i.e. through `sqlx::migrate!` itself, not a hand-rolled runner. Two of those four are the ones that matter here: `rerunning_full_migrator_is_a_noop` demonstrates the ledger guarantee that the non-idempotent `ALTER`s below depend on, and `populated_db_upgrade_backfills_and_preserves_data` demonstrates an existing populated database upgrading across this migration without data loss. *NOT performed, at the time this paragraph was written (A1).* **No Rust or TypeScript existed yet** — §2–§9 unbuilt; no clippy, no tsc, no smoke test. (§2 has since landed — see the A2 amendment below; §3–§9 remain unbuilt.) The scratch chain test additionally ran through **Python's `sqlite3`, not SQLCipher**, so the encrypted-at-rest path is covered only indirectly (by the `migration_*` suite above, which the app's own `db_encryption_sqlcipher` test complements — not run here). The schema is therefore **unproven against the repository layer** — in particular, whether `subject_id` can stay polymorphic across summary blocks (ids inside JSON) and action items (real rows) with no FK, following the `source_chunk_id` precedent (`20260706000000` header), is a design claim only A2 can confirm. **The two `ALTER`s are NOT SQL-idempotent** (SQLite has no `ADD COLUMN IF NOT EXISTS`): a second execution of this file would fail on duplicate columns. That is by construction and is protected *solely* by the sqlx ledger — identical to what `20260702000000` and `20260704000000` already rely on — but it means this file must never be run by hand against a live database. Treat this ADR as an accepted *decision* with a *validated schema* and an *unbuilt* implementation.

**Consequences:** **+** The correction signal becomes durable (survives regenerate), minable, and measurable with an objective that needs no golden dataset and no Phase-0 clips; `custom_prompt` gets fixed as a side effect of the injection seam; the differentiator is demoable from customer #1 because it is per-user; the rules screen doubles as the KVKK/AI-Act defence (opaque "our AI learns you" is exactly what cannot be defended). **−** One new table pair, one column on `summaries`, one on `settings`; `correction_events` holds meeting content (SQLCipher-encrypted at rest like the rest of the DB, LOCAL-ONLY, cascading on meeting delete); the LLM miner spends BYOK tokens (on-demand, user-triggered, never background); rule state is per-workspace, so Phase-2 must decide whether rules sync — recorded as owed above. **Scope note:** this ADR touches the summary/prompt path and the DB schema; per §10 it must not be combined with any audio-pipeline change.

**Status:** Accepted (2026-07-16 — owner directive: build it, and ship it active so customers can see the difference). **Implementation: §2–§7, §9, E1, D2, the deterministic §8 miner, and the LLM §8 miner's PARSER are built and verified — `cargo test -p mityu --lib` = 384/0 in the real crate on the owner's VS build (2026-07-18), plus frontend Vitest 96/96 + visual. The loop is closed, fed, and its result is on screen. C2's provider-call GLUE (~100 lines: gather corrections → prompt → BACKGROUND provider call on approve → persist) is now LANDED and verified (2026-07-18 — `cargo test -p mityu --lib` = 387/0, clippy clean; see the "C2 glue landed" amendment below), so ADR-0030's implementation is complete; the shared `assemble_provider` helper now backs both summarization and the miner. Owed only: the in-app smoke test (a live model + running GUI — turn on the toggle, approve, see a "Suggested by the model" proposal) and the integration-test BINARIES' re-run: green pre-`cargo clean`, blocked by a Tauri GUI-crate rlib-link quirk PROVEN not to be a code defect (every error is E0462/E0463 on `webview2_com`/`windows`/`cookie`, none in project source; reproduces on the owner's own clean build), and they re-verify as `target/` warms through normal app builds.** See the amendments below for exactly what is proven.

**Amendment (2026-07-16, at implementation of §2 — capture):** New `database/repositories/correction_event.rs`: `CorrectionEventsRepository`, insert + read only, so append-only is a property of the *surface* rather than a comment. Every human verdict now writes one event **inside the same transaction as the mutation it describes** — `SummariesRepository::write_sections_as_draft` gained an `Option<&NewCorrectionEvent>` and opens a transaction it never previously needed (the sections UPDATE alone did not require one); `ActionItemsRepository::set_status`/`edit` do the same inline. `set_block_status` and `set_status` gained an `Option<&str> reason` parameter (§3), threaded to the repository but **still `None` at every command call site** — the UI half of §3 is not built. Action items get their `model`/`template_id` from the meeting's `summaries` row via LEFT JOIN, since they carry no generation context of their own but are produced by the same pass.

*Verification performed.* `cargo test -p mityu`: **328 lib tests + every integration binary green, 0 failed**. New `tests/correction_capture_a2.rs`: **8/8**, the load-bearing one being `regeneration_destroys_the_inline_delta_but_not_the_log`, which asserts BOTH halves of this ADR's motivating claim in a single test — that `upsert_draft` really does wipe `original_content` (the defect is observed, not theorised) and that the log survives it intact. Also proven: `re_edit_records_the_model_text_not_the_previous_human_revision` (the model→human anchor — reading `content` on a re-edit would silently understate the burden); `deleting_the_meeting_erases_its_corrections` (the §10 CASCADE, now through the app's own sqlx pool rather than a hand-rolled connection); `the_retranscription_downgrade_is_not_a_human_correction` (system writes leave no event); `a_foreign_workspace_context_records_nothing`. `cargo clippy -p mityu --all-targets`: **zero** warnings attributable to any changed file. Worth recording: the pre-existing `migration_c1_summaries` suite **caught the new `applied_rules` column on its own** — it pins `summaries`' exact column set/order — and was updated to expect it (4/4 green). That test did its job; the pin is why the schema change could not land silently.

**Amendment (2026-07-16, at implementation of §3 — the reject reason):** Both reject commands take `reason: Option<String>`; `normalize_reason` (commands.rs) trims it and collapses blank to `None` — the ONE place that decides blank means "unexplained", deliberately not duplicated frontend-side where the two could drift. `summaryDraftService.rejectBlock/rejectActionItem` forward it verbatim. UI: clicking Reject opens an inline, autofocused reason field on that row (mirroring the existing inline-edit pattern); **Enter submits, blank included, so a reject costs one extra keystroke and explaining is free.** Escape backs out without rejecting. *Design tension, recorded because it was a real choice:* the reason must never gate the reject (§3), which argues for rejecting instantly and asking afterwards — but "afterwards" would mean UPDATE-ing a recorded event, and the append-only surface (§2) has no such method. Asking first, in one call, keeps the invariant; the cost is that reject is now two actions instead of one. Enter-on-empty is what makes that cost ~nil.

*Verification performed.* Rust: **330 lib tests green** (+2 pinning `normalize_reason`, incl. the blank/whitespace collapse), all integration binaries green, clippy clean on every changed file. Frontend: `npx tsc --noEmit` and `eslint` clean; **Vitest 96/96** including a new `summaryDraftService.test.ts` that reads the arguments a mocked invoke received — the reason's wire shape is pinned there rather than in the browser, because seeing across the IPC boundary requires stubbing `__TAURI_INTERNALS__`, which makes `isTauri()` true and wakes every Tauri-gated provider in the layout (the first attempt did exactly that and the page died white — the stub answered every command with `true`, so a provider expecting an array hit `true.map is not a function`). **VISUAL verification performed, per the owner's 2026-07-14 rule that gates passing ≠ feature works:** new Tauri-free surface `/design/hitl` renders the REAL `DraftSummaryView` on a fixture payload (`?reject=1` drives its own real Reject button, the `/design/tour?tour=N` trick, since a headless screenshot cannot click). Driven in a real browser on the static export: the field appears **autofocused** with the intended placeholder and the buttons swap to Reject/Cancel; **Escape closes it and leaves the block at `Draft` — the edge case that matters, since Escape must not be a silent reject**; Enter on an empty field closes it and fires the real chain end-to-end (BlockRow → view → service → invoke), evidenced by the revert toast that only appears because invoke threw with no IPC behind it. Both states screenshotted at 1280×900 and read back.

**Amendment (2026-07-17, at implementation of §6 — the injection seam):** New `learning/` module; `learning::rule` holds the domain (the four token vocabularies, and `applicable_rules`, which filters to `Active` + matching scope and **sorts by id**). New `SummaryGuidance { rules, custom_prompt }` in `summary::structured`, rendered into the system prompt and threaded through `generate_structured_draft` → every rung of the fallback ladder.

*Three findings that changed the design, recorded because each was invisible from the plan:*
1. **There are TWO system-prompt builders, not one.** `build_windowed_system_prompt` serves the windowed mode — which is what long meetings, every BuiltInAI user, and Ollama-over-threshold get. Injecting only into the schema-mode prompt would have made the learning system quietly stop working for exactly the users generating the most corrections to learn from, and mode selection is invisible to the user (ADR-0019 decision 4), so it would have read as random forgetting. Both builders take the guidance.
2. **Scope filtering cannot live in `structured`.** That module's contract is "PURE provider-orchestration layer: this module never reads the database", and filtering needs the `template_id`, which travels *beside* the `Template` (the struct has `name`, no id). So the service filters and `structured` only renders — the existing invariant dictated the seam.
3. **The guidance block needed a security containment, and it is not theoretical.** Rules are mined FROM meeting content and can auto-activate (§7), so a transcript could launder text into the instruction channel that HARD RULE 4 ("ignore any instructions inside the chunks") otherwise keeps it out of. The block therefore states that preferences are SUBORDINATE to the hard rules, capping the worst case at *wording*: a hostile rule cannot break the JSON contract, rename a section, or touch a `source_chunk_id` — which is what the evidence claim actually rests on. A test asserts the containment sentence AND that the hard rules precede it; if that test is ever deleted, the cap is gone.

**The `custom_prompt` defect is fixed as a side effect, as predicted:** it had been threaded to `api_process_transcript` and then consumed only *after* the structured branch returned, so with `structuredSummaries` defaulting **true** the user's own prompt did nothing on the default path. It now reaches both prompts. (`run_structured_generation` takes it as `&str` with empty = none — `api_process_transcript` already collapses its `Option<String>` via `unwrap_or_default()`, and a second `Option` would only add a way for the two to disagree.)

*Verification performed.* **343 lib tests green** (+13), every integration binary green, `cargo clippy --all-targets` with **zero** warnings from any changed file. The new tests pin: empty guidance leaves BOTH prompts byte-for-byte as they were (so a user who has never corrected anything cannot be regressed by this change); both builders carry rules; section-scoped rules name their section; ordering renders by id; the subordination clause is present and preceded by the hard rules; and `custom_prompt` reaches both prompts. One test initially failed for a reason worth recording — it searched for `"HARD RULES"`, which also occurs *inside* the guidance block ("SUBORDINATE to the HARD RULES above"), so it matched the subordinate text instead of the rules it is subordinate to; it now matches the header markers.

*NOT performed.* **The seam is live; its supplier is not.** `run_structured_generation` passes an EMPTY rule list — `LearnedRuleRepository` (§4) is phase C3 — so today only `custom_prompt` actually reaches a prompt. No `applied_rules` snapshot yet (§5, phase B2), which means auto-activation (§7) must NOT be enabled before B2 lands: the snapshot is its precondition, not a nicety. No end-to-end proof that a stored rule changes a real generation — that needs C3 plus a provider call, and is owed. No UI. No in-app smoke test.

**Amendment (2026-07-17, at implementation of §5 — the snapshot):** `AppliedRule { rule_id, rule_text, scope }` in `learning::rule`; `upsert_draft` takes `applied_rules: &[AppliedRule]` **as a parameter rather than a follow-up `UPDATE`**, so a draft cannot exist without the record of what shaped it. The service snapshots `guidance.rules` — the very list rendered into the prompt, deliberately not a re-read of the table: a second read could return a different answer (the user may edit a rule while the provider call is in flight) and the snapshot would then describe a generation that never happened. `SummaryDraftRow.applied_rules` is `Option<Vec<AppliedRule>>`.

*Two design corrections this phase forced, both worth the words:*
1. **`[]` and `NULL` are different claims, and A1 had them muddled.** The A1 migration comment said NULL meant "before this migration, **or** with no active rules", while the A1 test comment said NULL "must stay distinguishable from an empty snapshot" — those contradict. Settled: **`'[]'` = the learning system ran for this summary and applied nothing ("no, and we checked"); `NULL` = nothing was ever recorded (a pre-migration row, the first-run sample, which inserts its own row directly, or a summary from the legacy markdown path — "we do not know")**. An audit must be able to tell *no* from *don't know*, so the read path keeps the `Option` all the way out, and the column keeps **no DEFAULT**: a `DEFAULT '[]'` would silently promote every pre-existing row from "unknown" to "audited, nothing applied" — a claim the migration is in no position to make. The A1 migration comment was corrected in place; this is legitimate precisely because that file is **untracked and has never been applied to any real database** (§6 forbids editing *applied* migrations).
2. **`scope` is snapshotted as the raw token, not `RuleScope`.** An archival record must stay readable if the scope vocabulary later grows; parsing an old record against today's enum turns an archive into a liability.

*Verification performed.* **343 lib tests + every integration binary green**, `cargo fmt --check` clean, clippy with **zero** warnings from any changed file. New `tests/applied_rules_snapshot_b2.rs` (**4/4**) pins: the snapshot is a copy, not a reference (the rules are dropped before the read, so a lazy reference could not survive); `[]` reads back as `Some(vec![])` and a NULLed column as `None`; regeneration **replaces** the snapshot rather than accumulating (a stale snapshot is worse than none — it would confidently explain a summary that no longer exists); and the snapshot preserves the prompt's id order while excluding a foreign-template rule. ~18 existing `upsert_draft` call sites were mechanically updated to pass `&[]`.

*NOT performed.* **Editing or deleting a STORED rule is not exercised** — `learned_rules` still has no repository (§4, C3), so the copy-not-reference test proves the text is frozen into the row, not that it survives a real rule mutation; that case is owed at C3. **The `custom_prompt` is NOT snapshotted** — the column is `applied_rules` and the reproducibility argument that justifies it is about *learned, automatic* behaviour the user did not explicitly ask for, whereas a custom prompt is their own input for that one run. It remains a smaller, separate traceability gap; recorded, not fixed. **The legacy degrade path applies no rules and writes no snapshot** — consistent rather than silent (no snapshot = no rules, honestly), but it does mean a user's rules quietly do not shape a summary that fell back to the legacy markdown path.

**Amendment (2026-07-17, at implementation of §4 + §7 — the rule lifecycle and the policy):** `LearnedRulesRepository` (create / list_active / list_all / evidence_ids / set_status / edit_text / soft_delete), `learning::config::LearningConfig` in `settings.learningConfig` via the `redactionConfig` pattern, `rule_transition_allowed` + `initial_status` in `learning::rule`. **`run_structured_generation` now reads real rules — the B1 seam has its supplier and the loop is closed** from correction → rule → prompt → snapshot.

*Decisions worth their words:*
1. **`create` COMPUTES the birth status; it does not accept one.** Otherwise any future caller could mint an `Active` rule by asking, and auto-activation would stop being something the *user's policy* controls.
2. **The status machine has no arc back to `Proposed`.** Once the human has answered, the answer stands until they change it; re-proposing a settled question is the nagging the machine exists to prevent.
3. **Dismiss ≠ delete, deliberately.** `Dismissed` keeps the row — the refusal is ON THE RECORD, which is what lets the miner tell "never proposed" from "proposed and refused". `soft_delete` removes it from the list and carries no memory of refusal, so **a deleted rule can be re-learned** if the behaviour repeats. That is the system working, not a bug — but §9's copy must say so in those words, or users will read Delete as "never again".
4. **The corrupt-config fallback is `disabled()`, not `default()`.** `get_learning_config` distinguishes ABSENT (no preference was ever expressed → the product default, auto-activation on) from CORRUPT (the user *had* preferences we cannot read → everything off). Falling back to the default on a corrupt blob would silently re-enable auto-activation for someone who deliberately switched it off. This is the same principle as the redaction repository's "never silently enable redaction from a bad blob"; the fallback differs only because redaction's safe state happens to equal its default and learning's does not. Every config field carries a serde default, so an older/partial blob fills gaps from the product default instead of tripping the corrupt path — which keeps true corruption rare enough for `disabled()` to be as blunt as it is.
5. **`auto_activate` defaults ON, `llm_miner_enabled` defaults OFF.** The first because §7's three bounds hold (output HITL gate, §5 snapshot, §9 visibility) and the owner's directive is that customers must SEE the difference. The second not for privacy — the transcript already goes to that provider — but because the LLM miner **spends the user's own API budget**, and enabling a paid background process on someone's behalf is not a default anyone gets to choose for them.
6. **A failed rule/config read degrades to no guidance; it never fails the summary.** The user asked for a summary, not for a learning system.

*Verification performed.* **348 lib tests + every integration binary green**, clippy clean on every changed file. New `tests/learned_rules_c3.rs` (**9/9**) pins all of the above, plus full cross-workspace isolation (a foreign context can neither see nor mutate a rule), and — settling the B2 debt — **the snapshot surviving the real stored rule being rewritten AND deleted**, which B2 could only approximate because `learned_rules` had no repository then.

**The tests caught a real bug before it shipped, and the way it hid is worth recording:** the INSERT had 14 placeholders and 13 binds — `created_at` was missing, so every value after it was shifted one left and `created_at` silently received `activated_at`'s value. For a rule born **Active** that value is `Some(now)`, so the row inserted fine and the happy path passed; only a **Proposed** rule (`activated_at = None`) hit `NOT NULL constraint failed: learned_rules.created_at`. A suite that only exercised auto-activated rules would have shipped it.

*NOT performed.* **Nothing creates a rule automatically yet** — the miners (§8) are unbuilt, so in practice `list_active` returns rules only if something else put them there; the loop is closed but unfed. **No commands, no UI** (§9): rules cannot be seen, approved, edited or deleted from the app, which means auto-activation is currently the only path a mined rule could take and the user has no way to review it — **§9 must land before this ships to a user with §8 enabled.** No end-to-end proof against a live provider that an active rule changes real output; the injection is proven at the prompt-string level (B1) and the wiring at the repository level (here), not in one run. No in-app smoke test.

**Amendment (2026-07-17, at implementation of §9 — the rules screen):** 9 Tauri commands in `learning/commands.rs`, `learningService.ts`, and `LearningSettings.tsx` mounted in `PreferenceSettings` beside `RedactionSettings`. **This closes the bound C3 left open:** §7 permits a mined rule to activate without asking only because three things hold, and one of them — the user can see, read, edit and delete every rule — was a promise until this landed.

*Decisions:*
1. **The wire is flat tokens** (`scope: "template:daily_standup"`), not serde's enum encoding. The frontend gets the same closed vocabulary the database stores, pinned by tests on both sides.
2. **Dangling evidence is a COUNTED field, not an error.** `RuleEvidenceResponse { events, missing_count }`. A rule outliving the meetings it was mined from is the *designed* end state of §10, so the screen explains it — "4 corrections behind this rule have been erased with their meeting. The rule itself carries no meeting text, so it stays until you remove it." — rather than showing a short list that reads as a bug.
3. **The Dismiss/Delete copy says the thing outright**, per C3's warning that users read Delete as "never again": the Refused section states "Mityu won't suggest these again. Deleting one instead lets it come back if you keep making the same correction."
4. **`LearningSettings` takes an optional `service` prop** (`LearningDataSource`), defaulting to the real singleton. Not a test hack: `invoke` throws outside the Tauri shell, so without a seam this screen could only ever be photographed in its error state — and a screen nobody verified is exactly what §7's safety argument cannot rest on. It also makes the component unit-testable.

*Verification performed.* **350 lib tests + every integration binary green**, clippy clean on every changed file; `tsc`, `eslint`, **Vitest 96/96** clean. **VISUAL verification performed** (the owner's 2026-07-14 rule) on the static export via a new Tauri-free surface `/design/learning`, whose fixtures cover the states that carry risk rather than only the happy one. Driven in a real browser: the three policy switches render with **exactly `LearningConfig::default()`** (learn ON, auto-activate ON, LLM miner OFF — the money-spending one off, visibly); all five evidence states render correctly, including **singular/plural agreement** ("1 correction … has … its" vs "4 corrections … have … their"), the model→human delta, a reject with its reason, fully-erased evidence, and a user-authored rule's "no corrections behind it"; and the lifecycle moves the counts as it should — "Use it" took *noticed 1 → 0* and *in use 3 → 4*, "Turn off" took *in use 4 → 3* and *Refused 1 → 2*. Screenshotted at 1100×1500 and read back.

*NOT performed.* **§8 is the only thing left, and until it lands nothing is mined** — the screen will show "Nothing yet" for a real user, and `auto_activate` has nothing to act on. **No in-app smoke test**: the commands are exercised at the repository layer and the component against a fixture service; the two have never been run against each other through a live `AppHandle`. **No Vitest for `LearningSettings`** — the injectable seam now makes it possible; the browser drive covers the same ground for now. The `/design/learning` fixtures are hand-written and could drift from the real command shapes; only the Rust view tests pin the tokens.

**Amendment (2026-07-17, at implementation of §8's deterministic half — the miners):** `learning::miner` reads the correction log and proposes rules; `learning::commands::mine_and_persist` persists them; `api_mine_learned_rules` exposes it; **`api_approve_summary` runs it on success**. Approving is the natural trigger — the moment a human has finished reviewing is exactly when new corrections exist — and it keeps learning off the hot path of every keystroke. A mining failure never fails the approval: the user asked for a summary, not for a learning system.

*Turkish normalization moved to the core (`app_lib::text`), and `eval-harness/src/metrics.rs` now imports it.* This is E1's planned refactor, done early because the miner needed the same folding and the alternative was a second Turkish lowercase — i.e. a second answer to "are these the same word?", drifting from the first the moment either was touched. The dependency already pointed this way (`eval-harness` → the core, never the reverse). Costs one new core dependency (`unicode-normalization`). **The miner uses `strict` and never `folded`**, and that is load-bearing: a user correcting "konveyor" to "konveyör" is teaching precisely the distinction folding erases, so a folded miner would see no change and learn nothing. A test in `text.rs` pins that property in both directions.

*Four decisions inside the miner, each guarding against a specific way of learning nonsense:*
1. **One subject, one vote.** Events collapse to the LAST verdict per `(meeting_id, subject_id)` before anything is counted. Editing a block three times is one correction refined, not three; rejecting a block and restoring it is not a rejection at all. Without the collapse, one indecisive afternoon manufactures a rule out of a single opinion.
2. **A term substitution is one small replacement, not a rewrite.** Only a diff with exactly one contiguous replacement block, ≤3 words a side, over which ≥50% of the text survives. A wholesale rewrite has a perfectly real word-diff and no meaning — "they replaced these nine words with those eleven" is not a preference, and mining it would produce confident nonsense.
3. **A section the user EVER keeps is never proposed for removal.** Three rejects plus one approval is not a section they want gone; it is one the model gets wrong half the time — a different problem, and not one a rule fixes.
4. **A signature, not the text, is the identity.** `learned_rules.signature` (e.g. `term_substitution|global|aksiyon=>takip`) exists because rule text is USER-EDITABLE: a miner matching on text would re-propose a rule the instant its owner reworded it. Added to the (still untracked, never-applied) `20260716000000` migration rather than a second one, and deliberately NOT UNIQUE — the signature must be free to recur across the soft-deleted rows it leaves behind.

**The dismiss/delete asymmetry is now enforced end-to-end and tested:** `live_signatures` spans every status INCLUDING dismissed (the human answered; asking again is nagging) but EXCLUDES soft-deleted rows (delete carries no record of refusal, so the behaviour can be re-learned). That is precisely the distinction §9's copy spells out.

**Scope decision — the length-preference miner is deliberately NOT built.** It was in the plan; on inspection it is the weakest of the three: it fires on fuzzy evidence and phrases a vague rule ("keep bullets shorter"), and a bad rule that auto-activates is exactly what §7's bounds are stretched to permit. Term substitution and section rejection are legible enough that a user can look at the proposal and immediately tell whether it is right. Deferred, not forgotten.

*Verification performed.* **Whole workspace green:** 366 lib tests (+16), every integration binary, `cargo fmt --check` clean, clippy with **zero** warnings from any changed file. **eval-harness's own 15 metric tests still pass through the re-export**, which is what proves the normalization move did not break the harness. New `tests/rule_mining_c1.rs` (**6/6**) proves the loop on a database rather than on synthetic events: three real corrections, made through the real HITL commands, become a persisted `Active` rule that `applicable_rules` hands to a generation; a second pass proposes nothing; a dismissed rule is never mined again while a deleted one is; the master switch beats the log; with auto-activation off the same rule waits as a `Proposed`; and erasing the meetings leaves the rule standing with its evidence dangling — the §10 asymmetry, end to end. `miner.rs`'s own unit tests (12) pin each of the four decisions, including that output is deterministic across repeated runs rather than dependent on `HashMap` iteration order.

*NOT performed.* **The LLM miner (§8, phase C2) is unbuilt** — `llmMinerEnabled` is a switch with nothing behind it, so today only term substitutions and section rejections are found. **The correction-burden headline (D2) is unbuilt**, so the user sees rules appear but not the number that would tell them it is working. **No in-app smoke test:** mining is proven at the repository layer and against a real SQLite file, never through a live `AppHandle`. The 2,000-event mining window is a bound, not a tuned value; it logs when it bites. And the thresholds (`MAX_SUBSTITUTION_WORDS = 3`, `MIN_UNCHANGED_RATIO = 0.5`, support 3) are **reasoned but not calibrated** — no corpus of real corrections exists yet to tune them against, which is the same chicken-and-egg the Phase-0 gate has, and the reason the rules screen matters more than the numbers do.

**Amendment (2026-07-17, at implementation of E1 + D2 — the metric and the visible difference):** `text::levenshtein` moved from `eval-harness` (the rest of E1's refactor, same one-definition rationale as the normalization move; the harness's own metric tests still pass through the re-export). New `text::normalized_word_distance` divides by the LONGER side, not the reference — that bounds it at 1.0, and the bound is the point: it feeds an average a user reads as a percentage, and WER's ability to exceed 1.0 would let one verbose edit drag the average past 100%. New `learning::burden` computes the objective, `api_get_learning_stats` exposes it, and `LearningSettings` renders a headline.

**The load-bearing decision here is a WORDING decision, and it is the whole reason this phase was worth doing carefully.** The burden number is a *correlation, not a result*: it measures **what the user did**, not what the model did, and a user reviewing less carefully moves it in exactly the direction a working rule does. There is no control group and no way to separate the two from this side of the screen. So the metric is honest about its own limits in three places — the Rust module docs, the TS interface docs, and a comment on the UI component — and **the headline states facts and never causation**: "You've reviewed 62 points and taken 61% of them exactly as written. Your last 20: 70%. The 20 before those: 40%." The rules sit directly beneath it and the reader draws their own line. The one thing the copy must never say is "thanks to your rules", and the comment says so.

*Smaller decisions:* burden shares the miner's collapse (edit-then-approve is one reviewed item scored on the approval; a restore removes the item entirely — it is back in draft, unreviewed). A reject counts 1.0 (nothing survived). `accepted_rate()` is `Option` — a rate over zero items is *unknown*, not 0%, and a screen that explains itself must not tell a new user "0% accepted". The recent/earlier comparison appears only at `2 * WINDOW` (40) reviewed items — better no number than one built from three data points. `WINDOW = 20` and the 2,000-event stats window are judgements, not measurements, flagged as such.

*Verification performed — and a build-environment failure I must record honestly, because it means the Rust verification here is WEAKER than the phases before it.* What ran clean, before the mistake below: `cargo test -p mityu --lib learning::burden` → **8/8** (this compiled `text.rs` with the new `levenshtein`/`normalized_word_distance` and all of `burden.rs`, and pinned every rule above — empty log has *no* rate not a zero one, comparison hidden until two full windows); `cargo check -p mityu --all-targets` → clean after the stats command landed. Frontend, fully independent of the Rust toolchain: `tsc`, `eslint`, **Vitest 96/96** clean. **VISUAL verification performed** on the static export, BOTH states that carry risk: the populated headline renders facts-not-causation as intended, AND — the one every new customer hits first — `/design/learning?empty=1` shows "Nothing to measure yet" with **no "0%" and no "NaN%"**, the specific failure this careful-wording exercise existed to prevent. Screenshotted at 1100×900 and 1100×760 and read back.

**RESOLUTION (2026-07-18, owner's VS build): the whole lib test suite is green in the real crate — `test result: ok. 384 passed; 0 failed; 2 ignored`.** Run as `cargo test -p mityu --lib` from an x64 Native Tools Command Prompt after a full `cargo clean`. 384 = the 366 from C1 + 8 `burden` + 9 `llm_miner` + 1 `rule` birth-status test, i.e. every new module's unit tests plus all pre-existing ones, compiled by the real toolchain into the actual `app_lib` — this SUPERSEDES the isolated-harness result below. What is NOT yet re-run: the INTEGRATION test binaries (`rule_mining_c1`, `correction_capture_a2`, `applied_rules_snapshot_b2`, `learned_rules_c3`, the DB/tenant/migration suites). They hit `crate X required to be available in rlib format` — and crucially **this reproduced on the owner's clean VS build too, so it is NOT an artifact of my Git-Bash/cmd environment**: it is `app_lib`'s `crate-type = ["staticlib","cdylib","rlib"]` interacting with separate integration-test binaries after a `cargo clean`. It worked earlier this session only because `target/` still held rlibs from a prior full app build; it recurs from clean. The integration tests were green pre-clean and re-verify naturally the next time the app is built (`cargo build -p mityu` / `pnpm tauri:build` populates the rlibs) — they are owed re-runs of code that already passed, not unproven code. Correction to the framing below: the LIB compiles fine (384 tests prove it); only the integration-test *binaries* are blocked, which also means the C2 glue (which lives in the lib) CAN be compiled by the owner's build.

**A self-inflicted toolchain break, and how the core logic was verified anyway.** Chasing a stale-`target/`-metadata corruption from interleaved clippy/test runs (`found staticlib … instead of rlib`), I ran `cargo clean`. That destroyed the cached build of the **vendored OpenSSL** (`libsqlite3-sys`'s `bundled-sqlcipher-vendored-openssl` feature), and rebuilding it from this Git-Bash/cmd shell fought two Windows traps — Git's MSYS perl lacks a module OpenSSL's `Configure` needs, and Git's GNU `link.exe` shadows MSVC's, breaking the C build. **Both are now solved and documented** (memory `windows-cargo-build-recipe`): build from a clean PATH so MSVC `link.exe` wins, with Strawberry Perl first. The `mityu` lib then compiles. `cargo test` still then hit a **non-deterministic rlib-format corruption** (a different native crate each run, surviving even a full clean) that I could not clear from this shell — so the FULL integration suite (`rule_mining_c1`'s end-to-end loop, the repository/DB tests, HITL command wiring) did NOT get a clean post-D2 run and is owed on the owner's normal VS-prompt build. **But the entire pure-logic core WAS verified, definitively, just now** — via an isolated `#[path]` harness (ADR-0022 precedent) that pulls the REAL `text` / `learning::{config,rule,miner,burden}` source files into a tiny crate depending only on `unicode-normalization`+`serde`+`thiserror` (no sqlite/openssl/audio), built at `C:\dev\lh` rather than under `%TEMP%` where Defender blocks the linker's `.exe` writes: **37/37 green** — `text` 4 (incl. the strict-vs-folded property the miner rests on), `config` 5, `rule` 8, `miner` 12 (all four decisions), `burden` 8. So the part that "actually learns" and the number the owner wanted shown are proven; only their DB/command plumbing rides on the pre-clean green + the owed VS-prompt run. `eval-harness`'s `use app_lib::text::levenshtein` swap compiled clean inside the earlier successful `--lib`/whole-graph builds before the rlib issue appeared. This is the one place in ADR-0030 where full-integration re-run is owed for reasons of my own making, flagged rather than papered over.

*NOT performed.* **The headline is never wrong, but it can mislead a tired user** — that is inherent to measuring the reviewer's behaviour, mitigated by wording and not by arithmetic, and it cannot be fully fixed without a model-quality signal the app does not have. **The stats are pull, not push**: the screen shows the numbers as of load; nothing refreshes them live after an approval mines a new rule in the background. **No in-app smoke test** — the metric is proven against synthetic events and the screen against a fixture service, never through a live `AppHandle`. **`api_get_learning_stats` recomputes on every call** over up to 2,000 events; fine for a local settings screen opened occasionally, not for anything hot.

**Amendment (2026-07-17, at implementation of §8's LLM half — C2, the pure logic):** `learning::llm_miner` — `build_llm_miner_prompt` (corrections → prompt) and `parse_llm_rules` (model JSON → validated `RuleCandidate`s). This is the RISKY part of C2 and it is now built and verified: safely parsing UNTRUSTED model output.

*Decisions:*
1. **LLM rules never auto-activate.** `initial_status` now special-cases `MinedLlm` → always `Proposed`, whatever the claimed support and whatever the config. The model's "support" is an assertion, not a count of corrections it watched happen; letting an asserted number clear the threshold would let a model talk its own suggestion into force with no human in the loop — the precise bound §7 leans on. The LLM suggests; a person agrees.
2. **No fabricated provenance.** LLM candidates carry `support_count = 0` and `evidence = []`. The model cannot reliably map a pattern back to the correction-event ids that show it, so a count or citation would be invented. Zero is honest and the rules screen shows nothing where a real count would go.
3. **Term-substitution candidates get the SAME signature the deterministic miner produces** (`term_substitution|<scope>|<from>=>=<to>`, normalized identically), so an LLM rename the deterministic miner already found — or the user already dismissed — dedups against the record instead of nagging. Everything else gets an `llm|`-prefixed signature keyed on normalized text.
4. **A garbled reply is "learned nothing", never an error** — unparseable JSON (incl. prose/fence-wrapped) yields `[]`; malformed individual rules (bad scope, missing substitution side, no-op rename, empty text) are dropped individually.

*Verification performed.* The pure logic was proven the same way D2's was: an isolated `#[path]` harness at `C:\dev\lh` (out of `%TEMP%`, where Defender blocks the linker), pulling the real `llm_miner` + `rule` + `miner` + `text` sources into a crate with no sqlite/openssl. **47/47 green** — the earlier 37 plus **9 `llm_miner` tests** (dedup-with-deterministic, signature-on-record dropping, freeform `llm|` prefix, section scope, bad-scope drop, missing-side drop, in-batch dedup, prose/fence tolerance, garbage→empty, prompt shape) and **1 new `rule` test** pinning the birth-status safety (LLM never auto-activates even at support 999 with auto-activation on).

**Trigger decided (owner, 2026-07-17): on-approve, in the background.** The owner's reasoning corrects my "off because it costs money" framing: the DEFAULT provider is Ollama (local, free), so on-approve mining costs nothing for local-LLM users; the token bill only appears with a PAID provider, and enabling the toggle is the user accepting that. So the design is:
- **Runs on approve, when `llmMinerEnabled` is on** — alongside the deterministic pass in `api_approve_summary`.
- **BACKGROUND, never blocking.** This is the one hard constraint the trigger imposes: the deterministic miner is arithmetic (instant), but the LLM miner is a full model inference, and running it synchronously in the approve path would make every approval wait seconds (even on local Ollama). So it is fire-and-forget — approve returns immediately, and any new `Proposed` rules appear on the next rules-screen load.
- **Cost is provider-aware and stated, not hidden.** The toggle copy now reads "Free with a local model (Ollama); with a paid provider it spends tokens on every approval." Default stays OFF — honest, since a user can switch to a paid provider later, and off-by-default means the token spend is always an explicit opt-in.

*NOT performed — the glue, deferred to a WORKING COMPILER on purpose.* The provider CALL + its background wiring are NOT written yet, and this is a discipline decision, not neglect: assembling the provider (api key from keychain, provider/model/endpoints from config, redaction) is ~100 lines of full-crate-coupled code that **cannot be compiled locally until the owner's VS-prompt build restores `target/`** (see the D2 amendment). Writing that much network-calling glue BLIND — no `cargo check` — and calling it "flawless" would be a contradiction; one wrong signature fails to compile and the claim is empty. The flawless path is to write it against a working compiler, which the owner's build provides, and ideally factor the provider-assembly `service.rs` already does into a shared helper rather than duplicate it. So `llmMinerEnabled`'s **parser is built and harness-verified (47/47)**; its **trigger is designed and its cost is honestly surfaced**; only the ~100-line provider-assembly glue waits for the compiler. That is the ONLY remaining piece of ADR-0030.

**Amendment (2026-07-18, C2 glue landed — ADR-0030 implementation complete).** The ~100-line provider-call glue is written, against the working compiler as the note above insisted. `learning::commands::mine_llm_and_persist<R>(app, pool, ctx)`: gate on `enabled && llmMinerEnabled`; claim a per-workspace in-flight slot; pull the recent corrections (`list_recent`, capped at `MAX_CORRECTIONS_IN_PROMPT`); reduce them to model→human deltas (`summaries_from_events` — Edit/Reject only, Approve/Restore dropped as signal-free); redact BEFORE the provider sees them; build the prompt; resolve the provider; call it; `parse_llm_rules`; persist each candidate as `origin = MinedLlm`. Every safety property is upstream and untouched: `initial_status` still forces `MinedLlm → Proposed` (an LLM rule NEVER auto-activates), `support_count = 0` / `evidence = []` are carried from the parser (no fabricated provenance), and candidates dedup against `live_signatures`.

*Four decisions in the glue, each a specific hazard closed:*
1. **The provider-assembly is now ONE definition, not two.** `process_transcript_background`'s inline ~80 lines (provider enum, keychain key, Ollama/CustomOpenAI endpoints, sampling knobs) were extracted to `SummaryService::assemble_provider` + an owned `AssembledProvider`; both the summary path and the miner call it, so "which model, which key, which endpoint" can no longer drift between them. `ProviderParams::call` went `pub(crate)` so `AssembledProvider::call` reuses the exact same funnel to the LLM client rather than a second one. The refactor is behaviour-preserving — the summary path's error strings and control flow are identical, proven by the unchanged test suite.
2. **Background, and the approval NEVER waits or fails.** The trigger in `api_approve_summary` (now `<R: Runtime>`, taking the `AppHandle`) keeps the deterministic pass inline (instant) and fires the LLM pass through `tauri::async_runtime::spawn` — approve returns immediately, and any error inside the task is content-free-logged and swallowed. A model call outliving two quick approvals is why the in-flight guard exists: without it a burst of approvals would fan out into concurrent identical passes (the signature dedup would collapse the duplicate rules, but not paying for the second call is better).
3. **Redaction is fail-safe here too.** The miner sends correction TEXT, so the opt-in C6 policy applies: an unreadable redaction config ABORTS the pass rather than risk unredacted text reaching a (possibly cloud) model — the same fail-safe the transcript path uses. A `< 3` correction floor means a near-empty log never spends tokens to be told "no pattern".
4. **No `lib.rs` change was needed.** Making the command generic over `R: Runtime` does not change how Tauri registers it — `generate_handler!` binds by name, exactly as it already does for the pre-existing generic `api_process_transcript` / `api_cancel_summary`. Confirmed by a clean lib build.

*Verification performed (owner's VS Native Tools env, no `cargo clean` — the vendored-OpenSSL cache was preserved per the `windows-cargo-build-recipe` memory).* `cargo build -p mityu --lib` → green (only pre-existing warnings, none in any changed file). `cargo test -p mityu --lib` → **387 passed, 0 failed, 2 ignored** = the prior 384 + **3 new `llm_glue_tests`** pinning the pure `summaries_from_events` map (Approve/Restore carry no signal; an event with no `original_text` is skipped; an edit with no final text maps to `""` not a drop). `cargo clippy -p mityu --lib` → 0 errors and **zero warnings attributable to any changed file** (`service.rs`, `structured.rs`, `learning/commands.rs`, `summary/commands.rs`). `rustfmt` clean on the changed files.

*NOT performed — the in-app smoke, owed to a live model + a running GUI.* The provider CALL cannot be unit-tested (it needs a real model), and the `#[tauri::command]` path needs a live `AppHandle` no headless test constructs — so the loop is proven at the seam layer (parser 9/9, pure map 3/3, provider assembly compiled and shared), not yet end-to-end through a running app. **The one manual check owed:** in a running build, turn on "Let the model look for patterns" (free with Ollama), approve a summary, and confirm a "Suggested by the model" proposal appears on the rules screen — the whole point, and the only step a compiler cannot stand in for. The integration-test BINARIES remain owed their re-run for the same Tauri GUI-crate rlib-link quirk documented above (not a code defect; re-verifies as `target/` warms through a normal app build).

*Historical (A2, superseded).* The A2 amendment's "nothing reads the log yet" note was true when written; the deterministic miners (C1) and the burden metric (D2) now both read it. No UI (§3 reject-reason, and the §9 rules screen). No tsc — no TypeScript changed. **No in-app smoke test:** as `hitl_commands_c1.rs` documents, the `#[tauri::command]` wrappers need a live `AppHandle` no headless test can construct, so the commands are proven at the repository layer they delegate to, not through a running app. **Known gap, deliberate:** an assignee/due-only action-item patch records nothing — it is a real correction of the model's extraction but produces no model→human *text* delta, and recording it would enter the burden metric (§9) as a zero-cost edit and dilute it. Capturing structured-field corrections needs its own event shape; owed, not faked. Per-user attribution is also owed: `updated_by` is left NULL per the never-synced baseline, so the log records *what* was corrected, not *who* corrected it — irrelevant while local-first and single-user, load-bearing the moment Phase-2 teams arrive.

---

**Amendment (2026-08-08, §10's erasure guarantee was FALSE in production — corrected).** §10 states that deleting a meeting deletes the corrections derived from it, "with no residue". That was **not true**, and the verification quoted in the paragraph above is why it went unnoticed: the cascade was proven `with PRAGMA foreign_keys=ON`, but the app's only deletion path deliberately runs with foreign keys **OFF**. `delete_database_records` (`database/deletion.rs:74-86`) disables them so a malformed foreign-workspace child cannot be cascaded away with the caller's parent, and it hard-fails if the pragma did not take. `delete_meeting_with_connection` then deletes a **hand-maintained** child list — which omitted `correction_events`.

So on every real deletion, `correction_events` survived: the AI draft **and the human's rewrite of it**, i.e. meeting content, retained after a "delete my data" that returned success and after `complete_privacy_maintenance` certified the cycle. Reproduced on the real migration chain: after the production sequence, `meetings = 0`, `transcripts = 0`, `correction_events = 1` with its text readable. `speaker_turns` (ADR-0034) shipped with the same defect the same day; it is unpopulated, so nothing was retained, but it would have been the moment an engine wrote a turn.

The proximate fix is two `DELETE`s. The real fix is that **proving a cascade under `foreign_keys=ON` proves nothing about this app**, and two independent authors made that exact mistake — so the guard is now structural: `every_child_of_meetings_is_deleted_explicitly` reads `PRAGMA foreign_key_list` for every table in `sqlite_master` and fails if any table referencing `meetings(id)` is not deleted explicitly. A new child table now fails the suite the moment its migration lands. The sentinel test additionally seeds both tables, because listing a table in a residual `COUNT(*) = 0` assertion without ever writing a row to it passes trivially — verified by mutation: removing the `correction_events` delete fails both tests, and before the seeding it failed only one.

§10's *argument* is unchanged and still holds — corrections are erasable, rules survive deliberately. What was wrong was the claim that the code did it.

## ADR-0031 — A5 gate thresholds are locked before measurement, and term recall is gated per domain

**Context:** `docs/PHASE0_VALIDATION.md` §4 offers a *suggested starting bar* for the A5 transcription gate and explicitly instructs that "the agreed numbers" be recorded in `DECISIONS.md`. They never were. A5 has therefore been carrying a gate with no committed pass/fail line — an unrecorded threshold is not a gate, because the bar can move after the numbers land. Separately, the product owner decided (2026-07-26) to evaluate **two domains** in one run — 5 legal + 5 intralogistics clips — which changes what the jargon threshold even means: the `jargon` bucket now mixes two vocabularies whose recall figures are not comparable.

Both facts were surfaced while authoring `docs/A5_SPRINT.md`, the execution runbook for A5. The eval set itself remains `0/5` in every bucket; the only blocker is human evidence collection, not code.

**Decision:**

1. **The §4 starting bar is adopted verbatim and locked, before any measurement runs.** GO (full scope, field included) requires **median strict WER ≤ 0.15 (quiet)** *and* **≤ 0.25 (field)** *and* **jargon term recall ≥ 0.80**. CONDITIONAL(meeting-room) = quiet passes, field does not. NO-GO = quiet is unusable after vocabulary tuning. These are the constants already compiled into `eval-harness/src/report.rs` (`GO_QUIET_WER`, `GO_FIELD_WER`, `GO_TERM_RECALL`); this ADR is what makes them the *agreed* numbers rather than a default.
2. **Term recall is gated per domain vocabulary, not per bucket.** GO requires **every** jargon domain to clear 0.80 independently. A single blended bucket median is explicitly rejected as the gate number: with legal at 0.60 and intralogistics at 1.00 the merged median lands on exactly 0.80 and reads PASS while one domain plainly fails. If one domain passes and another does not, the result is **not** GO — CONDITIONAL may be considered for the passing domain and the failing domain remains open evidence debt.
3. **Turkish CER (diacritic-folded) is reported as a secondary metric and does not relax the bar.** Turkish is agglutinative, so word-level WER runs structurally higher than in English; CER makes that visible and informs the reading of a near-miss, but the thresholds above are not loosened on its account. Any future change to these numbers requires a new ADR — never an edit to this one after results are known.
4. **The verdict stays human.** The harness computes metrics and pre-fills the threshold table; it never writes GO/CONDITIONAL/NO-GO. `CLAUDE.md` §0.5 and the §4 protocol both require a human to sign the gate, and an AI agent may not substitute for the pilot reviewer.

**Consequences:** A5 now has a committed pass/fail line that predates its data, which is the only arrangement under which the gate constrains anything. The per-domain rule required harness work, delivered with this ADR (`eval-harness`): clips carry a domain vocabulary set (`eval/jargon.<set>.txt`, selected per clip by an `<id>.vocab.txt` sidecar, unknown names failing closed before a model loads); `Row.vocab_set` records which vocabulary scored each clip; `medians_by_domain` + `jargon_domains` land in `report.json`; and the Markdown threshold table emits one row per (config × jargon domain) with its own PASS/FAIL. A single-domain run renders as before. Cost: two domains mean 25 recordings rather than 20, and each `jl*` clip needs a one-line sidecar. Unchanged: the fail-closed ≥5-pairs-per-bucket gate, the human-corrected-reference requirement, the prohibition on public datasets as gate evidence, and ADR-0027's rule that A5 remains NOT EVALUATED — deferred for v1.0.4 publication only — until a human runs and signs it.

**Status:** Accepted (2026-07-26; product-owner threshold and two-domain decisions). Thresholds locked pre-measurement; A5 itself remains open and unevaluated.

---

## ADR-0032 — EU AI Act Art. 50 verification: the marking holds on every reachable path, and is now test-protected

**Context:** Art. 50 transparency obligations apply from **2 August 2026**. `docs/A5_SPRINT.md` §4 (Aşama 0.2) required a pre-deadline confirmation that the two things Mityu's documentation claims are actually true in the code: that the AI-generated marking cannot be suppressed, and that exported output carries a machine-readable marking. A claim in a comment is not a control; this ADR records the verification and what it changed.

**Verification result (2026-07-26, source-level audit):**

1. **Non-suppressible in-app marking — HOLDS on every reachable path.** With the default `structuredSummaries: true`, `DraftSummaryView` renders `ReviewRequiredBanner` in all four states (loading, error, empty, populated); it has no close control and no state that hides it. With the flag turned off, `SummaryPanel` derives `isLegacyUnverified = !!aiSummary && !structuredEnabled`, which is therefore *always* true when a summary exists, so `BlockNoteSummaryView` shows `LegacyUnverifiedBanner` and renders the content read-only with copy and export disabled. LLM-mined learning rules are labelled `Suggested by the model`.
2. **Machine-readable marking — HOLDS.** `buildExportProvenance` emits a versioned payload (`com.bluedev.mityu.export-provenance/v1`) carrying `ai_generated: true`, `human_reviewed`, `source_linked`, the per-item `source_links`, model and approval metadata. It is embedded in all three export formats: PDF as XMP under `https://bluedev.dev/mityu/export-provenance/v1#`, DOCX in `description` + `keywords`, Markdown as YAML front matter. `buildVerifiedExportProvenance` throws (`ExportApprovalError` / `ExportSourceLinkError`) rather than emitting an unapproved or unlinked document, so the renderer fails closed.
3. **One candidate gap was found and dismissed as unreachable.** `SummaryPanel` contains a block rendering Key Points / Action Items / Decisions / Full Summary with no AI label. It is dead code: `page-content.tsx` declares `const [summaryResponse] = useState<SummaryResponse | null>(null)` with no setter, so the value can never become non-null. Recorded here because the next reader will find the same block and must not "fix" it by wiring it up without adding a marking.

**Decision:** The verification stands as PASS for both claims, and the in-app marking is now protected by an executable test rather than by convention. `frontend/src/components/AISummary/DraftSummaryView.transparency.test.tsx` asserts the banner is present in all four states and contains no control that could dismiss it, querying it through its ARIA role and accessible name (wording, colour and icon are deliberately not pinned — the accessible marking is). The suite was proven to have teeth: removing the banner from a single branch turns exactly that case red.

This required the frontend's first component-test infrastructure. `environment: 'node'` stays the **default** — the new suite opts into jsdom with a per-file `@vitest-environment` docblock — so pure-logic tests keep their speed. Added dev dependencies: `jsdom`, `@testing-library/react`, `@testing-library/dom`. `oxc.jsx.runtime: 'automatic'` was required in `vitest.config.ts` because `tsconfig.json` sets `"jsx": "preserve"` for Next; note that this Vitest runs on Vite 8 / Rolldown, whose transformer is **oxc**, so an `esbuild.jsx` setting is silently ignored.

**Consequences:** A refactor that drops the AI-generated marking now fails CI instead of shipping. Art. 50 is a continuing obligation, so this protection matters after 2 August, not only before it. Component tests are now possible for other product-critical affordances (the approval gate, source links); nothing obliges them to exist, but the cost is no longer "add a test framework first".

**Explicitly NOT decided here — legal applicability.** This is a source-level engineering verification: it establishes that the documented mechanisms exist and cannot be bypassed. Whether Art. 50(2) applies to HITL-approved summarisation at all, and whether the "assistive function / no substantial alteration" carve-out is engaged, is a question for counsel and is not answered by this ADR. Art. 50(1) (informing a person they are interacting with an AI system) is not triggered by today's surfaces, but the planned "Ask This Meeting" conversational surface (Wave 1) would engage it and must be assessed before it ships.

**Amendment (2026-08-10) — the Art. 50(1) assessment this ADR left owed, for "Ask This Meeting".**

This ADR closed with: *"Art. 50(1) ... is not triggered by today's surfaces, but the planned 'Ask This Meeting' conversational surface (Wave 1) would engage it and must be assessed before it ships."* BACKLOG C9 made that a release gate, due at the next tag. `SummaryPanel` mounts `AskPanel` whenever a transcript exists, so 1.1.0 would be that tag. The assessment:

**The surface does engage Art. 50(1).** It is a conversational surface a person types into and receives generated answers from — an AI system intended to interact directly with a natural person. No carve-out is claimed; the "obvious to a reasonably well-informed person" exception is not relied on, because relying on it would be an argument rather than a control.

**What was already there, and what was not.** `AskPanel` already rendered an always-on, non-dismissable note reading *"AI-generated · review required"*, unconditional and above the input. That is an Art. 50(**2**) content marking. Art. 50(**1**) is a different obligation — informing the person that they are *interacting with an AI system* — and the wording only implied it. Implication is not disclosure.

**What changed.** The note now opens *"You are asking an AI assistant · answers are AI-generated and need your review"*, and its accessible name carries both halves. Three properties matter and each is now asserted separately, because a single check over both would stay green if either half were dropped:

1. **Present before interaction.** It renders unconditionally above the input, not after an answer arrives — being told afterwards is not being informed that you are interacting with an AI.
2. **Not dismissable.** No control inside the note, and no dismiss/close affordance in the panel.
3. **Both obligations stated.** The interaction disclosure and the content marking are distinct assertions in `AskPanel.test.tsx`.

**Proven, not asserted.** The tests were confirmed load-bearing by mutation — dropping the interaction sentence, dropping the interaction half of the accessible name, and gating the note on an answer having arrived each turn the suite red. The surface is also now rendered by the `/design/hitl` route, so a compliance disclosure that previously existed only as a jsdom assertion can be looked at.

**Still not decided here.** The two counsel questions this ADR opened are untouched: whether Art. 50(2) reaches HITL-approved summarisation, and whether the assistive-function carve-out is engaged. This amendment discharges the engineering obligation C9 named — the disclosure exists, holds on every reachable path, and cannot regress silently — and nothing more.

**Status:** Accepted (2026-07-26). Verification PASS; Art. 50(1) assessment for the Ask surface completed 2026-08-10 (amendment above); the Art. 50(2) applicability question remains for counsel.

---

## ADR-0033 — Tier-0: one honest device-capability probe, and a seam OS-native backends slot into

**Context:** The strategy's Tier-0 item (`STRATEGY_2026-2030.md`, Dalga 0) is a model/STT abstraction that makes OS-native small-model runtimes — Apple Foundation Models, Windows ONNX / Copilot Runtime — interchangeable with the built-in GGUF sidecar and BYOK cloud, selected per task from device capability. That wave is the largest tailwind behind the local-first thesis: it turns on-device inference from a quality tax into zero marginal cost.

Two findings shaped the scope:

1. **The LLM path has no hardware awareness at all.** `audio::HardwareProfile` (GPU type, performance tier) exists but lives inside `audio/` and is consumed only by the whisper engine. `summary/` — provider enum, `ProviderParams::call`, `assemble_provider` — never consults it. There is no shared answer to "what can this device run".
2. **Part of the existing detection does not detect.** `hardware_detector.rs::detect_memory_gb()` returns a hard-coded `8` unless a `MEMORY_GB` env var is set, and `PerformanceTier` is derived from it — so the tier is fictional on most machines.

The OS-native backends themselves cannot honestly be built in this environment: Apple Foundation Models needs macOS 26+ on Apple Silicon and Swift FFI (no macOS machine here — the §4 macOS smoke is still owed), and a Windows ONNX LLM path needs its own model-management and quality validation. Writing either would be unverifiable speculation.

**Decision:** Land the capability probe and the selection seam; declare the OS-native backends without implementing them. New dormant module `frontend/src-tauri/src/inference/`, pure and offline, called by nothing.

- **`capability`** — `DeviceCapabilities` (OS, arch, cores, RAM, GPU, NPU) with RAM measured through `sysinfo` (already a dependency) rather than assumed, plus a coarse `LocalInferenceFitness` for backend preference and product copy.
- **`backend`** — `LocalBackend` { `GgufSidecar`, `AppleFoundationModels`, `WindowsOnnxRuntime` }, a `BackendStatus` per host, a preference order (OS-native first on its own host), and `select_local_backend`.

Two honesty rules are enforced structurally rather than documented:

- **"Could not tell" is not "none".** `NpuVendor::Undetermined` is distinct from `None`, and `is_known_present()` is false for both, so an undetected NPU can never be counted as a capability. macOS is settled by construction (every Apple Silicon part has a Neural Engine); Windows and Linux report `Undetermined` because PnP enumeration is not implemented here and could not be validated against a machine that actually has an NPU. Reporting `None` there would let the product tell a Copilot+ PC owner they have no accelerator.
- **A declared backend is unselectable.** `select_local_backend` is *derived* from `BackendStatus` rather than hand-written, so an unimplemented arm cannot be returned by construction. `os_native_backends_are_never_selectable_yet` and `a_selected_backend_always_has_a_provider` pin it.

NPU classification is a pure function over a device name, split from the platform enumeration that feeds it, so it is fully testable without the hardware. This matters because the risk is false positives, not misses: a first pass that substring-matched `"npu"` flagged **"Microsoft Input Configuration Device"** on the (NPU-less) development machine — `I·npu·t` contains it. Matching is token-based, and that exact device name is now a regression test.

**Consequences:** Both STT and the summary path now have one place to ask what the device can do, and the arrival of an OS-native backend becomes an addition — implement it, flip its status to `Available` — rather than a refactor of every call site, matching the dormant-seam approach of `sync/` (ADR-0012) and `agents/` (ADR-0013). Nothing changes behaviour: no new inference path, no call sites, no UI. The local-first invariant holds by construction since the module performs no I/O beyond reading host properties.

**Owed, deliberately not faked:** the Windows/Linux NPU enumeration shim (needs a machine with an NPU to validate); the Apple Foundation Models and Windows ONNX implementations; and wiring the probe into provider assembly and the UI. `hardware_detector.rs::detect_memory_gb` is left as-is rather than changed underneath the whisper tier — the new probe measures RAM correctly for its own consumers, and correcting the whisper path is an audio-side change that must be made with §4's care and its own smoke test.

**Amendment (2026-08-07, out of the #13 review).** Three corrections, each one the module's own stated principle being violated by its first implementation:

1. **Cores were hardware threads.** `available_parallelism()` counts logical execution units, so a 2-core/4-thread part cleared the four-core floor and an accelerated 4c/8t part could reach `Comfortable`. `physical_cores: Option<usize>` (from `sysinfo`) is now separate from `logical_threads`, and `effective_cores()` halves threads when the physical count is unknown — understating a non-SMT part rather than overstating an SMT one, because overstating is what wrongly clears the floor.
2. **Vendor branding was read as an accelerator.** The `intel(r) ai` substring matched `"Intel(R) AI Camera"`, feeding a phantom NPU into the fitness verdict — the same false-positive class as the `I·npu·t` case, in the code written to prevent it. Only the `ai boost` device name matches now.
3. **Apple Silicon was treated as sufficient for Foundation Models.** The API arrives in macOS 26, and `DeviceCapabilities` carried no version, so the documented "just mark it Available" step would have selected it on macOS 15. `os_version_major` is now probed and the backend reports `UnsupportedHost` below 26 — including when the version cannot be parsed, so it fails closed.

**Status:** Accepted (2026-08-07). Seam dormant; OS-native backends declared, unimplemented, and structurally unselectable.

---

## ADR-0034 — Speaker diarization: post-hoc, not in the live pipeline; models pinned like Parakeet; quality unverifiable until A5 has multi-speaker audio

**Context:** `docs/DESIGN_READAI.md` Phase E requires "its own ADR before any model ships (model licensing, §9)", and the owner's 2026-07-09 scope decision promoted diarization from a maybe to a priority enabler. It is also the most visible parity gap: every competitor labels speakers; Mityu has no speaker code at all. The owner chose the real-model path (2026-08-08) over the cheaper channel-attribution alternative, because channel attribution ("you" vs "the room") does nothing for in-person capture — which is precisely Mityu's differentiator.

Three findings from reading the tree shaped this decision.

1. **Channel provenance is destroyed before transcription.** `audio/ffmpeg_mixer.rs:319` mixes microphone and system audio into one stream (`(m * mic_ducking) + (s * system_gain)`) and `pipeline.rs:901` feeds the mixed window onward. Both sources still exist separately at that call, so a cheap two-way attribution was available — but it is worthless for a room recorded on one microphone.
2. **A transcript row is not a speaker turn, so a row cannot carry a speaker.** `TranscriptSegment` carries id, text, timestamp and audio timings and no speaker field; a dead `transcripts.speaker` **column** does exist in SQLite (added 2025-11-10 from upstream, never read or written). Re-purposing it would not work anyway: a row is one VAD speech segment, and VAD is tuned to *bridge* pauses rather than cut at them — the import and retranscription paths pass `VAD_REDEMPTION_TIME_MS = 2000` (`audio/import.rs:57`, `audio/retranscription.rs:52`), a longer gap than the one between two people taking turns, and `audio/import.rs` treats segments over 25 s as normal. One row therefore routinely spans several speakers, and talk-time — a sum of durations — cannot be derived from row labels at all.
3. **Saved audio is optional, so some meetings can never be diarized.** `RecordingSaver::start_accumulation(false)` discards every mixed chunk (`audio/recording_saver.rs:236-249`) and `stop_and_save` then returns without producing a file (`:434-440`). "Transcripts only" is a supported recording mode.
4. **`CLAUDE.md` §10 forbids refactoring the audio pipeline and the DB schema in one change**, and diarization needs both.

**Decision:**

1. **Diarization runs post-hoc on the saved recording, not inside the live capture pipeline.** Speaker clustering is inherently global — deciding that the voice at 02:00 is the same person as at 47:00 requires both — so a streaming implementation would be worse *and* would put new code in the most fragile subsystem in the app (`CLAUDE.md` §4). The saved audio already persists until the user deletes the meeting, so the live path is not touched at all.
2. **Model stack: `pyannote-segmentation-3.0` for segmentation plus a speaker-embedding model, obtained from the sherpa-onnx GitHub release assets**, not from Hugging Face. This matters: `huggingface.co/pyannote/segmentation-3.0` is **MIT-licensed but gated** — verified from the model card, which states "You need to agree to share your contact information to access this model". An app cannot satisfy a click-through gate on the user's behalf, so an in-app download from that origin is not viable. sherpa-onnx publishes converted ONNX artifacts at its `speaker-segmentation-models` and `speaker-recongition-models` release tags, which are stable, unauthenticated, and pinnable — exactly the shape `parakeet_engine` already consumes (revision-pinned, expected size and SHA-256 checked through `utils::verify_file_integrity`).
3. **Speaker identity is stored as timed turns, never as a label on a transcript row.** A new `speaker_turns` table holds `(meeting_id, workspace_id, speaker_label, start_ms, end_ms, confidence)`; the UI renders a row's speakers by overlapping it with turns, and talk-time sums turn durations. Overlapping turns are permitted, because people talk over each other and a diarizer should be able to say so. Turns are **local-derived and not synced** — ADR-0012 pins the synced set, and a peer can regenerate turns from audio; syncing them later requires amending ADR-0012. The dead `transcripts.speaker` column stays dead and is explicitly **not** the diarization surface.
4. **Diarization is offered only when the audio still exists, and availability is probed rather than stored.** A transcripts-only meeting (finding 3) has no input, and ADR-0027's retention goal means audio can also disappear *later* — so a stored availability flag would go stale on deletion. The check reuses `find_audio_file` (`audio/retranscription.rs:149`), which already solves the identical problem for retranscription. Four UI states follow: no audio → "not available for this recording"; audio with `diarized_at` NULL → offer to run; `diarized_at` set with no turns → inconclusive; set with turns → show them. `meetings.diarized_at` exists precisely so an empty turn list is not read as "no speakers found".
5. **Ship the schema before the engine, per §10.** Order: (a) this ADR; (b) the `speaker_turns` table plus `meetings.diarized_at`, populated by nothing; (c) the diarization engine and model download; (d) UI for speaker labels and talk-time. Each lands separately.
6. **Speaker labels stay anonymous and human-named.** The system emits `Speaker 1..N`; attaching a real identity is a manual human action. Mityu does no voice-based identification, and no emotion, charisma, engagement or performance inference is derived from them (`DESIGN_READAI` guardrail 3, `CLAUDE.md` §10). This is also what keeps the processing outside the "unique identification" purpose that would make it biometric special-category data under KVKK m.6 / GDPR Art. 9 — see `docs/legal/A5_KVKK_PACK.md` §4.1, where the same distinction is already drawn for the eval recordings.
7. **Talk-time is descriptive only** — seconds and share of the meeting, source-linked like every other derived figure. It is not a score, ranking, or judgement of any participant.

**Licensing status (incomplete on purpose):**

| Artifact | Reported licence | Verified from primary source? |
|---|---|---|
| `pyannote/segmentation-3.0` | MIT (repository gated) | **Yes** — model card read 2026-08-08 |
| 3D-Speaker CAM++ embedding | Apache-2.0 | **Yes** — ModelScope API + repo LICENSE, 2026-08-09 (see amendment) |
| NeMo TitaNet embedding | CC-BY-4.0 | **Yes** — HuggingFace API `cardData.license`, 2026-08-09 (see amendment) |
| `reverb-diarization-v1` (alternative segmentation) | unknown | **No** — not investigated; Rev.ai terms need checking before it is considered |

CAM++ is the preferred embedding model on licence grounds (Apache-2.0 carries no attribution-in-product obligation); NeMo TitaNet is the fallback and would add no new machinery, since Parakeet already ships under CC-BY-4.0 with attribution in `README.md` and the About screen (ADR-0020). **Neither may ship until its licence is verified from the model card the way ADR-0020 verified whisper and Parakeet, and until it is confirmed that redistribution through sherpa-onnx's release assets preserves the required notices.** Whether Hugging Face's access gate binds a downstream consumer who obtains the same MIT-licensed weights elsewhere is a question for counsel, not for this ADR.

> **The implementation-approach paragraph below is superseded by ADR-0035 (2026-08-09):** the `sherpa-rs` crate it names is archived; the live crate is k2-fsa's own `sherpa-onnx`, and the choice between it and `ort` turned out to be forced rather than open, because sherpa-onnx and the app's `ort` **cannot link into one binary** (measured). **Nothing else in this ADR is superseded** — in particular item 2's model provenance and its licence-verification obligation still stand, and ADR-0035 lists them as prerequisites of shipping.

**Implementation approach (recommended, not yet decided):** the whole diarization pipeline is segmentation → embedding → clustering. `ort` is already a dependency and could run both models, but the segmentation post-processing and speaker clustering would then be ours to write — that is a research project, not an integration. The `sherpa-rs` bindings to sherpa-onnx provide the assembled pipeline instead, at the cost of a substantial native dependency that must build on Windows, macOS and Linux. That trade needs its own decision once the schema lands.

**Consequences:** The live capture path is unchanged, so the §4 risk of this feature is confined to a post-processing step that can fail without affecting a recording. The schema gains a new table and one nullable column, both empty, so nothing breaks for existing meetings. Diarization becomes an opt-in post-processing pass rather than a cost every recording pays. The cost of the turn model is that rendering is an overlap query rather than a column read, and that a turn boundary can fall inside a transcript row — the UI must show a row attributed to more than one speaker instead of pretending rows and turns align.

**The verification gap, stated plainly:** *diarization quality cannot be measured today.* The A5 `multi` bucket — three or more speakers with natural overlap — contains **zero recordings** (`docs/A5_SPRINT.md`). There is no multi-speaker audio in this project at all, so any claim about how well speakers are separated would be unfounded. This binds the feature to the A5 sprint: **no speaker-accuracy claim may be made in product copy, and the feature ships labelled best-effort, until the `multi` bucket is collected and a human has reviewed the result** — the diarization sanity field that `PHASE0_VALIDATION.md` §6 already reserves for exactly this.

**Status:** Accepted (2026-08-08) as the design gate. No model ships under this ADR alone: item 2's licence verification and the implementation-approach decision are both still open.

**Amendment (2026-08-09, item 2's licence verification is DISCHARGED).** The table above recorded two embedding models as "verified: **No** — search result only", and item 2's closing sentence made both, plus the notice-preservation question, blockers on shipping. All three are now verified from primary sources:

| Artifact | Licence | Verified from | Gated? |
|---|---|---|---|
| pyannote segmentation-3.0, as sherpa republishes it | **MIT**, `Copyright (c) 2022 CNRS` | the `LICENSE` file **inside** `sherpa-onnx-pyannote-segmentation-3-0.tar.bz2` (downloaded and unpacked), whose `README.md` states it is "converted from huggingface.co/pyannote/segmentation-3.0" | sherpa asset: **no** |
| 3D-Speaker CAM++ (`3dspeaker_speech_campplus_sv_zh-cn_16k-common.onnx`, 28,281,138 B) | **Apache-2.0** | ModelScope's own API for `iic/speech_campplus_sv_zh-cn_16k-common` returns `License = Apache License 2.0`, publisher `iic`, public — and the 3D-Speaker repository's `LICENSE` is Apache-2.0 with no separate carve-out for weights | **no** |
| NeMo TitaNet large (`nemo_en_titanet_large.onnx`, 101,405,493 B) | **CC-BY-4.0** | HuggingFace API for `nvidia/speakerverification_en_titanet_large`: `cardData.license = cc-by-4.0`, `gated: false` | **no** |

**Redistribution preserves notices — the open question is answered by inspection, not by argument.** sherpa's segmentation package ships the upstream MIT licence *with its original copyright line* and a provenance README. That is what MIT requires, and it is present in the artifact we would actually ship.

Two obligations follow and belong in `resources/MODEL-NOTICES.txt` at ship time, per the ADR-0020 precedent: the MIT text and CNRS copyright for segmentation, and the Apache-2.0 attribution for CAM++ — which, unlike the segmentation tarball, is published as a **bare `.onnx` with no accompanying licence file**, so the notice is ours to supply. CAM++ is preferred over TitaNet on both licence grounds (Apache-2.0 carries no attribution-in-product obligation) and size (28 MB vs 101 MB).

**Not verified, and stated rather than assumed:** none of the CAM++ variants is trained on Turkish (they are `zh-cn`, `zh_en`, or English VoxCeleb). Speaker embedding models are largely language-independent because they model voice rather than words, but this project has no measurement of that, and ADR-0034's ban on speaker-accuracy claims covers it.

**Amendment (2026-08-08, the instrument landed and is validated).** The verification gap above said diarization quality cannot be measured. Half of that is now closed: `eval-harness/src/diarization.rs` scores DER over RTTM with optimal speaker mapping, and `der` / `der-suite` expose it. It is **validated against NIST `md-eval.pl` on 216 real VoxConverse dev recordings** — 1296 comparisons at each of collar 0.00 / 0.25 / 0.50, agreeing on every one at the printed resolution (max 0.00 pp; both scorers print two decimals, so 0.01 pp is the finest observable difference — the claim is agreement, not bit-identical arithmetic) (`tools/diarization/crosscheck-mdeval.py`, reproducible; `docs/DIARIZATION_EVAL.md`).

That cross-check earned its place immediately: it found two convention errors that every unit test had passed, because the same author wrote the code and the tests. The evaluation region was wrong (we counted false alarm outside the reference's extent; md-eval does not), and the speaker mapping was decided on the post-collar scored region rather than the pre-collar evaluation region — which optimised the mapping against the score and made our DER systematically **lower** than the literature's. An instrument that flatters us is worse than no instrument.

**The gap that remains is unchanged in substance:** VoxConverse is English broadcast and YouTube audio, the A5 `multi` bucket still holds zero recordings, and no engine exists. So this ADR's ban stands — **no speaker-accuracy claim in product copy** until target-condition evidence exists. What changed is that when an engine does land, its quality is now a number rather than an opinion.

**Correction (2026-08-08, after merge — the erasure claim was wrong).** PR #17's commit body said "deleting a meeting cascades its turns away". It does not: the app's deletion path runs with `PRAGMA foreign_keys = OFF` by design, so `speaker_turns`' declared `ON DELETE CASCADE` never fires, and the table was missing from the explicit delete list. Verified in sqlite3 with foreign keys ON — the one state production negates. `speaker_turns` is unpopulated so nothing was retained, but `correction_events` (ADR-0030) had the identical defect and *was* live. Both are now deleted explicitly and guarded structurally; see the ADR-0030 amendment of the same date.

**Review correction (2026-08-08, before merge):** the first draft of this ADR stated that no speaker field existed and proposed storing one anonymous label per transcript row. Review found both wrong: a dead `transcripts.speaker` column does exist, and — more importantly — VAD's 2000 ms redemption means a transcript row routinely spans multiple speakers, so row labels would mis-attribute speech and could not produce talk-time. The draft also assumed saved audio always exists, which "transcripts only" recordings disprove. Findings 2–3 and decisions 3–5 above are the corrected text; nothing had been merged or applied when they changed.

## ADR-0035 — Diarization engine: the sherpa-onnx pipeline, run out-of-process to keep a second ONNX Runtime out of the capture process

**Context:** ADR-0034 fixed the *shape* of diarization (post-hoc, timed `speaker_turns`, anonymous labels) and deliberately left the implementation approach open: `sherpa-rs` bindings versus writing segmentation post-processing and clustering ourselves on `ort`. That question is now answered, and the measuring instrument it depends on exists — `eval-harness`' DER scorer, validated against NIST `md-eval.pl` on 216 VoxConverse recordings, agreeing at the printed 0.01 pp resolution on all 1296 comparisons per collar (`docs/DIARIZATION_EVAL.md`).

Three things that were *assumed* when ADR-0034 was written turned out to be wrong, and each changes the decision:

1. **`sherpa-rs` is dead.** The crate named in ADR-0034 is archived read-only (last push 2026-03-08, 32 open issues); its README points at k2-fsa's own `sherpa-onnx` crate. The live crate is `sherpa-onnx` 1.13.4, **Apache-2.0**, published 2026-07-08, from the upstream repository.
2. **No Rust crate can be added to the app that does the job on `ort`.** `speakrs` (Apache-2.0) genuinely implements the full pipeline, but requires `ort ^2.0.0-rc.12` while the app's VAD dependency pins it exactly: `silero_rs` (rev 26a6460) declares `ort = "=2.0.0-rc.10"`, and `ort-sys` declares `links = "onnxruntime"`, so only one may exist in the graph. `pyannote-rs` is the only rc.10-compatible candidate and is not a diarizer — no powerset decode, no clustering; it benchmarks at 80% DER.
3. **ADR-0034's licence route was based on a gate that does not bind us.** `pyannote/segmentation-3.0` is MIT but access-gated on Hugging Face; sherpa-onnx republishes converted ONNX artifacts on unauthenticated GitHub release tags, which is the origin ADR-0034 already blessed.

**Decision:**

1. **Integrate the assembled sherpa-onnx pipeline via k2-fsa's official `sherpa-onnx` crate (Apache-2.0), not `sherpa-rs`, and not a hand-written pipeline on `ort`.** Segmentation → embedding → clustering is a research project to write and an integration to adopt; we have no way to justify the former while the thing it would buy — tunability — is unmeasurable against target-condition audio we do not yet have.

2. **It runs OUT OF PROCESS, in a `diarize-helper` sidecar binary.** Measured on Windows MSVC (2026-08-09):

   | Configuration | Result |
   |---|---|
   | `sherpa-onnx` alone, default (`static`) | **Links, runs, exit 0.** Zero linker warnings on a forced relink. |
   | `sherpa-onnx` (`static`) **+ `ort = "=2.0.0-rc.10"` in one binary** | **`LNK1169: multiple defined symbols`** — fatal. |
   | `sherpa-onnx` (`shared`) **+ `ort` in one binary** | **Links and runs.** Both FFI boundaries crossed. |

   The `static` combination fails with exactly the predicted class — `LNK2038: 'RuntimeLibrary' mismatch: MT_StaticRelease vs MD_DynamicRelease` against `libort_sys(onnxruntime_c_api.obj)`, plus `LNK2005` duplicate C++ runtime symbols and `LNK4098 defaultlib 'LIBCMT' conflicts` — because sherpa's Windows archive is `…-win-x64-static-MT-Release-lib.tar.bz2`, a static-CRT build, while `ort` brings a `/MD` one.

   **The third row is why this decision is not "forced".** An earlier draft of this ADR said two ONNX Runtimes "cannot share one binary"; that is false, and only the `static` row had been measured when it was written. With sherpa's `shared` feature the combined binary links, runs, and initialises both sides. So the sidecar is a **choice**, and it has to be justified on its merits rather than on a linker error:

   - **In-process would put a second ONNX Runtime instance in the app's own process.** Measured: `ort` links its runtime *statically* (`cargo:rustc-link-lib=static=onnxruntime`, ONNX Runtime 1.22.0 — `ort::info()` reports `rel-1.22.0` at run time) while sherpa loads *its own* `onnxruntime.dll` from the shared archive. Two independent runtimes, two sets of global state, in the process that also runs capture and transcription — `CLAUDE.md` §4's most fragile subsystem. Nothing was run through actual inference in either, so that combination is **unproven, not merely inelegant**.
   - **Packaging.** In-process `shared` puts **39 MB of loose DLLs** beside the app (`onnxruntime.dll` 17.4 MB, `DirectML.dll` 18.5 MB, `sherpa-onnx-c-api.dll`, …). The sidecar is **one 17.7 MB self-contained file**.
   - **It would couple Parakeet's runtime to sherpa's**, which is what the `silero_rs` → `ort = "=2.0.0-rc.10"` pin exists to prevent.

   Separate processes have separate address spaces, so the sidecar removes all three at once: **zero lines change in `parakeet_engine`, the `silero_rs` `ort` pin is untouched, no `ndarray` 0.16→0.17 migration.** The repo already ships two sidecars (`binaries/llama-helper`, `binaries/ffmpeg`, `externalBin` in `tauri.conf.json`, manager in `summary/summary_engine/sidecar.rs`), so this is an existing pattern, not a new one. Diarization is a *better* fit than `llama-helper`: ADR-0034 makes it post-hoc and whole-file, so it is one invocation per meeting with no keep-alive.

3. **Default (`static`) features, giving a single self-contained executable.** Measured: 17.7 MB release, and it runs from an otherwise **empty directory** — no `onnxruntime.dll`, no sibling libraries to ship or to get out of step. One file into `binaries/`, exactly like the two already there. The `shared` feature is available and not needed.

4. **The native archive MUST be verified before `sherpa-onnx-sys` is ever built — and NOT by a build script of ours.** `sherpa-onnx-sys`' `build.rs` downloads a prebuilt archive from a GitHub release and performs **no integrity check of any kind** — verified by reading it. That is squarely against ADR-0020's exact-size-plus-digest precedent for model files.

   The obvious mitigation does not work, and the trap is worth stating so nobody implements it: a `build/ffmpeg.rs`-style check inside `diarize-helper`'s own build script runs **too late**. Cargo builds a dependency's script before the dependent crate's, and a build script cannot set environment for a sibling that has already run — so the unverified bytes would be downloaded and extracted before our digest check executed.

   So the verification must sit **outside cargo**, in one of two places:
   - a **bootstrap step run before `cargo build`** (developer command and CI step) that fetches the archive, checks **exact byte length and SHA-256** against a pinned release tag and URL — the full ADR-0020 precedent, not a digest alone — fails closed on mismatch, and hands it over through the documented `SHERPA_ONNX_ARCHIVE_DIR` / `SHERPA_ONNX_LIB_DIR`; or
   - a **vendored/patched `sherpa-onnx-sys`** whose build script does the check itself.

   The bootstrap is preferred: it leaves the upstream crate untouched and therefore upgradable. **Built 2026-08-09** as `tools/diarization/fetch-sherpa-archive.py`, wired into `ci.yml` behind a `Cargo.lock` guard so it activates by itself when the helper lands.

   **This is a prerequisite for making `diarize-helper` a workspace member, not merely for shipping.** `ci.yml:36-38` runs `cargo clippy --all-targets` and `cargo test --all` on `ubuntu-latest` for every push and pull request, so from the moment the helper joins the workspace, **every PR performs the unverified download this decision forbids** — on a runner, unattended. Adding the crate before the bootstrap exists is therefore the wrong order, not a shortcut.

5. **Speaker count is never assumed.** `FastClusteringConfig::default()` is `num_clusters: -1, threshold: 0.5` — read from the crate source and confirmed at run time. `-1` means "unknown, cluster by threshold". A fixed default would have imposed a speaker count on every meeting, and a two-person conversation is Mityu's typical recording; if a future version changes that default, this is the line to re-check.

6. **The result SHAPE matches the schema; the units and the label do not, and the conversion is part of this contract.** `OfflineSpeakerDiarizationSegment { start: f32, end: f32, speaker: i32 }` is one timed turn per row, which is exactly what ADR-0034 decided independently — so no re-modelling is needed. But it is **not** a copy:

   | sherpa | `speaker_turns` | conversion |
   |---|---|---|
   | `start`/`end`: `f32` **seconds** | `start_ms`/`end_ms`: `INTEGER` **milliseconds** | `(secs * 1000).round() as i64`. Storing the float directly would place every turn ~1000× too early. |
   | `speaker`: `i32` index, 0-based | `speaker_label`: `TEXT`, e.g. `Speaker 1` | `format!("Speaker {}", speaker + 1)` — anonymous, 1-based for humans (ADR-0034 decision 6). Storing `0` would put a bare integer where the report renders a name. |
   | (not reported) | `confidence`: `REAL` nullable | `NULL`. The engine reports no per-turn confidence, and NULL means "not reported", which is not the same as "low". |

   A turn whose rounded `end_ms <= start_ms` is dropped rather than stored.

**How the evidence was obtained, and one thing it corrects:** two throwaway crates outside the workspace (`C:\t\spike-diarize`, `C:\t\spike-both`), so a failed spike could not touch CI. The first version of the standalone spike only printed a string; it linked and produced a **128 KB** binary, because with nothing referencing sherpa the linker's `/OPT:REF` had discarded the entire library — a false pass that would have proven nothing. Calling `OfflineSpeakerDiarization::create()` took it to 21.4 MB and produced errors from sherpa's own C++ source (`offline-speaker-segmentation-model-config.cc:Validate:38`), which is what proves the FFI boundary was crossed at run time rather than merely satisfied at link time. `create()` returned `None` on bad configuration instead of aborting — a native library that crashed there would be unusable in a sidecar.

The earlier analysis called the `/MT` clash "the kill risk" for the standalone build. It is not: standalone links cleanly. It is fatal only for the **in-process `static`** build — and, as decision 2 records, not for the in-process `shared` build, which links and runs. So the linker settles one configuration, not the architecture; decision 2 rests on the runtime-coupling and packaging grounds stated there.

**Consequences:** **+** The live capture path is untouched (`CLAUDE.md` §4 risk nil); the app's `ort`/`ndarray`/`silero_rs` graph is untouched; reversibility is deleting one workspace member and one `externalBin` entry; the engine can be swapped later without touching the app. **−** ~18 MB on the installer; a second, independently-versioned ONNX Runtime to keep an eye on; and one more binary to sign.

**What this ADR does NOT authorize:**

- **Shipping.** Decision 4 (the archive integrity bootstrap) is a prerequisite and is not built. ADR-0034's item 2 — primary-source licence verification — **is now discharged**; see its 2026-08-09 amendment.
- **Any speaker-accuracy claim.** ADR-0034's ban stands: the A5 `multi` bucket holds zero recordings, and the DER scorer is validated on English broadcast audio, not a Turkish meeting room.
- **macOS.** Only Windows was measured here. `ci.yml` does build and test on `ubuntu-latest` for every push and PR, so a **Linux** break would surface immediately — but **macOS is built by neither `ci.yml` nor `release.yml`** (`build-macos.yml` is dispatch-only), so a macOS break would surface at release or later. A macOS check for the helper is owed as part of the implementation; a Windows one is owed because `ci.yml` does not build Windows either.
- **Any statement about runtime or memory on a 60–90 minute recording.** Unmeasured; clustering is superlinear in segment count, and this determines whether the sidecar needs chunked progress reporting.

**Status:** Accepted (2026-08-09). Closes the implementation-approach question ADR-0034 left open, and supersedes that paragraph only. ADR-0034's item 2 (model provenance) is untouched; its licence-verification obligation was **discharged on 2026-08-09** — see that ADR's amendment of the same date — so the one remaining shipping prerequisite here is decision 4's archive integrity bootstrap.

---

## ADR-0036 — Talk time is a share of *speech*, not of the meeting; speakers are listed in the order they spoke, never ranked

**Context:** ADR-0034 step (d) — the UI for speaker labels and talk time — leaves two numeric decisions open that look like formatting and are actually claims about people.

**Decisions:**

1. **The denominator is the sum of attributed speech, not the meeting's duration.** People talk over each other, so speaking time exceeds elapsed time: 30 s of recording with a 10 s overlap contains 40 s of speech, and shares over duration would read 100% and 33%, summing to 133%. Dividing by total speech makes the shares sum to 1 and makes them true. The screen states which denominator it is and why, because "40%" with an unstated denominator is the part a reader fills in wrongly.

2. **Each speaker's own time is the UNION of their turns**, not the sum. Segmentation can emit turns for one speaker that overlap themselves; summing would credit that speaker twice for one stretch of audio.

3. **Speakers are ordered by first appearance. Never by duration.** Sorting by talk time turns a description into a leaderboard, and a leaderboard of who talked most is the employee-performance reading `CLAUDE.md` §10 and ADR-0034 refuse. The panel says outright that speaking time is not a measure of contribution. Colour is assigned by that same order and identifies a speaker across the page; the palette is deliberately flat, with no red/green and nothing that reads as good or bad.

4. **A transcript row is attributed to a LIST of speakers, never to one.** Rows and turns come from separate passes over the same audio and do not align — the `speaker_turns` migration says so in its own note. Choosing "the dominant speaker" would print one confident name over a stretch where two people spoke. Overlaps below **250 ms, or 15% of the row for short rows**, are discarded as boundary artefacts: without a floor, touching edges put two names on nearly every row, which reads as constant crosstalk and hides the genuine overlaps.

5. **A row with no timings shows nothing at all**, not "Unknown". Recordings predating recording-relative timestamps have no extent to match against, and an empty space says "we cannot tell" where a chip would assert something.

6. **The four states stay four.** No audio / never ran / ran-and-separated-nothing / has turns each get different words, and only the states where an action is possible show a button. Collapsing "ran and found nothing" into "never ran" — the natural mistake, since both have an empty turn list — would either keep offering a pass that already happened or tell someone their single-voice recording was never analysed. This is why the repository stamps `diarized_at` even for an empty result.

**Verification:** the arithmetic is pure and unit-tested (`frontend/src/lib/speakerTurns.test.ts`), and the tests were confirmed load-bearing by mutation: sorting by duration, attributing a row to only its dominant speaker, summing instead of unioning, and dropping the overlap floor each fail the suite. The rendered claims are asserted in `SpeakerTurns.test.tsx` so a redesign cannot quietly drop the caveats. The screen itself was **looked at**, not just typechecked, via `tools/ui/shoot.py` against `/design/speakers`, which renders all four states from fixture data.

**One thing the screenshot found that no test would have:** `text-display`, `text-body`, `text-small` and `text-caption` are used across the report and `/design/*` screens as if they were a type scale, and are **defined nowhere** — they emit no CSS. Measured in a browser, an `<h1 class="text-display">`, a `.text-small` and a `.text-caption` all compute to `16px / weight 400`, identical to `body`; the apparent hierarchy comes entirely from font weight and colour. The two files added here were converted to real utilities; the other five were left for their own change, made in ADR-0037.

**What this does NOT authorize:** any claim about *accuracy*. ADR-0034's ban stands — the A5 `multi` bucket holds zero recordings, so how often these labels are right is unmeasured on this project's own audio. It also does not add naming: there is no field, and no button, to attach a person to a voice.

**Amendment (2026-08-09, same day, after review):** four corrections, all of them claims the screen was making that the code or the rules did not support.

1. **A row with two speakers is not crosstalk.** The chip said `overlapping` on every multi-speaker row, but most such rows merely span a handover — turns `[0, 5s]` and `[5s, 12s]` are sequential and do not overlap each other at all, so an ordinary change of speaker was being described as people talking over one another. `hasCrosstalk()` now asks whether two *different* speakers' turns actually intersect inside the row (100 ms floor, below which it is rounding), and only genuine simultaneous speech is marked, as "at the same time". Two chips already say "two speakers"; a word there added nothing but a false claim.

2. **The result is labelled best-effort on screen.** ADR-0034 states that "the feature ships labelled best-effort" until the A5 `multi` bucket is collected and reviewed. That bucket still holds zero recordings. Decision 7 of this ADR said accuracy was not claimed — but saying so in an ADR does not discharge a rule about **product copy**. The panel now says the separation is a best-effort estimate whose accuracy has not been measured on the reader's own recordings.

3. **A failure to ask was invisible.** The hook correctly reported a failed availability query as an `error` with no state — and the panel then gated the entire block on that state, so a database or command failure made the whole feature vanish silently. A reader could not tell "this meeting has no speakers" from "we could not find out", and only the second has a retry. The error now renders independently, with a Try again.

4. **The download button understated the download.** It said 34 MB, which is the on-disk total; the segmentation model arrives as a `.tar.bz2` and expands, so 35.2 MB actually crosses the network. Corrected to 35 MB and pinned by `models::tests::the_download_size_the_ui_promises`, so bumping a model pin fails the build instead of leaving the button quietly wrong.

Each correction carries a test, and each test was confirmed load-bearing by restoring the defect and watching the suite fail.

**Status:** Accepted (2026-08-09). Implements ADR-0034 step (d).


## ADR-0037 — The named type scale was never real; the app's one typography vocabulary is Tailwind's own

**Context.** ADR-0036 recorded that `text-display`, `text-body`, `text-small` and `text-caption` were used as a type scale and defined nowhere — not in `globals.css`, not in the Tailwind config, absent from the built CSS. Two files were converted then; five were left. Reading those five turned up two more of the same kind, `text-h1` and `text-h2` (10 occurrences), which the original count missed. Two of them are the `<h1>` page titles of `/design/hitl` and `/design/learning`: because Tailwind's preflight resets `h1` to inherit size and weight, those titles rendered at `16px / 400` — the page heading was the same size as its own subtitle.

**Decision.** Delete the vocabulary rather than implement it. The six names were replaced with Tailwind utilities:

| was | now | computes to |
|---|---|---|
| `text-display` | `text-2xl font-semibold` | 24 / 600 |
| `text-h1` | `text-xl font-semibold` | 20 / 600 |
| `text-h2` | `text-lg font-medium` | 18 / 500 |
| `text-body` | `text-[15px]` | 15 / 400 |
| `text-small` | `text-sm` | 14 / 400 |
| `text-caption` | `text-xs` | 12 / 400 |

**Why not define the scale instead.** It was the other option and it loses on three counts. (1) **There is no design system to make real.** `docs/DESIGN_READAI.md` — the doc the vocabulary was assumed to come from — contains no typography section, no tier names and no sizes. Nothing in the repo defines these names. Implementing them would have been inventing a design system and backdating it, not honouring one. (2) **It would create a second vocabulary, not a first.** The app already has one: Tailwind's scale, ~640 usages across 99 of 149 `.tsx` files. The six phantom names survived in five, all but one of them `/design/*` preview routes. Adding `text-body` alongside `text-base` and `text-small` alongside `text-sm` gives two ways to say one thing, and the smaller one is the newcomer. (3) **The mechanism assumed doesn't exist here.** This project is on Tailwind **3.4.19** with a `tailwind.config.js`; `@theme` and `@utility` are v4 features. It was still doable via `theme.extend.fontSize`, but the premise was wrong and worth stating.

The first-order argument is simpler: two files had already been converted this way, and consistency with what shipped beat consistency with what was imagined.

**The one thing that changed beyond a rename.** `/design`'s typography specimen advertised sizes it did not render — "Display — 32 / 700" on a line computing to 16/400. Since the mapping puts display at 24/600, applying it verbatim would have replaced one false label with another. Each row now prints the utilities it uses (`Display — text-2xl font-semibold — 24 / 600`), so the label cannot drift from the line again.

**Verification — measured, not typechecked.** `tsc`, `next lint` and 176 vitest tests all passed *before* this change too; they cannot see a class that emits no CSS. So each route was built and measured in headless Chrome via `getComputedStyle`, the check that found the bug, with a control: an element carrying the *old* class name injected into each rendered page. On every route all six controls still compute to `16px / 400` while every converted element resolves to its intended size — one measurement showing both that the names were dead and that the replacements are live. `/design`, `/design/report`, `/design/hitl`, `/design/learning` and `/settings` (for `ThemeToggle`) were each verified, and the four `/design` routes screenshot with `tools/ui/shoot.py`.

**Known gap, not fixed here.** Nothing prevents the next `text-h3`. An undefined utility is invisible to `tsc`, to `next lint` and to any test that does not rasterize — which is how these survived. A check that greps `className` for `text-*` tokens absent from the built CSS would close it and does not exist yet.

**Status:** Accepted (2026-08-09). Completes the cleanup ADR-0036 deferred.

---

## ADR-0037 — v1.1.0 publishes under ADR-0027's terms; A5 and C8 are still NOT PASSED

**Context:** ADR-0027 reclassified A5 and C8 as deferred evidence debt **for v1.0.4 publication only**, and said so twice — "this patch only" in `RELEASE_CHECKLIST.md`, "only for publication of v1.0.4" in BACKLOG C8. v1.1.0 is a **minor** release, not a patch: it adds on-device speaker diarization with talk time. The exception therefore does not reach it, and publishing without saying so would assert an authorisation that does not exist. On 2026-08-10 the product owner, having been shown exactly what the exception is and is not, directed that 1.1.0 publish.

**What was checked before recording this, rather than assumed.** Every substantive constraint ADR-0027 imposed is satisfied by 1.1.0:

| ADR-0027 constraint | 1.1.0 |
|---|---|
| no measured WER/CER or verified accuracy claim | none made |
| **no diarization-quality claim** (named explicitly in ADR-0027 §2) | the panel says "best-effort estimate ... has not been measured on recordings like yours"; the landing page repeats it |
| copy must say accuracy varies and output must be verified | unchanged, and the notices now state the fact rather than claiming a deferral |
| does not unlock D1 / F0 / G1 | untouched |
| signing, exact-SHA CI, updater continuity fail-closed | unchanged |

ADR-0027 anticipated this shape: *"Later patches may carry the same honest limitation."* This records that the same limitation is carried, for a minor rather than a patch, with the owner's direction as its basis — the same basis ADR-0027 had.

**Decision:** A5 and C8 remain **DEFERRED / NOT PASSED** for v1.1.0 publication. This is a sequencing exception on the same terms as ADR-0027, not a PASS, GO, CLOSE or waiver. Everything ADR-0027 forbade remains forbidden, and its clauses 1-6 carry forward verbatim.

**Risks accepted that did NOT exist at v1.0.4, and are disclosed rather than buried:**

1. **Diarization accuracy is unmeasured on real meetings.** The A5 `multi` bucket still holds zero recordings. Worse than "unmeasured": in the two synthetic runs, the 60-minute file produced **four** speakers and the 90-minute file, built from the same two voices by the same method, produced the correct **two**. Same material, same pipeline, different answer. The product copy claims nothing about accuracy, and H9 is now the gating work rather than a nice-to-have.
2. **A diarization pass takes about a quarter of the recording's length** — measured 14 min 27 s for 60.1 min and 21 min 21 s for 90.1 min — **with no progress indicator and no cancel** (H12). A user watching an `Analysing…` label for fourteen minutes may reasonably conclude the app has hung. Disclosed on the landing page; H12 is filed.
3. **Peak memory outgrows the input**: 600 MB at 60 minutes, **1033 MB** at 90. The 2-hour case is unmeasured and must not be extrapolated from two points.

**What this does NOT authorize:** any accuracy or field-performance claim; any target-environment or industrial positioning; a pilot-proven value claim; or any downstream epic that depends on C8. A5 remains mandatory before those. The next release that wants to broaden validated scope needs the real human artifacts, not another ADR.

**Status:** Accepted (2026-08-10; explicit product-owner direction, on the same basis as ADR-0027). A5 and C8 are deferred for v1.1.0 publication, not passed.

---

> **Numbering note (2026-09-11):** two accepted records above share the number **ADR-0037** (the type-scale record and the v1.1.0 publication record). Both are left as they are — renumbering an accepted ADR would break the references that already point at it — and this record takes **ADR-0038**.

## ADR-0038 — Live Copilot: private, not covert — in-meeting assistance without the deception features of its category

**Context:** The owner asked (2026-09-11) for a gap analysis against Cluely and its open-source clones, and a plan to close the gaps. The analysis is recorded in `docs/COMPETITIVE_CLUELY_GAP.md`; the executable tasks are BACKLOG **EPIC I**. Four findings shaped this decision:

1. **The gap is a category, not a feature.** Everything Mityu does happens *after* capture: source-linked summaries (C1), action items (C2), Evidence Search (C3a), Action Center (C3b), export (C4), even "Ask This Meeting" (Product Intelligence slice 3) runs over a finished transcript. The competitor category is defined by help *during* the conversation — a private always-on-top panel with "what should I say next", follow-up questions, a live recap, fact-checking against a knowledge base, and screen context — plus meeting modes and post-call recipes. Cluely's documented surface (`docs.cluely.com`: Live Insights, Pre Call Briefs, Meeting Notes, Knowledge Base, Customization Suite, Role Management, CRM/ATS Integrations, Undetectability) confirms the shape; the reference repository (Natively) shows one working architecture for it.
2. **The category's signature feature is deception, and it does not even work.** "Undetectable" overlays, process-name disguise, dock/taskbar hiding and keyboard hooks that type without taking focus exist to hide an assistant from other people in the room or from proctors. That contradicts `CLAUDE.md` §0.6 (consent is architectural), the EU AI Act Art. 50 posture Mityu has verified (ADR-0032), and the KVKK stance in `docs/SECURITY_PRIVACY.md`. It is also technically hollow on macOS: since macOS 15 ScreenCaptureKit ignores `NSWindow.sharingType = .none`/`setContentProtection` (Apple DTS, developer forums thread 792152: *"At this time there are no public APIs for preventing screen capture"*; tauri-apps/tauri#14200, open, upstream). Windows `WDA_EXCLUDEFROMCAPTURE` does work (Windows 10 2004+; older versions render a black box); Linux has no equivalent. A product that promised invisibility would be lying on one of its two supported platforms.
3. **The reference code cannot be used, only learned from.** Natively ships under the *Natively Personal Use Source License v1.0* (non-commercial only); Glass, Cue, Cheating-Daddy and the old Pluely releases are GPL-3.0. Mityu is a commercial MIT product; none of that code, and none of those prompts, may be copied. The permissively licensed building blocks are Tauri's own window APIs (`set_content_protected`, `always_on_top`, `transparent`, `set_visible_on_all_workspaces`, `set_ignore_cursor_events`), `tauri-plugin-global-shortcut` (MIT/Apache), `tauri-nspanel` (MIT/Apache; used in production by Cap and Screenpipe) and `xcap` (Apache-2.0).
4. **Most of the substrate already exists.** `TranscriptUpdate` already carries `source` (`microphone`/`system`), `is_partial` and recording-relative timings (`audio/transcription/worker.rs:30`); `transcript-update` is already an app-wide event that Rust code subscribes to (`recording_commands.rs:323`); `ask::grounding` already implements "drop, never repair" citation checking; the `summary/` provider layer is already BYOK with a built-in local model; `redaction::redact` already sits at the prompt boundary; `summary/templates` already loads built-in and user-authored templates; the recording consent ticket (`recording_consent.rs`) already gates every capture start. The copilot is mostly a new *consumer* of existing seams.

**Decision:** Build the live copilot as BACKLOG EPIC I, under four invariants that are enforced structurally and re-verified at the I9 gate, and with an explicit reject list.

*Invariants*

1. **Private, not covert.** The `copilot` window is content-protected for the user's *privacy* (their private notes must not leak into a screen share) and carries an honest per-platform indicator — Windows "hidden from screen share (Windows 10 2004+)", macOS "best-effort: macOS 15+ may still capture this panel", Linux "not supported". The app never hides its own existence: no `skip_taskbar` hiding, no process renaming, no keyboard hooks, the recording indicator stays visible, and the word "undetectable" never appears in product copy or marketing.
2. **The copilot never captures on its own.** It consumes only the `transcript-update` stream of a recording session whose consent ticket was consumed; there is no separate listening mode; no task in EPIC I modifies `audio/` (CLAUDE.md §4). Screen context is a single, user-initiated, previewed and confirmed capture — never continuous, never on a timer, never persisted by default.
3. **Every insight is an AI-labelled, source-linked, non-persisted draft.** Art. 50(1) disclosure on first open and a non-dismissable Art. 50(2) marking on every card (the ADR-0032 test pattern); every claim cites a transcript segment (`source_chunk_id`) or, once I5 exists, a knowledge chunk; the `ask::grounding` rule holds — a citation outside the retrieved window is dropped, never repaired; an empty window yields a refusal, never an empty answer; the only persistence is "Pin to notes", which writes a **draft** block with `source_chunk_id` into `summaries.sections` through the existing repository (status forced `draft`, the C1 rule). There is deliberately **no** copilot table. The copilot produces text and nothing else: no tool calls, no sends, no writes to external systems (CLAUDE.md §10; OWASP LLM01 — transcript, screen and knowledge text are untrusted input).
4. **Flag-gated, default OFF, quality-gated ON.** Mechanics land behind the `liveCopilot` beta flag (with it off: no shortcut registered, no window created, byte-identical behaviour — the `sync/`/`agents/`/`inference/` dormant-seam precedent). This may happen before A5/C8 on the same basis as C1/C3a (ADR-0019, ADR-0024): the mechanics are transcription-quality-independent. **Defaulting the flag ON, and any "live help" or latency claim, requires A5 GO plus the I9 gate** (network-OFF smoke, live smoke on real consented meetings with latency and citation-validity measured, Art. 50 tests, `/security-review`, multitenancy-guardian). The copilot does not pass A5; A5 unlocks the copilot.

*Where the code goes* — `frontend/src-tauri/src/copilot/` (`window.rs`, `session.rs`, `insights.rs`, `policy.rs`, `commands.rs`), `knowledge/` (documents, chunks, FTS5 derived index — the `transcript_search_*` pattern of ADR-0024/0026), `modes/` (a superset of `summary/templates`), `screen/` (`xcap` + OS-native OCR behind the ADR-0033 capability seam), `usage/` (content-free `usage_events`); frontend `app/copilot/` loaded by the second window from the static export, `services/{copilot,knowledge,modes,usage}Service.ts`, a Copilot settings tab, a Knowledge tab and a Modes editor. `ask::grounding`'s `SourceRef` gains `Knowledge` and (non-persisted) `Screen` kinds under the same rule.

*Schema* — `knowledge_documents` and `knowledge_chunks` are SYNCED-class tables (common + sync columns from day one) so a Phase-2 admin-managed team knowledge base (the competitor's "Customization Suite") is additive; `knowledge_search_*` and `usage_events` are local-only. Every schema change ships as its own PR, never with an audio change (CLAUDE.md §6/§10).

*Sequence* — I0 (this ADR) → I1 window shell + global shortcuts → I2 deterministic live context ∥ I4a modes registry → I3 on-demand insights (Suggest · Follow-ups · Recap · Define) → I5 local knowledge base (enables "Fact check" against transcript + knowledge only) → I6 screen context → I8 opt-in proactive offers, usage ledger, local pre-call brief (brief waits for G2) → I9 gate. I7 (deterministic recipes over approved content, cross-meeting recall, manual speaker names) is independent of the I-series and builds on C1/C3b/H6.

*Rejected, with the reason recorded rather than implied*

| Rejected | Why |
|---|---|
| Undetectability, stealth mode, process disguise, dock/taskbar hiding, keyboard hooks | Deception; §0.6; unreliable on macOS 15+; keyboard hooks are a keylogger-class security surface |
| Candidate-side interview/exam assistance, coding-interview solver modes | Assessment integrity; brand risk; contradicts the regulated-enterprise beachhead |
| Continuous screen recording (Rewind/Screenpipe model) | Privacy, storage, KVKK; screen context is one previewed frame |
| Web research/profiling of meeting participants ("who am I talking to") | Third-party personal data; the local version (calendar metadata opt-in + prior meetings + open approved items) gives the value without it |
| Web fact-checking by default | Egress; a future opt-in BYOK search needs its own ADR |
| Post-call "coaching" and communication-gap scores | Emotion/performance inference boundary (`DESIGN_READAI.md`; EU AI Act); the reference product switched its own off as low-value |
| Autonomous send / CRM write / calendar change | HITL (§0.5, §10); approval-gated push lives in EPIC G |
| Cloud-proxied OAuth for calendar | ADR-0018 requires client-direct OAuth with tokens in the OS keychain |
| Copying Natively or GPL code/prompts | Licence incompatibility with a commercial MIT product |

**Consequences:** Mityu gains the category's genuine value — help while the conversation is happening — on the one axis the category cannot follow: consent-grade, source-linked, human-approved and honest about what the OS can and cannot hide. `CLAUDE.md` §1's "not a meeting bot" is unchanged (the copilot joins nothing and captures nothing new). `ROADMAP.md` gains Phase I; `STRATEGY_2026-2030.md` §2 gains the corresponding parity rows. Costs accepted: a second window and a global-shortcut plugin widen the desktop surface; a knowledge base doubles as a second copy of user documents inside SQLCipher (same posture as the FTS index, ADR-0026); the live experience is bounded by the capture pipeline's VAD segmentation (seconds, not milliseconds — no latency figure is published without I9's measurement); any streaming/latency work in `audio/` is a separate, §4-smoked change.

**Status:** Accepted as design direction (2026-09-11; owner review pending). I0 satisfied by this record; I1–I9 tracked in BACKLOG EPIC I; `liveCopilot` default OFF until I9.

---

## ADR-0039 — The live context follows the transcript producer, not the plan: every emission is final, and no segment is attributed to a device yet

**Context:** Building BACKLOG I2 (2026-09-13) against the actual producer of `transcript-update` (`frontend/src-tauri/src/audio/transcription/worker.rs`) showed that two premises written into ADR-0038 finding 4 and into the I1/I2 acceptance criteria are false:

1. **`TranscriptUpdate.source` does not carry `microphone`/`system`.** The worker hardcodes `source: "Audio".to_string()` for every segment (`worker.rs`, the single `TranscriptUpdate { … }` construction). There is no me/them signal in the stream. I1 had already run into this on the panel side and shipped the tail unattributed; I2 is where the service layer met the same fact.
2. **`is_partial` is not a lifecycle.** Whisper sets it as `duration_seconds < 15.0` (`whisper_engine/whisper_engine.rs:738`); Parakeet never sets it. Each chunk is emitted exactly once with a fresh `sequence_id` from a shared counter, and nothing later replaces it. The live VAD closes a segment after **400 ms** of silence (`audio/pipeline.rs`'s `redemption_time`; the 2 000 ms `VAD_REDEMPTION_TIME_MS` belongs to the offline import and retranscription paths only, as ADR-0034 states), so nearly every real utterance is far shorter than 15 s and therefore flagged "partial". The main transcript view (`TranscriptContext.tsx`) already treats every emission as one segment and de-duplicates by `sequence_id`; the recording saver stores all of them. The I1 panel had implemented the plan's reading and was **dropping `is_partial` segments — hiding most of the meeting**.

A third fact shaped the buffer, and the first draft of this record got it **wrong**: it claimed three transcription workers run in parallel, so that `sequence_id` would not follow audio time. The producer does the opposite — `worker.rs`'s `const NUM_WORKERS: usize = 1; // Serial processing ensures transcripts emit in chronological order` — so emission order and audio order coincide today. (The claim came from the stale `"workers": 3` literal in the `recording-started` payload, which describes nothing; correcting that literal is `audio/`'s to do.) The buffer is still ordered by audio time, now justified as defence in depth rather than as a response to observed behaviour. This correction was made before the record landed, by the pre-merge audit; nothing outside this branch ever read the false version. A fourth fact was caught by the multitenancy-guardian review of the I2 diff: `recording-stop-complete` is emitted **only by the tray's stop** (`tray.rs`); the ordinary stop path emits `recording-stopped` (`audio/recording_commands.rs`). The I1 panel listened to the tray event alone, and the first draft of I2 did too — which would have kept a finished meeting's transcript in memory until the next recording.

**Decision:**

- `copilot::session` treats **every** `transcript-update` as a final segment. `is_partial` is carried as `Turn.short_chunk` and decides nothing. A segment's identity is `sequence_id`; a re-emission replaces in place and never fires a second cue.
- Segments are ordered by **audio time** — defence in depth against `NUM_WORKERS` being raised again, not a response to present behaviour — and the rolling window is measured back from the latest segment *end*, not from the wall clock, so a pause in the meeting does not empty it. The window is a **view for building a prompt, not a retention bound**: nothing is evicted by it, and a meeting is resident in full while it is recorded.
- Channel attribution is an explicit `Channel::Unknown` until the pipeline carries it. `Channel::{Microphone, System}` and the rule "no cue from the user's own microphone" are implemented and tested **now**, against the value the producer will send, so that the day `source` becomes real nothing in the copilot moves. Populating `source` is an `audio/` change and stays out of EPIC I (CLAUDE.md §4); it is filed as BACKLOG **H13**.
- The panel's transcript tail shows every segment, unattributed.
- The session buffer is rebuilt at `recording-started` — re-resolving `AuthContext` there, so a Phase-2 tenant switch between two meetings cannot leave a buffer stamped with the previous workspace — and cleared on **both** stop events. The panel clears on both as well.
- The `copilot-cue` event carries ids and numbers only (`workspace_id`, kind, confidence, evidence `sequence_id`s); the consumer resolves them against the transcript it already holds. I3's consumer must drop a cue whose `workspace_id` is not its own.
- ADR-0038 finding 4 is **not** edited — accepted records are not rewritten; this record corrects it. BACKLOG I1/I2, `COMPETITIVE_CLUELY_GAP.md` §6 (the me/them row: ADOPT → DEFER) and `ROADMAP.md` Phase I are updated to say what the code does.

**Alternatives rejected:**

| Alternative | Why not |
|---|---|
| Implement the AC literally (drop partials, split me/them on `source`) | Drops most speech; invents an attribution the data does not contain. Both were caught only because the producer was read before the consumer was written. |
| Infer me/them from a heuristic inside the copilot (energy, device names in `timestamp`, voice) | Guessing a speaker from the signal is what ADR-0034 forbids; device-name heuristics have no data to work on. |
| Change `worker.rs` in I2 to send the real device | Correct fix, wrong place: the audio pipeline is the §4 high-risk zone and no EPIC I task may touch it (ADR-0038 invariant 2). It gets its own smoked change. |
| Keep the buffer in `sequence_id` order | Correct today (one serial worker), but it makes "the last 180 seconds" depend on a producer constant no consumer controls; audio-time ordering costs one `partition_point` per segment. |

**Consequences:** I3's prompt assembly must not rely on me/them, and its "Suggest" action cannot distinguish the user's own question from the counterpart's until H13 lands; the I8 proactive prefilter inherits the same limit. Any speaker-side feature in EPIC I depends on H13, which is an `audio/` task with the §4 manual smoke as its gate. The "no cue from microphone" gate is documented as inert where it lives (`session.rs` module doc).

**Status:** Accepted (2026-09-13). Implemented in I2 (`feat/i2-live-context`). Two producer facts in the first draft (three parallel workers; a 2 s live VAD) were false and were corrected above by the pre-merge audit before this record reached `main` — which is the same failure this ADR exists to prevent, caught one step earlier.

## ADR-0040 — The live window is redacted where it is built, and a live insight does not leave the device by default

**Context:** BACKLOG **I3a** (2026-09-13) adds the first model call in EPIC I: the copilot takes the rolling window built by I2 and asks a provider for one of I4a's four actions. Building it surfaced two facts that the I3 acceptance criteria assumed were already handled.

1. **The live window has never been redacted.** C6's opt-in redaction (`redaction::redact`) runs at two seams: the transcript write into SQLite, and the summary prompt. The live context is neither — `copilot::session` is built straight off the `transcript-update` stream and never touches the database. So an opted-in workspace's policy would have held on every surface *except* the one that speaks while the meeting is still happening, which is the surface where the conversation is freshest and the user is least able to check what left.
2. **A live insight is not a per-meeting provider decision.** The summary path's provider is chosen deliberately, once, for a meeting that has ended. A live insight fires from a global hotkey mid-sentence. Treating "which provider is configured" as consent to send the last minutes of a live conversation to a third party makes a privacy decision follow from a keypress the user set up for something else.

**Decision:**

- **Redaction happens at the prompt boundary, in `copilot::insight::window_passages`, and that function is the only path from a `Turn` to anything a prompt may contain.** A turn's `text` is never read directly by prompt assembly. Citation ids are derived from `sequence_id` alone (`passage_id`), so redaction rewrites the evidence text without breaking the link back to the segment — the HITL source-linking contract (docs/CONTRACTS.md §4) is preserved exactly as it is at the other two seams.
- **`CopilotConfig::allow_cloud_insights` defaults to `false`.** With it off, `generate_insight` refuses any provider it cannot *show* is local and returns `InsightError::CloudNotAllowed`; the built-in local model answers instead. `is_local_provider` is conservative and asymmetric: only `BuiltInAI` and `Ollama` are local, because locality is a property of the build for those two. `CustomOpenAI` is **not** local even with a `localhost` endpoint — the endpoint is user-supplied config that can change between the check and the request, so the policy would rest on a string rather than a fact. An unparseable provider name fails closed. The gate sits after the window is built and before the provider is assembled, so the user is told "not allowed" rather than "nothing to say", and no prompt carrying conversation text is ever handed to a provider the workspace did not allow.
- **An empty window is a refusal, and the model is not called.** `LiveInsightOutcome::NoContext` mirrors `AskOutcome::NoEvidence`: there is no "answered with nothing" variant, because an empty card reads as a statement about the conversation. A model consulted with an empty window would be answering from its own prior knowledge, which is the thing this epic promised not to ship.
- **`EvidencePolicy::Open` is declared but NOT honoured yet, and that is written down rather than implied.** Honouring it means a second grounding path that keeps unsourced model output — the one thing the epic's invariants exist to prevent — so it is not being added in the slice that introduces the first model call. Until it is, the one `Open` built-in (`lecture`) is grounded as strictly as a `SourceFirst` one. The failure mode is "the lecture copilot said less than it could have", not "something unsourced reached the user". A test asserts the equivalence so this record fails the day someone assumes leniency.
- **A prompt is capped at `MAX_PROMPT_TURNS` (40) and keeps the *recent* end**, reporting `turns_omitted` rather than silently truncating. The window is measured in seconds, so a fast meeting can overflow what the built-in local model will cope with; a live question is about now.
- **`prepare` is split out of `generate_insight`** so every refusal that happens before a model is consulted — wrong workspace, disallowed action, no readable source, empty window — is a unit test over data rather than something only an integration test can reach. Tenant mismatch between the live context and the caller's `AuthContext` is one of them (docs/MULTITENANCY.md rule 2: in-memory state is tenant state).
- **`AssembledProvider::call_cancellable`** is added alongside `call`, which now delegates to it with `None`. Every provider already honoured a `CancellationToken`; `call` simply never had one to pass. A live insight is abandoned on a 25 s timeout that also cancels the in-flight request, rather than leaving it to finish into nothing.

**Consequences:** The eight rules above are held by mutation-checked tests (removing the `redact` call, inverting the tenant check, letting an empty window through, keeping the oldest turns instead of the newest, using `allowed_sources` instead of `usable_sources`, dropping the action gate, defaulting the cloud policy to on, and treating `CustomOpenAI` as local each fail at least one test). Nothing in this slice is reachable from the UI: I3a registers no Tauri command and is called from nothing — I3b adds the panel, the Article 50 marking and "Pin to notes". The first model call therefore ships **unexercised against a real provider**; prompt assembly, the redaction boundary, the policy gate and grounding are proven by unit tests, but what a model actually returns for these prompts is an I9 live-smoke question and is not claimed here.

## ADR-0041 — "Pin to notes" cannot be built on the existing summary repository, and is split out of I3b rather than shipped broken

**Context:** BACKLOG **I3b** (2026-09-13) was specified with a "Pin to notes" action: one insight becomes a `draft` block carrying its `source_chunk_id`, written into that meeting's structured summary draft **through the existing C1 repository**, with the status forced to `draft`. ADR-0038 finding 3 says the same. Building the rest of I3b meant reading that path properly for the first time, and the specification turns out to rest on three premises that the code does not support. Each was verified directly, not inferred.

1. **There is no meeting row to write to while the meeting is happening.** `SummariesRepository::upsert_draft` refuses outright when one is missing (`summary_draft.rs:333-347`, `SummaryDraftError::MeetingNotFound`), and `edit_block`/`set_block_status` degrade to `Ok(false)`. The only production `INSERT INTO meetings` for a recording is inside `TranscriptsRepository::save_transcript`, which mints the id itself (`transcript.rs:35`, `meeting-{uuid}`) in the same transaction that stores the transcript — i.e. **after the recording ends**. During recording, `audio/recording_commands.rs` holds a pending recording with `meeting_id: None`. A pin during a live meeting would therefore fail 100 % of the time, not occasionally.
2. **The citation a pinned block would carry does not resolve.** Every block's `source_chunk_id` must point at a live `transcripts` row for that meeting (`ensure_sources_resolve`, `summary_draft.rs:215`), re-checked under the write lock. The copilot's evidence ids are `t{sequence_id}` (`copilot::insight::passage_id`) — synthetic, in-memory, and correct for the live window, where no `transcripts` row exists yet. There is no id-space bridge between the two, and building one is not a UI change.
3. **Even after the meeting is saved, the pin is guaranteed to be destroyed.** `upsert_draft`'s UPDATE binds `sections = ?` unconditionally from the caller's draft alone (`summary_draft.rs:397-402`); existing sections are never read first. `SummaryService` calls it with only the model's output (`summary/service.rs:1066-1073`). So the ordinary, unavoidable post-meeting summary — and every "Regenerate" after it — erases any pinned block, with no archive of the previous JSON and with `rev` bumped so it reads as a legitimate write. There is no append API anywhere on `SummariesRepository` to avoid this; the only suitable primitive, `write_sections_as_draft_if_current` (`summary_draft.rs:1150`), is private.

**Decision:**

- **"Pin to notes" is removed from I3b and filed as BACKLOG I3c.** I3b ships everything else in its acceptance criteria: the four actions, the Tauri command over `copilot::insight`, cancel and timeout, the Art. 50(1) disclosure and the non-dismissable Art. 50(2) marking, and a distinct rendering for every refusal. Nothing in I3b persists anything, which was already the epic's invariant 3 — the slice is now simply *entirely* non-persisting.
- **Shipping it against the current repository was considered and rejected.** The failure is not a rough edge: it silently deletes content the user hand-curated during their meeting, on a code path they cannot avoid taking. A feature that loses user-authored work is worse than an absent one, and "it usually survives" is not available here — the loss is certain, not probabilistic.
- **I3c's minimum design is recorded now so the next person does not re-derive it**: buffer pins in memory in `copilot::session` keyed by the `sequence_id` the live context already holds; flush at save time, once the meeting id and real `transcripts` ids exist and the `t{sequence_id}` → `transcripts.id` mapping can be resolved; add `SummariesRepository::append_block` on top of the existing private CAS writer rather than open-coding a second `sections = ?` writer; and decide how `upsert_draft` preserves non-generated blocks — either a provenance marker on `DraftBlock` (`generated` vs `pinned`) or an explicit merge. The last of those changes the `summaries.sections` JSON shape, so it needs its own ADR, a `docs/CONTRACTS.md` §4 update and a sync-compatibility note **before** any code.
- **ADR-0038 finding 3 is not edited.** Accepted records are not rewritten; this record corrects the "Pin to notes" half of it. What it says about marking, source linkage, grounding and refusal is unaffected and is implemented in I3a/I3b.

**Consequences:** The copilot can now answer during a meeting but cannot keep anything, which is a smaller product than I3 described and is stated as such in BACKLOG rather than left for a user to discover. The I9 gate must not be read as covering I3c. One thing improved on the way: the live context is no longer reachable as a borrowed value — `session::with_context` takes a synchronous callback, so the process-global mutex guard cannot be held across a model call, and `copilot::insight` was split into a synchronous `prepare` and an async `complete` to match.

## ADR-0042 — A mode's wording is editable, its permissions are not; and a custom mode can never shadow a built-in

**Context:** BACKLOG **I4b** (2026-09-14) puts I4a's mode registry in front of the user: a chip in the panel saying which mode is answering, and a Settings tab that lists the built-ins, installs a custom mode from a file, edits one and removes one. I4a deliberately shipped the *data and the validator* with no way to reach them, so the questions this record answers are the ones only the UI raises.

**Decision:**

- **The Settings editor exposes `name`, `purpose` and `voice` — and nothing else.** A mode's `id`, its `live.allowedActions` and its `allowedSources` are what it *permits*, and they stay in the authored file. Widening a permission is an install, not an edit: a text box that could add `Suggest` to a mode whose author deliberately withheld it (the recruiting-interview mode is exactly that case) would give the mode a capability nobody wrote. This is the same reasoning as I4a's refusal to put `serde(default)` on `Mode` — permissions are authored, never migrated or typed in beside a save button. `modes_update` enforces it server-side; the editor simply has no field for the rest.
- **Built-in modes are read-only and cannot be removed.** They are the shipped vocabulary, and a user who could rewrite `General` could make the copilot's default behaviour unexplainable to the next person who opens the app — including to support. Importing a copy under a new name is the supported path and is one click.
- **A custom mode can never shadow a built-in, so `all_modes` needs no precedence rule.** `validate_mode` already refuses an id or name that collides with anything installed, and the built-ins are always in the list it checks against. `registry::all_modes` therefore concatenates built-ins then custom with nothing to break a tie between. "Your custom `general` silently replaced the shipped one" is the class of surprise that makes behaviour unexplainable, and the design makes it unrepresentable rather than merely unlikely.
- **`resolve_active` is total; a stale pointer falls back to the default.** The stored `activeModeId` can name a mode the user has deleted, or one written by a build that had a mode this one does not. The copilot must still answer, so an unknown id resolves to `general` rather than failing a request the user just made. `modes_remove` *also* rewrites the pointer when it removes the active mode — not because the fallback needs help, but so the stored state and the Settings list never disagree about what is selected. Every command returns the whole view with the **resolved** id for the same reason.
- **A stored blob from another workspace is discarded, never migrated.** `modes::store::load` compares the stored `workspace_id` against `context::current()` and falls back to the built-ins on a mismatch (`docs/MULTITENANCY.md` rule 2). Rewriting the field to the current tenant is the tempting one-liner and is precisely the bug the rule exists to prevent: it would hand one tenant another's custom modes, purposes and role wording. Losing a custom mode on a tenant switch is the correct failure.
- **No validation is mirrored in TypeScript.** The import flow asks the backend to check a file and renders the validator's own sentence. A second copy of the size cap, the collision checks and ADR-0038's rejected category would drift, and only the Rust one guards the store.

**Consequences:** Choosing a mode in Settings changes the panel's buttons and the prompt on the next status poll — `copilot_get_status` now reports the *active* mode's actions, id and name, and `copilot_request_insight` builds from the active mode rather than the default. Seven registry tests and fifteen Vitest cases hold the rules above; six mutations were applied and killed (a panicking `resolve_active`, an ignored workspace check, built-ins hidden from the taken list, built-ins made editable, an import that skips the preview, and an unwired save guard — the last of which **survived the first attempt**, because the tests exercised the pure guard function without asserting the button was connected to it, and a test was added for the connection). The Settings tab has no `/design` route: giving it one would mean restructuring a container into a presentational component purely to produce a screenshot, and the interaction wiring is already covered by tests; the panel's mode chip *is* in `/design/copilot`.

## ADR-0043 — v1.2.0: three pre-release security fixes, and what publishing it does and does not authorise

**Context:** EPIC I (I1, I2, I3a, I3b, I4a, I4b) is on `main` and the owner directed that it publish. A pre-release security/privacy review and a local-first/tenancy audit were run over the whole change set before cutting the version. Neither found a BLOCKER, but the security review found two HIGH findings that would each have made a promise the product makes untrue, and the tenancy audit found a third. All three are fixed in this release rather than carried, because each is small, contained, and about a claim rather than a nicety.

### The three fixes

1. **A remote Ollama endpoint defeated the egress policy.** `copilot::insight::is_local_provider` answered on the provider *name*, returning `true` for `Ollama` unconditionally — while the same file argued, two paragraphs above, that `CustomOpenAI` is **not** local *because its endpoint is user-supplied config that can change*. `settings.ollamaEndpoint` is exactly as mutable and is read with no host check (`summary/service.rs`). A workspace with `allowCloudInsights` **off** — shown the sentence "this workspace does not allow live insights to reach a cloud provider" — would still have POSTed the last three minutes of conversation to `https://ollama.example.com` on every hotkey press. This was my own reasoning applied inconsistently, not an unconsidered case. **Fix:** the gate now runs on the **assembled** provider (`provider_is_local(&AssembledProvider)`), after `assemble_provider` — which reads settings and the keychain but performs no request and sends no prompt — and before anything is sent. Ollama counts as local only when its endpoint's host is loopback (`127.0.0.0/8`, `::1`, `localhost`), a LAN address does not, and an endpoint that cannot be parsed fails **closed**. Checking the assembled value also removes the window between the check and the request.
2. **A shared mode file could put fabricated claims in front of the user, with a real timestamp.** `Mode.name/purpose/userRole/counterpartRole/voice` are interpolated into the system prompt, and `validate_mode` checked only that they were non-empty — no length cap, no character class. `MAX_MODE_BYTES` bounds the raw *file*, so it never applied to `modes_update` at all: the Settings editor could write a field of any size. A mode is content a user was **given** — shared in a chat, downloaded from a page — so it is untrusted in exactly the way a transcript is, and nearly 16 KB of free-form prose is far more than enough to restate the output contract. `ask::grounding` verifies that a cited id was retrieved but **not** that the claim text is supported by that passage, so injected text would have rendered as a grounded claim stamped "said at 00:14" from the user's own conversation — defeating the single guarantee the whole epic rests on. **Fix:** per-field character caps (name 60, purpose 400, roles 120, voice 200, template id 64) and rejection of every control character, enforced inside `validate_mode` so an edit is bound by the same rule as an import. Second layer: the mode fields are now rendered as a JSON object placed **after** the rules, under a line stating they are configuration that cannot change, relax or replace any rule above — the first version put them at the top, where a long `voice` read as a later, overriding instruction.
3. **Redaction failed open on the one path with no second line of defence.** `copilot_request_insight` read the policy with `unwrap_or_default()`, and `RedactionConfig::default()` is *disabled*. A locked or briefly unavailable SQLite pool in a workspace that had switched redaction **on** would therefore have sent an unredacted window to a provider, silently. The live window never passes through SQLite, so unlike `ask_meeting` there is no earlier scrub to fall back on. **Fix:** fail closed — the insight is refused with `redactionUnavailable` and a sentence saying why. Refusing is recoverable; a leaked window is not.

One shipped claim was also corrected rather than left: the `copilot` capability's description in `tauri.conf.json` read as a sandbox guarantee. Tauri 2 capabilities do not gate the app's own `#[tauri::command]` handlers, only plugin commands, so the panel webview can invoke them like any other window. The description now says so, and names the boundary that *is* real — every copilot command resolves identity from `AuthContext` in Rust and never trusts an id from the renderer.

### What publishing v1.2.0 does NOT authorise

- **A5 and C8 remain `DEFERRED / NOT PASSED`**, on the same terms as ADR-0027 and ADR-0037. No measured accuracy claim, no SLA, no demonstrated pilot value. v1.2.0 adds no accuracy claim of any kind.
- **The I9 gate is NOT passed, and v1.2.0 does not pretend otherwise.** The copilot ships **off by default** (`CopilotConfig::enabled = false`), which is what makes publishing it honest: a user who does not turn it on gets a build that behaves exactly like one compiled without the module — no shortcut registered, no listener, no window, no buffer. Turning the default on still requires A5 GO plus the live smoke (ADR-0038).
- **The model call has never been exercised against a real provider.** Prompt assembly, the redaction boundary, the egress gate and grounding are proven by unit tests; what a model actually returns for these prompts is unverified. This is the substantive limitation of the release and must not be described as tested.
- **"Pin to notes" is not in the product** (I3c, ADR-0041). Nothing the copilot produces can be saved.
- **The release matrix is Windows x64 only**, updater-signed, Authenticode deferred (ADR-0029). macOS and Linux remain development targets; nothing here implies macOS validation.

### Accepted, disclosed residuals

| Residual | Why it is not fixed now |
|---|---|
| `allowCloudInsights` is stored in `copilot.json` with no `workspace_id` | `LOCAL_WORKSPACE_ID` is the only workspace that exists, so it is unreachable today. **Must be fixed before any multi-workspace build ships** — it is an egress policy over tenant content in a workspace-agnostic file. |
| `modes.json` holds one workspace's state under one key, so a second workspace would overwrite the first's custom modes | Same reason. A durability bug, not a confidentiality one: `load` already discards a foreign blob rather than leaking it. |
| `Mode` has no `created_at`/`updated_at`/`rev`/`deleted_at` | Serde defaults absorb them later, so Phase 2 stays an addition rather than a rewrite. Worth adding while `modes.json` has no installed base. |
| Polar API version pin (`2026-04`) unverified | `RELEASE_CHECKLIST.md` §2c asks for a live check; the session's egress proxy refused `api.polar.sh`, so this is **unverified, not passed**. The pin is soft (`PolarApi::post` retries unpinned), so a stale pin is a wasted round trip rather than an outage. |
| `content_protection` request failure is logged, not surfaced | Settings can report protection as available while the panel is capturable. Pre-existing; folding the call result into `ProtectionVerdict` is a follow-up. |
| CSP `connect-src` still lists `http://localhost:5167` (the archived Python backend) and `8178` | Pre-existing and outside EPIC I. Editing CSP immediately before a release adds risk for no release-scoped benefit; filed as follow-up. |
| The failed-`stop_recording` path can retain the in-memory buffer | Documented in `session::discard_if_idle` and `docs/SECURITY_PRIVACY.md`; fixing the recorder's own state is an `audio/` change (CLAUDE.md §4). |

**Consequences:** six mutations were applied to the three fixes and all six were killed (Ollama local regardless of endpoint; an unparseable endpoint failing open; the field cap removed; control characters allowed; the framing sentence removed; mode fields rendered as raw prose instead of JSON). CI's real gate was run rather than assumed: `cargo fmt --all --check` clean, `cargo clippy --all-targets` 125 warnings with **none** in `copilot/` or `modes/`, `cargo test --all` **848 passed / 0 failed**, `pnpm run lint` clean, `pnpm tsc --noEmit` clean, `pnpm test` 247 passed. The release itself is dispatched by the owner from `main` through the `Release` workflow, whose `Production` environment requires their review — no part of publishing happens from an agent session.

## ADR-0044 — Every action pin is a sha on Node 24, the Vulkan composite is vendored, and CI enforces both

**Context:** The v1.2.0 Release run (#4, 2026-09-14) succeeded with one annotation: `build-windows / build` — *"Node.js 20 is deprecated. The following actions target Node.js 20 but are being forced to run on Node.js 24: actions/cache@0057852…, actions/cache@v4, actions/checkout@34e11487…, actions/setup-node@49933ea5…, pnpm/action-setup@b906affc…"*. Today that is a warning; when the runners drop the fallback it is a hard failure, and the first pipeline it breaks is the one that publishes. Two facts made this a design record rather than a repin:

1. **It had already been fixed once and the fix did not reach the file that matters.** PR #40 repinned six workflow files off Node 20 and deliberately deferred `build.yml` — *"another working copy has it rewritten and uncommitted, so editing it now would collide"*. That copy never landed. The deferral became permanent by accident, and nothing noticed, because a Node 20 pin costs nothing until the day it costs a release.
2. **One of the five offenders was in no file of ours.** `humbletim/install-vulkan-sdk@30ba978… (v1.2)` is a *composite* action; its own `action.yml` nests `uses: actions/cache@v4` — a floating tag, on Node 20, inside an action we had pinned to a sha. Our workflow looked clean. The warning is emitted when the composite is expanded, so it appears even though the nested step never ran (`cache: true` was set, but the step is gated on `inputs.cache != 'false'` — it *does* run). `v1.2` is that repository's newest tag; there is nothing to bump to. This is a supply-chain gap as much as a deprecation: the repo's own rule is immutable pins everywhere, and a pin whose contents float defeats it invisibly.

**What was verified rather than assumed.** Every remote action in `.github/workflows` was resolved from its sha with `git ls-remote --tags` (annotated tags peeled) and its `action.yml` read at that exact sha via a shallow fetch:

| Action | Was (sha → tag, runtime) | Now (sha → tag, runtime) | Inputs we pass still valid |
|---|---|---|---|
| `actions/checkout` | `34e11487` v4.3.1, **node20** | `3d3c42e5` v7.0.1, node24 | yes — same sha the other six files already use |
| `pnpm/action-setup` | `b906affc` v4, **node20** | `fc06bc12` v5.0.0, node24 | yes — #40's analysis: `dist/index.js` byte-identical to v4, only the runtime declaration changed |
| `actions/setup-node` | `49933ea5` v4.4.0, **node20** | `82076278` v7.0.0, node24 | yes |
| `actions/cache` (×2) | `0057852b` v4.3.0, **node20** | `caa29612` v5.1.0, node24 | yes — input set identical (`path`/`key`/`restore-keys`/`upload-chunk-size`/`fail-on-cache-miss`/`lookup-only`/`save-always`) |
| `actions/upload-artifact` (×3) | `ea165f8d` v4.6.2, **node20** | `043fb46d` v7.0.1, node24 | yes — we pass `name`/`path`/`retention-days`; `overwrite`/`include-hidden-files` default `false` and `archive` defaults `true`, matching v4 behaviour |
| `humbletim/install-vulkan-sdk` | `30ba978f` v1.2, composite → nests `actions/cache@v4` (**floating, node20**) | **vendored** as `./.github/actions/install-vulkan-sdk` with the nested cache pinned to `caa29612` v5.1.0 | identical inputs (`version`, `cache`) |
| `digicert/ssm-code-signing` | `fb61e357` v1.1.1, **node20** | unchanged — **allowlisted** | newest tag v1.2.1 (`1d820463`) is also node20; no node24 release exists |
| `tauri-apps/tauri-action` | `84b9d35b` action-v0.6.2, node24 | unchanged | — |
| `actions/github-script` | `3a2844b7` v9.0.0, node24 | unchanged | — |
| `swatinem/rust-cache` | `e18b4977` untagged commit, node24 | unchanged | — |
| `dtolnay/rust-toolchain` | `4be7066a` untagged commit, composite (no Node) | unchanged | — |

One correction to an earlier reading of mine: the `# v4.6.2` label on `upload-artifact` was **accurate** — the sha really was v4.6.2. The mistake was reading `ea165f8d` as the v7 sha because #40's message listed both. The stale-label rule stays in the guard regardless: labels do drift, checking them costs one `ls-remote`, and a label that lies is how the next reader makes the same mistake.

**Decision:**

- **`build.yml` is repinned to the table above.** No behaviour change is expected at any call site, and each one was checked against the new version's `action.yml`, not against release notes.
- **`install-vulkan-sdk` is vendored, not replaced.** `.github/actions/install-vulkan-sdk/` holds upstream's `action.yml`, `vulkan_prebuilt_helpers.sh` and `LICENSE` (MIT, © 2022 humbletim) copied verbatim from `30ba978f`, with exactly one line changed — the nested `actions/cache@v4` → `caa29612 # v5.1.0` — and a header saying why. Re-implementing the SDK install would have traded a known 60-line script for an unknown one; vendoring keeps the diff against upstream to a single line anyone can verify.
- **`digicert/ssm-code-signing` stays on node20, by allowlist, with the reason in the file.** DigiCert has shipped no node24 release. Every step that uses it is gated behind Authenticode secrets that are not configured — Windows code signing is deferred by owner decision (ADR-0029) — so the action never executes. The allowlist entry is printed as a warning on every CI run so it cannot be forgotten, and says to remove itself the moment a node24 tag exists.
- **The stale "Manual S3 upload … `s3://mityu-updates/latest.json`" line is removed from the release summary.** The updater's only endpoint is `https://github.com/aydogandagidir/mityu/releases/latest/download/latest.json` (`tauri.conf.json`); the S3 instruction is an upstream-Meetily leftover that told the person publishing a release to do something that does not apply.
- **`tools/ci/check-action-pins.py` enforces the property that regressed, in `ci.yml` as `workflow-pins`, on every push and pull request.** Because `release.yml` runs `ci.yml` as its quality gate, a Node 20 pin now blocks a release **before a draft is created**. Rules: every remote `uses:` is a 40-hex sha; every JavaScript action declares `runs.using: node24` (composite and docker pass; node12/16/20 fail unless allowlisted with a reason); **composite actions are opened and their nested `uses:` held to the same rules**, so a floating tag inside a third-party action fails with the hint to vendor or bump; a `# vX.Y.Z` label must name a tag that points at the sha; job-level reusable-workflow calls (`uses: ./.github/workflows/x.yml`) are recognised as workflows, not actions. `--self-test` runs first on every CI invocation with a fake resolver and no network — eight cases, each proving a rule both fires and stays quiet, including the two bugs found while writing it (a `--root` path error and reusable workflows misread as actions).

**Consequences:** Run against pre-fix `main` the guard reports **9 failures** — the eight node20 pins in `build.yml` and the nested `actions/cache@v4` — and against this change **0 failures, 1 allowlisted, 5 notes** (the notes are the two branch-head pins whose labels cannot be tag-verified, reported as information). The check needs github.com from the runner: about thirteen `ls-remote`/shallow-fetch round trips, roughly a minute. A transient network failure reads as "could not list tags" and fails the job — that is fail-closed and intended; re-run it. The `WORKFLOWS_OVERVIEW.md` entry for `ci.yml` now lists the job. Nothing in the product changed; the v1.2.0 draft built by run #4 is unaffected and remains valid — the warning was about how it was built, not what was built.

## ADR-0045 — The copilot's on-switch is a Beta card, and the settings surface it was mounted in never rendered

**Status:** Accepted (2026-09-14). **Context:** The owner walked the v1.2.0 UI looking for EPIC I and found nothing. Two facts, one intended and one not. Intended: `liveCopilot` ships OFF until the I9 gate (ADR-0043), and with it off the module registers no shortcut and creates no window. Not intended: the only UI that sets `enabled: true` — `CopilotSettings` — and the I4b `ModesSettings` editor were mounted in `frontend/src/components/SettingTabs.tsx`. That component is imported by `Sidebar/index.tsx` and `CustomDialog.tsx` and rendered by neither; `git log -S"<SettingTabs"` over the whole history finds no commit that ever put it in JSX. The Settings page the user sees is `app/settings/page.tsx`, with its own hard-coded tab list (General, Recordings, Transcription, Summary, Beta, License). I1 and I4b were wired into a dead file because it *looked* like the settings surface, and nothing — no test, no screenshot of the real page — checked reachability. **Decision:** (1) `CopilotSettings` and `ModesSettings` are mounted as two cards inside **Settings → Beta**, under the existing yellow banner, each with the BETA badge. The owner chose Beta over a top-level tab: a feature that has not passed its own gate, whose model call has never run against a real provider, should be labelled as such where the user finds it. (2) The two entries are removed from `SettingTabs.tsx` so the components have exactly one home; the dead component itself is upstream debt and is left alone. (3) `BetaSettings.test.tsx` asserts both children are mounted from the Beta tab and that the banner precedes them; removing either child fails its test (mutation-checked). **Consequences:** From the next build, Settings → Beta → Live copilot → switch, then Ctrl+Shift+M (Teams holds that combination for in-call mute; Settings shows what the OS did). Until then the only path is the store file `%APPDATA%\\com.bluedev.mityu\\copilot.json` with `{"config":{"enabled":true}}`, which the partial-config test guarantees loads with shortcuts on. Default remains OFF; nothing about the gate moved. **Lesson recorded:** a component test proves a component, not a route. Every future Settings addition names the page file it is mounted from in its PR, and the /design fixture is not a substitute for opening the real page.

## ADR-0046 — Pinned blocks are first-class provenance in the sections JSON, and the t{seq} bridge is an exact id, not a guess

**Status:** Accepted (2026-09-14). Unblocks BACKLOG **I3c**, whose blocking condition was exactly this record (ADR-0041: "changes the `summaries.sections` JSON shape, so it needs its own ADR, a `docs/CONTRACTS.md` §4 update and a sync-compatibility note **before** any code").

**Context.** ADR-0041 verified three reasons "Pin to notes" could not be built, and left one question open: how `upsert_draft` should preserve non-generated blocks — "a `DraftBlock` provenance marker (`generated` | `pinned`) or an explicit merge". Reading the save path for this decision turned up a fourth fact ADR-0041 did not have, and it is the one that makes the feature cheap instead of expensive.

**The bridge already exists and was being thrown away.** ADR-0041 says the copilot's `t{sequence_id}` ids have "no id-space bridge" to `transcripts` rows, and that building one "is not a UI change". That was true of the code as read, but not of the data in flight. `TranscriptContext` accumulates each live segment carrying `update.sequence_id`, dedupes by it, sorts by it, and `storageService.saveMeeting` passes the whole array verbatim to `api_save_transcript`. The Rust `TranscriptSegment` struct simply has no `sequence_id` field, so serde drops it on the floor. The alternative considered first — matching a pin to a saved row by `audio_start_time` — is float-equality matching on a value that passes through two type conversions, and it would silently mis-attribute a citation when two segments start close together. Mis-attributed evidence is worse than absent evidence: it puts a real timestamp under a claim it does not support, which is the one thing this epic's grounding rule exists to prevent.

**Decisions.**

1. **`TranscriptSegment` gains `sequence_id: Option<u64>`** (`#[serde(default)]`). The renderer already sends it; this stops discarding it. `save_transcript` builds the `sequence_id → transcripts.id` map inside the same transaction that mints the ids, and returns it with the meeting id. **No migration.** The map is consumed immediately by the flush in `api_save_transcript` and never needs to outlive it — persisting a column to re-derive later would be a schema change bought for nothing. `None` (the recovery and audio-import paths, which mint their own segments) means a pin citing that segment does not resolve, and an unresolved pin is reported, never guessed at.

2. **`DraftBlock` gains `provenance: BlockProvenance`** — `Generated` (the `#[serde(default)]`) or `Pinned`. The marker wins over a pure merge strategy for a reason beyond merging: the editor has to *label* a pinned block ("pinned during the meeting"), because a human reading their notes a week later must be able to tell what a model wrote from what they themselves kept mid-conversation. A merge rule alone leaves the JSON unable to answer that. Every `sections` blob already stored deserializes as `Generated`, so the shape change costs no migration and no backfill.

3. **`upsert_draft` merges instead of replacing.** It reads the existing row's sections first, lifts out every block with `provenance = Pinned`, and re-emits them as one trailing section (`Pinned during the meeting`) after the newly generated sections. Weaving them back into model-authored section titles was rejected: those titles change on every regenerate, so a pinned block would migrate between sections for reasons the user never caused.

4. **Preserved pinned blocks keep their own `status` and `original_content`; only incoming generated blocks are forced to `draft`.** Forcing a carried-across block back to `draft` would silently revoke a human's approval on every regenerate — the HITL rule ("generation may never produce an approved block") is about what generation *produces*, not about pre-existing state it passes over. The summary-level status still drops to `draft`, so the whole set is re-reviewed. This is precisely the shape `downgrade_to_draft` already has for retranscription.

5. **`ensure_sources_resolve` still applies only to the incoming generated blocks.** Re-validating carried-across pins would let one retranscription make summary generation impossible for that meeting, and deleting them instead would destroy user-kept content on an unavoidable path — the exact failure ADR-0041 refused to ship. `downgrade_to_draft` sets the precedent: retranscription downgrades, it never drops. A pinned block whose cited segment is gone therefore survives in the draft but **cannot be approved**, because `set_block_status` re-checks evidence at approve time. The gate stays where it was designed to be.

6. **`SummariesRepository::append_block`** is added over the existing private CAS writer, as ADR-0041 required, and additionally **creates the summaries row when none exists** (status `draft`, `model` NULL). That is the common case, not an edge: pins land when the meeting is saved, and summary generation happens afterwards — which is why decision 3 is what stops the ordinary post-meeting summary from erasing them.

7. **Pins buffer in memory in `copilot::session`**, workspace-scoped, cleared at the session boundary with the live context. **A pin can never fail a save.** The flush runs after `save_transcript` has committed; unresolvable or unwritable pins are logged with a count and reported to the renderer, and the meeting is saved regardless. A feature for keeping notes must not become a way to lose a recording.

**Sync-compatibility note (docs/CONTRACTS.md §5, Phase 2).** `summaries.sections` is a synced JSON column. The change is additive and forward-compatible in both directions: a peer on an older build deserializes an unknown `provenance` as absent and, via `#[serde(default)]`, treats the block as `Generated` — it renders and syncs the block normally, losing only the label, never the content. Writing back from such a peer downgrades a pinned block to a generated one, which the next regenerate on a current build may then replace. That is a label-fidelity loss under a mixed-version fleet, not data loss, and it is acceptable for a single-tenant local-first product whose sync is dormant. When sync activates, `sections` needs a schema version — recorded here as a prerequisite of Phase 2, not of this change.

**Consequences.** I3c becomes implementable without a migration and without touching the audio pipeline. The `summaries.sections` §4 shape gains one optional field, documented in `docs/CONTRACTS.md` §4. The verification ADR-0041 demanded — "a test that runs a full generate-after-pin cycle and asserts the pinned block survives" — is now expressible, and is the test that would fail if decision 3 were ever reverted. One residual, stated rather than hidden: a pin made during a recording that is then abandoned (never saved) is lost with the session buffer, because there is nothing to attach it to. The panel says pins are kept when the meeting is saved.

## ADR-0047 (design ADR-A) — The semantic token set is expanded, and `--ring` is decoupled from `--primary`

**Status:** Accepted (2026-09-16), landed by redesign WP1. Scope: `frontend/src/app/globals.css`, `frontend/tailwind.config.js`.

**Context.** The shipped token set was 24 variables anchored on the bluedev brand, and ~871 hardcoded colour utilities across 73 files bypassed it — only 193 `dark:` variants existed, so 22 files rendered a light screen on a near-black app. There was no token for a *status*, for a *machine draft*, for *recording*, or for a *human decision*; components invented those in raw palette classes, one file at a time, and could not agree. `--ring` was byte-identical to `--primary` in both themes, so `focus-visible:ring-ring` on a `bg-primary` button drew a **1.00:1** ring — a focus indicator that did not exist. Radius mapped only `sm/md/lg`, leaving ~95 `rounded`/`rounded-xl`/`rounded-2xl` usages outside the system; there was no elevation, motion or z scale at all, so `z-[95]` was hand-written.

**Decision.** `DESIGN_SYSTEM.md` §4.2/§4.3 land verbatim as CSS variables: surfaces (`--surface-2`, `--surface-3`), text (`--subtle-foreground`), the four status families (`success` / `warning` / `destructive` / `info`), the `--ai` violet draft register, `--verified` ("a human decided"), `--recording`, `--meter`, the shell family and `--chart-1…6` — each as `x` (fill) / `x-foreground` (label on the fill) / `x-surface` (tint) / `x-ink` (text on the tint) / `x-border`. `--ring` becomes its own value in both themes (light `225 100% 50%`, dark `225 100% 72%`). Radius maps `xs…2xl` off `--radius`, with `sm`/`md`/`lg` arithmetically pinned to today's 6/8/10px so ~290 existing corners move zero pixels. Elevation, motion and z become tokens and real utilities (`shadow-elev-*`, `duration-*`, `z-95`), and `tertiary: '#64748b'` — a raw hex in the Tailwind config with zero call sites — is deleted.

**The migration rule, which is the whole reason this is cheap.** *New values land under the OLD token names.* `--background`, `--card`, `--muted-foreground` and the rest keep their names and change their values, so every one of the ~871 existing semantic call sites improved on this commit rather than breaking, and no component package is blocked on a rename. Every token is declared on **both** `:root` and `.dark` — a token present in only one theme renders transparent in the other — and values stay bare HSL triplets consumed as `hsl(var(--x))`, so `bg-primary/20`, `ring-ring/50` and `bg-muted/60` keep working. Definitions stay class-scoped (`.dark`), never `@media (prefers-color-scheme)`: next-themes writes the class, `/design` wraps a subtree in `.dark` for side-by-side proof, and `tools/ui/shoot.py` depends on it.

**Consequences.** 86 tokens per theme, parity verified by script. Decoupling `--ring` does **not** by itself satisfy SC 2.4.11 — `--ring` against `--primary` is 1.22:1 (1.71 dark). What satisfies it is the mandatory 2px `ring-offset-*` in the surface colour, whose worst case is 5.32:1; the decoupling exists so no component can silently regress to 1.00:1. Files that still carry raw palette classes render the old greys beside the new surfaces until their own package lands — a deliberate, visible, shrinking intermediate state, not a regression. `scratchpad/contrast-recheck.txt` recomputes every §4.4.1–4.4.7 number from the committed tokens (HSL → 8-bit sRGB → WCAG 2.1) and passes; it is the thing to re-run after any token edit.

---
## ADR-0048 (design ADR-B) — DM Sans → Inter, and a 12px floor for anything a person must read

**Status:** Accepted (2026-09-16), landed by redesign WP1.

**Context.** DM Sans shipped at 400/500/600/700 as `--font-dm-sans`, with `theme.fontFamily.sans` holding that variable and **no fallback stack** (a font failure fell through to the browser default serif), no `font-mono` token although `font-mono` was used, and `subsets: ['latin']` only — no Turkish `ş ğ İ ı`. The type scale was Tailwind's defaults plus 79 arbitrary `text-[Npx]` usages, and ADR-0037 had already caught classes that compile to nothing (`text-medium`, `text-s`, `shadow-xs`).

**Decision.** **Inter**, via `next/font/google` (self-hosted at build; the Tauri CSP allows no external font host), weights 400/500/600, `subsets: ['latin','latin-ext']`, exposed as `--font-sans` with a real fallback stack; `font-mono` is declared as the system stack, downloaded from nowhere. Eleven type steps become **real `theme.extend.fontSize` entries** carrying size, line-height, weight and tracking — `text-display` · `text-title-lg` · `text-title` · `text-title-sm` · `text-read` · `text-body` · `text-label` · `text-caption` · `text-micro` · `text-eyebrow` · `font-mono`. **12px (`text-caption`) is the floor for anything a person must read**, including every consent, disclosure and compliance string; 11px `text-micro` is reserved for an uppercase eyebrow, a count or an `mm:ss`, and its only sentence-level use is the 380px copilot window — whose compliance strings are still `text-caption`.

**Why Inter over DM Sans.** Inter was drawn for 12–16px UI and has the x-height to hold at the 13px label / 12px caption sizes this density needs; it ships true tabular figures, which every timestamp, duration, stat tile and `mm:ss` in this product depends on; and it covers `latin-ext`. DM Sans's geometric forms and proportional lining numerals are wrong for a compliance-facing transcript tool.

**Consequences.** Declaring the scale as real utilities structurally kills the phantom-class failure: an undeclared step does not compile. The face change moves metrics app-wide, so the 380px copilot and the 280px Meetings pane are the first things to re-measure in their packages. `AiLabel` and `AiMarking` move from 10px to `text-caption` in their own WPs — that is the compliance half of this decision and it is not optional. One weight is dropped (700); nothing in the system specifies it.

---
## ADR-0049 (design ADR-C) — `tailwind-merge` downgraded to `^2.6.0`

**Status:** Accepted (2026-09-16), landed by redesign WP1.

**Context.** `package.json` asked for `tailwind-merge@^3.3.1` and resolved 3.6.0, while the project is on Tailwind **3.4.19**. tailwind-merge 3.x is built for Tailwind v4's class groups. `cn()` is the merge function behind every shadcn primitive, and WP1 adds new `fontSize`, `boxShadow` and `ring` utilities — exactly the groups whose definitions moved in v4. A mis-grouped merge does not throw: it silently drops or keeps the wrong class, which reads as "the style did not apply" in one composed component and nowhere else. The lockfile also already carried 2.6.1 transitively, so two copies shipped.

**Decision.** Pin `tailwind-merge` to `^2.6.0`, the line built for Tailwind v3, and de-duplicate the lockfile to a single 2.6.1. Revisit only together with a Tailwind v4 migration, never separately.

**Consequences.** One copy in the bundle. `twMerge`'s API is unchanged between the two lines, so no call site moves. This is a downgrade against the registry's "latest", which dependency bots will offer to undo; the constraint is the Tailwind major, and it belongs in the PR description of any future bump.

---
## ADR-0050 (design ADR-H) — Reduced-motion policy, and the deletion of the `vibrate` keyframe

**Status:** Accepted (2026-09-16), landed by redesign WP1 (CSS half) and by each component package (framer-motion half).

**Context.** `src/` had **zero** `prefers-reduced-motion` handling against 111 `animate-*` utilities, 202 `transition-*` utilities, 11 framer-motion files, and a `vibrate` shake keyframe used to reject an invalid unlock — a 0.3s translate shake, which is a vestibular trigger under WCAG SC 2.3.3.

**Decision.** (1) A global `@media (prefers-reduced-motion: reduce)` block in `globals.css` collapses animation and transition duration to 1ms, pins iteration count to 1 and forces `scroll-behavior: auto`. (2) The `vibrate` keyframe and `.animate-vibrate` are **deleted outright** rather than slowed — the shake carried no information that its sibling message did not. (3) The fallbacks are **semantic, not merely faster**: the recording dot stops pulsing and becomes a solid dot *while the text carries the state*; `mityu-tour-pulse` becomes a static 2px `--ring` outline so the anchor it points at is still identifiable; the typewriter transcript effect renders final text immediately; skeletons show flat `--surface-2`; the jump-to-source highlight does not fade but stays for 4s; `animate-spin` becomes a static three-quarter ring beside its existing `role="status"` text. (4) `mityu-tour-pulse` and the BlockNote `!important` overrides are otherwise kept verbatim — the tour anchor is a MUST-PRESERVE pin.

**Consequences.** The CSS half covers every `animate-*`/`transition-*` in one place, including files nobody will revisit. The framer-motion half cannot be done in CSS and is owed by each of the 11 files via `useReducedMotion()`; a package that adds motion without it has not finished. Two call sites still passed `animate-vibrate` as a class name (`ModelSettingsModal`, `TranscriptSettings`); with the keyframe gone the class was inert — a phantom class, i.e. the ADR-0037 failure class this very commit claimed to close — and the remaining feedback was `text-red-500` alone, which is WCAG 2.1 SC 1.4.1 (use of colour). **Both were repaired in the review pass (ADR-0055), not deferred:** the shake is now a `role="status"` sentence, *"This key is locked. Use the lock button to edit it."*, which works with motion reduced, in greyscale, and for a screen reader.

---
## ADR-0051 (design ADR-L) — Interaction and label tokens, the `ringOffsetColor` map, and the four AA failures they fix

**Status:** Accepted (2026-09-16), landed by redesign WP1. Recorded apart from ADR-A because it **changes shipped colour**, not only names.

**Context.** The shipped palette had one value per role and let components improvise hover and pressed states with `hover:bg-primary/90`, `hover:opacity-90` and raw palette classes. Auditing every state against every surface it is allowed to sit on — rather than against `--card` alone — found four live WCAG failures and seven sub-threshold boundary pairs.

**Decision — the tokens.** `--foreground-hover/-active`, `--primary-hover/-active`, `--verified-hover/-active`, `--destructive-hover/-active`, `--recording-foreground/-hover/-active`, `--input-hover`, plus the `theme.extend.ringOffsetColor` map of fifteen entries over twelve distinct surface values. `ringOffsetColor` is **not optional**: the shared focus treatment is `focus-visible:ring-offset-<surface>`, and the offsets must be **surfaces, never fills** — Tailwind draws the offset outside the border box, so a fill-coloured offset paints a halo instead of a gap.

**Decision — the four colour changes.**
1. **Dark `--primary-hover` was `225 100% 65%`; white on it is 3.82.** That is the hover state of the single most important control in the product — the filled `verified` button carrying `Approve summary` and the frozen 🔒 `I confirm participants are informed — start recording`. It is now `225 100% 56%` (5.43). The usable dark band is **53–60 % L**: below it the fill drops under 3:1 against `--card`, above it the white label drops under 4.5:1. The hover step therefore goes **down** in lightness in dark. **There is no global "hover = darker" rule** — the direction is chosen per token by measurement, and inventing one is how this broke.
2. **The record/stop control painted `--primary-foreground` (white) on `--recording`; in dark that is 3.84.** A dedicated `--recording-foreground` now exists in both themes: white in light (4.83), near-black red `4 85% 10%` in dark (4.74) — the same inversion the dark `--success`/`--warning` foregrounds already used.
3. **`--input` cleared 3:1 on `--card` only** (light) / on `--card`, `--background`, `--popover` only (dark), so seven boundary pairs failed 1.4.11 on surfaces the spec actually puts inputs on. It is re-tuned to `220 12% 52%` / `222 14% 48%` (min 3.28 / 3.23 across all six declared surfaces), and a new `--input-hover` — not `--border-strong` — is the hover boundary; `--border-strong` at 1.65:1 is decorative and may never be a control's only boundary.
4. **`--subtle-foreground` and `--primary-ink`** are re-tuned so meta text and brand text clear 4.5:1 on `--surface-3` and `--accent` too, not only on `--card`. The `--primary` **fill** is unchanged — the #1E56FF brand promise is about the fill, not the text token.

**Consequences.** Dark `destructive` first shipped 45 → 50 → **47 % L** (6.02 → 5.09 → 5.63), i.e. non-monotonic — the press moved the fill back *toward* idle and read as a partial un-hover. That was defended as forced by the token's legal band (45–54 % L: below 45 the fill drops under 3:1 against `--card`, above 54 the white label drops under 4.5:1) and the defence was wrong: 51–54 % all continue past hover and all pass AA. `--destructive-active` is **`358 62% 54%`** as of ADR-0055 — the top of its own band, **4.58** on the white label and **3.99** against `--card`. Do not push past 54 %: that is where 1.4.3 breaks. Every number above is reproduced from the committed tokens by `scratchpad/contrast-recheck.txt`, which fails if a future edit moves one.

---
## ADR-0052 (design ADR-J, part 1 of 2) — `/design/*` is the contract surface, and the guardrails' blind spot is named rather than hidden

**Status:** Accepted (2026-09-16). WP1 lands the tooling half; WP16 owns the `--theme dark` flag, the dead-class check, the CI job and the orphaned-suite disposition.

**Context.** CI runs lint · `tsc` · `pnpm test` · the landing test. **Nothing in the pipeline proves rendering.** The product routes cannot render in a browser at all — they `invoke()` on mount and sit on a loading state forever — so the only surface that can be screenshotted is the `/design/*` fixture set, which renders real components with injected data and adds no mount-time `invoke`/`listen`.

**Decision (WP1 half).** (1) `tools/ui/shoot.py` splits a route on `?` and requests `path.html?query`, sanitising the PNG filename, so the two documented URL APIs (`design/hitl?reject=1`, `design/tour?tour=3`) can be shot by the packages that gate on them; it also accepts an `--expect` marker beginning with `-`, because half the required markers for `/design` *are* token names (`--ai-surface`, `--verified`). (2) Four `no-restricted-syntax` esquery guardrails land in the existing `.eslintrc.json` — no raw palette utility or hex in `className`, no arbitrary `text-[Npx]`, no `ring-ring` without a `ring-offset-*` sibling, no `outline-none` without a `focus-visible:` replacement — with **no new npm dependency**, and the file stays eslintrc so `next lint` keeps working. (3) Turning them on globally today would fail the build in 76 files that WP2–WP15 are scheduled to rewrite, so the trailing `overrides` entries are a **shrinking quarantine** of exactly those files: each package deletes its own entries as it migrates them, no array may ever gain one, and when an array is empty its entry goes. **The quarantine is per-rule (ADR-0055).** §11.4 authorises dropping rule 1 for files awaiting their WP; it does not authorise dropping all four, and the first cut set `"no-restricted-syntax": "off"` wholesale — leaving arbitrary type sizes, bare `ring-ring` and unreplaced `outline-none` unpoliced across 76 files including `ui/dialog.tsx` and `ui/sheet.tsx`. The quarantine now re-declares guardrails 2–4 and only the **21** files that actually trip one of them carry a narrower entry naming exactly which.

**The blind spot, stated plainly.** A literal-value selector can only read a string literal. `src/` holds ~2342 literal `className="…"` sites, but also ~85 `className={cn(…)}` and ~95 template-literal sites — about **180 compositions the rules cannot read** — and the `ui/` primitives, where 13 files use `outline-none`, are exactly where `cn()` is densest. Therefore: the rules are a cheap first pass over ~93 % of call sites, **not** a proof; the `ring-ring` rule does **not** carry SC 2.4.11 (the §4.4.6 arithmetic and the `ringOffsetColor` map do); and the compensating control is WP16's **dead-class / built-CSS check**, which reads the *emitted* CSS and therefore sees through `cn()`. This is written into `docs/CONVENTIONS.md` so it is not rediscovered as a surprise.

**Consequences.** `tailwind.config.js` `content` gains `./src/lib/**`, `./src/hooks/**` and `./src/contexts/**`: `lib/recordingNotification.tsx` renders JSX with Tailwind classes and was not scanned, so its classes could emit **no CSS** in the static export while `tsc`, lint and vitest all passed — the ADR-0037 failure class, now closed for three whole directories. A Rust baseline was attempted for the packages that need one and could not be produced here: `cargo clippy` fails in `gdk-sys` because the container has no GTK/WebKitGTK development packages. That is recorded in `scratchpad/baseline-clippy.log`, and it is the concrete argument for the `rust-gates` CI job — a Rust-touching package that reports "clippy clean" from this environment has not run clippy.

---
## ADR-0053 (design ADR-C, part 2 of 2) — One shadcn generation (v3 `forwardRef`), and `cn()` is taught the tokens

**Status:** Accepted (2026-09-16), landed by redesign WP2. Part 1 (the `tailwind-merge` downgrade) is ADR-0049, landed by WP1. Scope: `frontend/src/components/ui/*`, `frontend/src/lib/utils.ts`.

**Context.** `ui/` held **two** shadcn generations. Nineteen primitives were the v3 line — `React.forwardRef` over `@radix-ui/react-*` — and three (`accordion`, `button-group`, `input-group`) were the v4 line: plain function components, `data-slot` attributes, Tailwind-v4 utilities, and, in `accordion.tsx`, Radix pulled through the **`radix-ui` umbrella** rather than the per-primitive package the rest of the file tree imports. That is not a cosmetic split. It gave the app two focus idioms (`accordion`'s `focus-visible:ring-[3px] ring-ring/50` with no offset exists nowhere else), two ways to take a ref (the v4 files could not take one at all), and two import paths into Radix — and a v4 primitive composed inside a v3 `Dialog` or `Tooltip` can read a different copy of a Radix context than the one its parent provides. It also carried **`shadow-xs`**, a Tailwind v4 class that compiles to nothing on Tailwind 3.4: an elevation decision that was a no-op, which is the ADR-0037 phantom-class failure class, and `dark:bg-input/30`, which painted a translucent *boundary* token as a *fill*.

**Decision.** The v3 `forwardRef` generation is the single generation. `accordion.tsx`, `button-group.tsx` and `input-group.tsx` are rewritten into it; the `radix-ui` umbrella import is dropped and `accordion` imports `@radix-ui/react-accordion` like everything else; `shadow-xs` and the v4-only colour idioms go with it. The two new primitives §5.14/§5.5 require follow the same rule and **add no npm dependency**: `alert-dialog.tsx` is composed from `@radix-ui/react-dialog`, which is a direct dependency — Radix spreads `...contentProps` *after* its own `role: "dialog"`, so `role="alertdialog"` is a genuine override, and the remaining differences (no dismiss on outside interaction, focus opens on Cancel, no close X) are props and a default `onOpenAutoFocus`. `checkbox.tsx` is a native `<input type="checkbox">` with `appearance-none` and a `peer-checked` mark: reaching back through the umbrella for a checkbox would have re-opened the import path this ADR closes, and a native checkbox already brings form participation, platform AT semantics and `:checked` / `:disabled` / `:focus-visible` for free.

**The second half: `cn()` silently deleted the type scale.** `tailwind-merge` resolves `text-*` by validator — a t-shirt size is a font-size, and *anything else is a colour*. WP1's eleven §4.5 steps are neither, so `twMerge('text-label', 'text-muted-foreground')` returned exactly `"text-muted-foreground"`: the type step vanished, with no error, in every composition where a primitive sets a step and the call site sets a colour — and `ui/` is where `cn()` is densest. `shadow-elev-2 shadow-none` and `duration-instant duration-base` likewise both survived, leaving stylesheet order rather than the call site to decide the winner. `cn()` now uses `extendTailwindMerge` to register the §4.5 / §4.8 / §4.9 / §4.11 keys as literal class-group members, so they beat the colour group's catch-all and stay mutually exclusive with `text-sm` and friends. This is the concrete instance of the risk ADR-0049 described in the abstract ("a mis-grouped merge does not throw: it silently drops or keeps the wrong class"), and it is why `src/lib/utils.ts` — not in WP2's declared `files[]` — had to move with this package: without it, every primitive WP2 ships loses its type step at the call site.

**Consequences.** One generation, one focus idiom, one Radix import path, and `ui/` primitives that can all take a ref. `ButtonGroup`'s two `has-[select…]` corner-rounding hacks are dropped with the rewrite — no consumer composes a `Select` into a `ButtonGroup`; if one ever does, the rule comes back as an explicit variant rather than as an inherited selector nobody can read. The `cn()` registration must be **extended whenever `tailwind.config.js` gains a custom key in an existing Tailwind class group** — a token added to `theme.extend.fontSize` without a matching entry here is a token that works everywhere except inside `cn()`, which is the hardest possible place to notice it.

---
## ADR-0054 (design ADR-D) — Blue means "a human decided": the default button is ink, and `verified` is the only filled blue

**Status:** Accepted (2026-09-16), landed by redesign WP2. Scope: `frontend/src/components/ui/button.tsx` and the nine call sites that moved with it.

**Context.** `ui/button.tsx` shipped ten variants, four of which were raw palette: `green` (`bg-green-600`, hover **identical** to idle), `blue` (`bg-blue-500`), `red` (`bg-red-500`) and `gray` (hover identical to idle). No §4.4 row could ever cover them, §11.4 guardrail 1 forbids that vocabulary everywhere else, and two of the four had no hover state at all. Worse, the *semantic* default was `bg-primary` — brand blue — so blue was the colour of "a button", spent equally on `Generate`, `Cancel`, `Save`, `Ask` and on `Approve summary`. When everything is blue, the one action that legally matters — a human committing to an AI draft — has no way to look different from the action that regenerates it.

**Decision.** Exactly seven variants: `default` (**ink**, `--foreground`), `verified` (**brand blue**), `outline`, `ghost`, `link`, `destructive`, `record`. `green` / `blue` / `red` / `gray` are deleted, and so is `secondary`, which had zero call sites and which `outline` already is in this system. The neutral primary — Generate, Continue, Save, Retry — is ink. Blue appears as a **fill** only on `verified`: the one human-commit action per viewport (`Approve summary`, the frozen 🔒 `I confirm participants are informed — start recording`). Every variant declares **idle, hover, active and disabled**; a variant with an idle fill only forces the next implementer to invent the interaction colour of the product's most compliance-critical controls, and an invented value cannot be checked against §4.4. `record` takes `--recording-foreground`, never `--primary-foreground`: white on the dark `--recording` is **3.84:1** and fails AA (ADR-0051).

**Why it is a type error and not a review convention.** `buttonVariants` is `cva` + `VariantProps<typeof buttonVariants>`, so deleting a key turns every remaining call site into a **compile error**. `pnpm tsc --noEmit` therefore cannot pass unless the call sites move in the same commit — which is why this is one atomic design-system change and why `AISummary/DraftSummaryView.tsx` sits in WP2's blast radius although WP10 owns its visual rework. The spec enumerated seven legacy sites; there were **nine**. The two it missed are `DraftSummaryView.tsx:488` and `:738`, where the per-row Approve button was `variant={status === 'approved' ? 'green' : 'outline'}` — a *dynamic* variant, invisible to a grep for `variant="green"`. Both became plain `outline` (§5.4's row-level Approve). The approved state is therefore currently carried by the button's `disabled` state alone until **WP10** gives it the §5.8 `verified` **Badge**; promoting it to a filled `verified` button instead would have put one filled blue control on every approved row, which is precisely the thing this ADR exists to prevent — `verified` marks an *action a human takes*, not a state a row is in.

**Consequences.** Buttons change colour on screens no redesign package has reached yet — that is the point of landing the primitive layer atomically, and it is visible rather than hidden. `size` gains `xs` / `row` / `icon-lg` and `icon` moves 36→**32px**, matching §8's hit-target table (32×32 for row and toolbar icon buttons, 40×40 for rail items and the record control). The two `LearningSettings` sites are covered only by copy assertions, so they were checked by eye in the `/design/learning` fixture. 🔒 One rule this file cannot enforce and every call site must: **the record CTA is never disabled while recording** — `disabled:` exists on that variant for the pre-flight states (no device, no permission), and stop must always be reachable.

---
## ADR-0055 (design ADR-M) — The WP1–WP3 review repairs: what changed in shipped behaviour, and what the spec got wrong

**Status:** Accepted (2026-09-16). Scope: the adversarial review of WP1 (`44d87cc`), WP2 (`6570023`) and WP3 (`37ea16a`). Recorded as one ADR because several of these repairs **amend DESIGN_SYSTEM.md itself** rather than merely correcting an implementation of it.

**1. `/design/*` had no scroller, so most of its own deliverable was unreachable.** `globals.css` sets `body { overflow: hidden; height: 100% }` while `html` stays `overflow: visible`, so the hidden overflow propagates to the viewport and nothing on any route can scroll the page; every product route compensates with `h-screen` + an inner `flex-1 overflow-y-auto`, and the fixture routes did not. The §11.1 token matrix and primitives gallery are several viewports tall each, so most of both was below the fold in a browser and absent from the §11.2 PNGs — while the `--expect` markers kept passing, because they are checked against `--dump-dom`, which sees the whole tree regardless of visibility. **A new `src/app/design/layout.tsx` owns the scroller for the whole route group**, and the two theme panels now split at `md` (768px) rather than `lg` (1024px) so the both-themes claim actually holds at the documented 900px minimum window. 🔒 **What that means for §11.2's PNGs**: because the scroller is *inside* the page, the document is exactly one viewport tall, so a CDP `captureBeyondViewport` capture would return the same pixels — the lever is the **window**. `tools/ui/shoot.py --height 3600` is the documented full-page form, and its docstring now says so. A default 1280×900 shot of a §11.1 panel shows its header and nothing else: a valid PNG and no evidence.

**2. Guardrails 3 and 4 failed on the legal, spec-conforming form.** Both were written as a positive match plus a **forward-only lookahead**, so `"ring-offset-2 ring-offset-card ring-2 ring-ring"` — exactly what §4.4.6 prescribes — was reported as an error purely because the offset was written first, and this repo has no `prettier-plugin-tailwindcss` to normalise class order. Both are now **order-independent**: a positive match plus a `[value!=/…/]` exclusion. The acceptance proof covers both orderings for both rules.

**3. The quarantine was per-file; it is now per-rule.** See ADR-0052 as amended. 76 files × all four rules became 21 files × the specific rule each one trips.

**4. `--gutter` shipped its responsive behaviour as a comment.** The token was declared once at 24px with `/* 16px below 1000px */` beside it and no media query anywhere, so every `p-gutter` consumer got 24px at the app's narrowest supported width — wider chrome on the narrowest window, the opposite of the intent. `globals.css` now carries `@media (max-width: 999.98px) { :root { --gutter: 1rem } }` in the same `@layer base` block as the token.

**5. The read-only variant also matches a DISABLED input, so disabled fields shipped with no boundary.** Per HTML, `:read-only` matches any input that is not mutable, which includes `<input disabled>`, and in Tailwind's pseudo-class order that variant is emitted **after** `disabled` — so the read-only boundary rule won at equal specificity and a disabled field lost its boundary entirely, the exact opposite of §5.5 ("disabled … boundary stays `border-input`"). Reachable today at `ModelSettingsModal`, `CopilotSettings` and `LearningSettings`. `Input` and `Textarea` now scope those three rules with `[&:read-only:not(:disabled)]:`.

**6. The focus offset is a prop now, not a constant.** `focus-ring.ts`'s contract is that the 2px offset is the colour of the surface the control **sits on** — that offset, not `--ring` (1.22:1 on `--primary`), is what carries SC 2.4.11. Every control primitive hardcoded `focusRing("card")`, and in dark `--card` / `--popover` / `--background` / `--sidebar` are four different values, so every control inside a Dialog, Sheet or Popover drew a halo in the wrong colour. `Button`, `Input`, `Textarea`, `SelectTrigger`, `Switch`, `Checkbox`, `InputGroup` and `TabsTrigger` take a `surface` prop (default `card`, `FocusSurface`-typed so it cannot be a free string), and `AlertDialogAction` / `AlertDialogCancel` default it to `popover`, which is what their own content is. Remaining call sites inside floating layers are their packages' to pass.

**7. `TabsList` defaulted to `underline` and imposed it on a consumer that could not opt out.** `/settings` still owns its framer-motion `layoutId` bar and sizes its triggers with `py-4`, so the default painted **two** 2px primary rules on it and collapsed the trigger box from ~52px to `h-9`; the call site could not escape, because `data-[state=active]:after:bg-primary` sits in a modifier group nothing at the call site can merge away. The default is now `plain` — no track, no height, no `::after`, focus treatment only. Recorded in §5.7 as amended; WP12 passes `variant="underline"` in the change that deletes the framer-motion bar.

**8. `SourceChip` conflated "has a printable clock time" with "has a source link".** The single most important HITL control was `disabled` by default for every real summary block and action item, and rendered the sentence *"This item has no transcript link"* about items whose `source_chunk_id` was present and required. §5.9 said so too (`unresolved (no timestamp → disabled …)`) and **contradicted its own Behaviour paragraph**, which keys the jump on `onJumpToSource(source_chunk_id)` and makes the playhead seek conditional on `audio_start_time` being known. §5.9 is amended: unresolved means **no `source_chunk_id`**, passed explicitly by a caller that knows; the timestamp is purely additive. Two smaller repairs ride along: the inert chip is `aria-disabled` rather than `disabled`, so the `aria-describedby` explanation is reachable by the keyboard and AT users it was written for; and `aria-controls="transcript-drawer"` is no longer a default, because a dangling IDREF is an axe `aria-valid-attr-value` violation and no `#transcript-drawer` exists on the route that ships to prove the component.

**9. The indeterminate Progress ran ~9× too fast and never crossed its own track.** Its arbitrary `duration-` value compiled to **nothing**: that namespace is claimed by both core `transitionDuration` and `tailwindcss-animate`'s `animationDuration`, so Tailwind reported it ambiguous and emitted no rule, leaving the §5.16 1.4s sweep at `.animate-in`'s built-in 150ms. Separately, `slide-in-from-left-full` translates by the element's **own** width, and the fill is `w-1/2`, so the sweep covered only the left half and parked mid-bar. There is now a real `progress-indeterminate` keyframe (`translateX(-100%)` → `translateX(200%)`, 1.4s, infinite) in `tailwind.config.js`. **And the reduced-motion form is no longer "the same bar, held still":** a stopped half-width fill at the left edge is indistinguishable from a determinate 50%, so `globals.css` turns `[data-slot='progress-indeterminate']` into the full-width **striped** track §5.16 asks for, which cannot be read as a percentage.

**10. `divide-{color}` repaints per-child border colours.** `divide-border` emits the border-color **shorthand** on `> * + *` at specificity (0,3,0), which beats `border-l-verified` / `border-l-destructive` at (0,1,0) — so §5.10's load-bearing status rule became a neutral grey hairline on every review row except the first (`+ *` never matches the first child, which is why the draft row looked right and hid the bug). The fixture uses `divide-y` + `[&>*+*]:border-t-border`, which touches one side and cannot collide with a left rule. **Do not put `divide-{color}` next to per-child border colours** — WP10 copies this row markup.

**11. Two deliberate, previously unrecorded consequences, now on the record.**
* **`theme.transitionTimingFunction.out` silently redefines the stock `ease-out` utility.** The key `out` produces the class `ease-out`, which Tailwind already ships as `cubic-bezier(0, 0, .2, 1)`; it now resolves to `var(--ease-out)` = `cubic-bezier(.2, .8, .2, 1)` at all 12 existing `ease-out` call sites in `src/`. **This is accepted deliberately, not tolerated**: §4.9's easing is meant to be the product's easing, and an opt-in `ease-design-out` would leave a dozen sites on the stock curve forever. (`in-out` is byte-identical to Tailwind's default, so only `ease-out` actually moves. The CSS *keyword* `ease-out` in `globals.css` is a different thing and is unaffected.)
* **Inter is loaded at 400/500/600, per §4.1 — there is no 700 cut.** The 19 remaining `font-bold` sites in `src/` therefore render **synthetic** bold until their packages sweep them to `font-semibold` / `text-title*`. No guardrail catches a weight that is referenced but not downloaded; the sweep rides the same shrinking quarantine as the raw-palette classes.

**12. `cn()` was registered for WP2's keys only.** ADR-0053's own rule is that the registration extends whenever `tailwind.config.js` gains a custom key in an existing Tailwind class group. WP1's `theme.extend.spacing` aliases (`h-header`, `w-rail`, `p-gutter`, …) fell into **no** tailwind-merge group at all and `ease-emphasis` was outside its `ease` literals, so `cn('h-header','h-9')` and `cn('ease-emphasis','ease-out')` both emitted **both** classes. Registered now, before WP4 — the package that actually uses them — inherits the trap.

**13. Smaller repairs, no ADR of their own.** `--destructive-active` 47 % → 54 % (ADR-0051 as amended). The `z-70` floating-layer band is written into §4.10 with its reasoning and its one known cost, instead of living as a comment repeated in four files. `StatTile`'s loading value goes through `SkeletonRegion`, so the `<dd>` announces "Loading {label}" instead of nothing. `SourceChip` follows §5.9's colour contract (`hover:border-primary-ink`, `Clock` in `currentColor`). `alert.tsx`'s docblock names all **eight** importers — it missed `RecordingControls` and `app/_components/SettingsModal`, both of which this restyle already moved (padding `px-4 py-3` → `p-3`, description 14px → 12px). `/design/primitives` renders the four surfaces it exported but never showed (`EmptyState variant="filtered"`, `ReportSkeleton`, `SettingsSectionSkeleton`, `CardDescription`) and gives its dark panel's `role="note"` a distinguishing accessible name, because two `note`s sharing one name inside one tree is precisely the DOM shape `notice.tsx` says must never occur.

**Consequences.** Three of these (1, 8, 9) were defects in the **evidence**, not only in the code: a screenshot that shows one viewport of an unscrollable page, a control disabled for every real data shape on a fixture that only ever passes synthetic data, and a duration that compiles to nothing. None was catchable by `tsc`, lint or `pnpm test`, and only the last is catchable by WP16's dead-class check. The fixture routes are the contract surface (ADR-0052), so a fixture that cannot be scrolled, or that never renders a state, is not a weaker proof — it is not a proof.

---
## ADR-0056 (design ADR-E) — The shell is a rail, a pane the user owns, and a content column that stops moving

**Status:** Accepted (2026-09-16). Implements DESIGN_SYSTEM.md §3.2 and §5.1–§5.3 (WP4).

**Context.** `components/Sidebar/index.tsx` was 893 lines that rendered **two different navigations** from one boolean. Collapsed, it was a rail of icons; expanded, it was a list that re-declared Home, Action Center, the record CTA, Import, Settings and About as a second set of controls. Both halves rendered `data-tour="record-button"`, and the tour's selector worked only because exactly one branch was ever mounted. The library lived inside that same collapsible column, so looking a meeting up moved the content pane 200px sideways, and hiding the column hid the only list of meetings. The collapse state was `useState(true)`, so every launch started collapsed no matter what the user had chosen. Three components separately computed the sidebar's width as a margin (`MainContent`, `app/page.tsx`, `StatusOverlays`), each with its own `4rem`/`16rem` literals and its own transition, which is why the record pill drifted while the sidebar animated.

**Decision.**
1. **`AppRail`** (56px) is the navigation, in every state: brand → About, the Meetings toggle (`aria-expanded`/`aria-controls`), Home, Actions, then Record, Import, Search, Settings and the version. One `data-tour="record-button"` exists, in one place, always.
2. **`MeetingsPane`** (280px) is the library, toggled by the user and **persisted** to `localStorage['mityu.ui.meetingsPane']`; with no stored preference it follows the window (open ≥1100px). It owns search, the meeting rows and their rename/delete actions.
3. **The shell is a flex row**, so `MainContent` no longer mirrors the sidebar's width — it is simply what is left. The record pill and the status overlays became `absolute` inside the content pane instead of `fixed` to the window, which is what retired all three copies of the width literals. `StatusOverlays`' `sidebarCollapsed` prop is kept and deprecated rather than removed, so no call site breaks.
4. **`PageHeader`** replaces `components/MainNav` (`h-0`, zero importers, deleted here). It detects "scrolled" with a 1px sentinel and an IntersectionObserver, because every screen owns its own scroll container and there is no window scroll to listen to.
5. **`SystemNotices`** is the single slot for the trial chrome and the unencrypted-at-rest warning. Each banner used to supply its own `px-4 pt-4`, so the top gutter changed size depending on which of them was live.
6. The **`serverAddress` gate is replaced by an explicit `ready` flag**. The string is a leftover from the archived Python backend; gating the first meeting fetch on "is a localhost URL set" made the timing of the first database read an accident of an unrelated legacy field. Both addresses stay, because `ModelSettingsModal` and `useModelConfiguration` still read them; nothing gates on them.
7. **`window.openSettings` now navigates.** It is a Rust→webview contract (`tray.rs`), and it previously set a piece of state that nothing rendered — so the tray's Settings item silently did nothing from this side.
8. The window drops `"theme": "Light"` (it forced light mode on a themed app) and gains `minWidth: 900` / `minHeight: 600`, the size the layout is verified at.

**What was deliberately not built.** §5.2 asks for date-grouped rows. `api_get_meetings` returns `{id, title}` only, so the sole way to group by date today is to parse it out of the generated title — the exact defect the audit recorded, where a renamed meeting loses its date. The grouping machinery ships with **one** group; when the list payload carries `created_at`, this becomes a change to `groupMeetings()` alone.

**What was removed, and why it is safe.** The old file also held a model-config fetch, a transcript-config fetch, a `model-config-updated` listener and two save handlers. None of it reached the screen: its only consumer was `SettingTabs`, which ADR-0045 already recorded as rendered by nothing. The save handlers were never called, so `Analytics.trackSettingsChanged('transcript_config')` was an identifier that could not fire; `…('model_config')` still fires from `useModelConfiguration.ts`. Giving the transcript config a real save belongs to the settings package, and the identifier belongs with it.

**Fixtures.** `/design/shell` renders the **real** rail and pane, not a mock, by supplying the four contexts as literal values — their providers all `invoke()` on mount and cannot run in a browser. The context objects are exported for that purpose and product code still uses the hooks, whose throw is what stops a consumer rendering outside its provider. Shooting it found two defects a type checker cannot: the rail and the pane were `h-screen`, so inside any box shorter than the window their lower halves — Record, Search, Settings, the version — were clipped; and `PageHeader` at a fixed `h-14` collided with its own meta line. Both are fixed; the header is `min-h-header` and still measures exactly 56px when it carries only a title.

**Consequences and what this environment could not verify.** The Rust gates this package owes (`cargo fmt --check`, `cargo build`, `cargo clippy -D warnings`) **did not run**: the container has no GTK/WebKitGTK development packages, so `clippy` dies in `gdk-sys` (recorded in `scratchpad/baseline-clippy.log` by WP1). The only Rust-adjacent change here is `tauri.conf.json`, which is validated as JSON; the schema check and the manual gates — tray → Settings, tray → record toggle, `start-recording-from-sidebar`, `check-updates-from-tray`, drag-and-drop import — need a machine that can build the app and are owed before release.

---
## ADR-0057 (design ADR-G) — Home is a review queue, and an AI action item carries its marking everywhere it is shown

**Status:** Accepted (2026-09-16). Implements DESIGN_SYSTEM.md §6.1 (WP7). Closes the audit's highest-severity compliance finding.

**Context.** `HomeDashboard` rendered `api_get_open_action_items` — model output that **no person has approved** — as a line of text with a 2px coloured dot whose only explanation was a `title=` tooltip. No Art. 50 marking, no link to the transcript segment the claim came from, and no way to approve or reject without opening the meeting. The `source_chunk_id` was already on the wire and simply unused. Everywhere else in this product an AI claim is a draft with an approve control and a source link; on the first screen a user sees, it was presented as a fact.

Two smaller defects sat beside it. The meeting cards printed a date parsed with a regular expression out of the auto-generated title, so a renamed meeting lost its date and a meeting a user happened to name `2026-01-02_notes` gained a wrong one — the payload carries `{id, title}` and nothing else. And the first-run tour **navigated the user to a seeded sample meeting**, so the first thing a new user saw was a report of a meeting they had never recorded, in the same chrome as their own.

**Decision.**
1. **`ActionItemRow`** is the one way an action item is drawn. It carries the `StatusPill`, the `SourceChip`, the `AiLabel` (`AI-generated · review required`) and inline Approve / Edit / Reject with the 🔒 frozen accessible names. Mutation is optimistic with a **revert plus a toast** when the call resolves `false` — every `summaryDraftService` mutation returns a boolean where `false` is a legitimate soft no-op, not an exception, so a queue that keeps a status the backend refused is the failure mode to design against.
2. **Home leads with what needs a decision.** The queue is first; the library is a digest below it, because the pane beside it already holds the whole list.
3. **A load failure is visible, not silent.** It renders a `destructive` notice with Retry while the rest of Home still renders. The previous `.catch(() => {})` meant a broken review queue and an empty one looked identical.
4. **No date is printed and no metric tile is drawn** for a number this app cannot produce. The regex is deleted rather than guarded: a date that is right most of the time is worse than none, because nothing tells the reader which kind they are looking at.
5. **The tour opens where the user already is.** `TourProvider` keeps 🔒 `TOUR_ANCHORS`, `WELCOME_COPY` and the three steps verbatim, and `replayTour` still navigates — there the user asked for it. The sample becomes an **offer** on Home ("Try the sample report").

**Consequences.** `/design/home` renders the real screen with an injected service — the `LearningSettings` pattern — across four states: drafts, load failure, nothing waiting, and first run. Its `--expect 'AI-generated · review required'` marker is the machine-checkable proof that the Art. 50 gap is closed, and one fixture item's reject resolves `false` so the revert path is visible rather than only asserted. Nothing on this surface reports anything about the content it shows: 🔒 `Analytics.trackPageView('home')` stays where it was, and no new tracking is introduced on a screen that handles meeting text.

---
## ADR-0058 (design ADR-I) — The Action Center is on tokens, and its contract is now a test rather than a comment

**Status:** Accepted (2026-09-16). Implements DESIGN_SYSTEM.md §6.4 (WP11).

**Context.** `/actions` was the single largest un-themed surface in the app: **72 raw palette utilities and zero `dark:` variants**, so opening it in dark mode switched the window to white. It was also a 307-line screen with **no automated coverage at all**, holding four behaviours that are invisible to a reader and only appear when pages arrive in an order the happy path does not produce.

**Decision.**
1. **Every colour is a token**, and the provenance disclosure becomes a `consent`-tone `Notice` rather than a hand-built blue box. 🔒 Its `aria-label="Action provenance"`, its title `AI-extracted · human approved` and its sentence are unchanged, as are the `role="status"` loading text, the `role="alert"` failure title and its constant, the empty-state text, and `<ul aria-label="Approved actions">`.
2. **The screen moves to `components/actions/ActionCenter`** and `app/actions/page.tsx` becomes a thin route shell. Next validates a route component's props against its own `PageProps`, so a screen that takes an injected loader cannot BE the route — and without injection the fixture and the test would each have to re-implement the screen they claim to check.
3. **Four behaviours are now asserted**: backend order is preserved (the fixture data is deliberately neither alphabetical nor chronological, so any local sort fails the test); pages merge by id, because offsets shift and the same row can arrive twice; a stale response loses to a newer one, because Tauri invokes are not cancellable and the guard is a request nonce; and a failed page keeps a way forward.
4. **`Load more` no longer hides behind the error.** It previously rendered only when `error` was null, so the one control that could retry the failed page disappeared exactly when it was needed; it now stays and reads `Try again`.
5. **The worker's UTC timestamp is never printed as a position in the recording.** `audioStartTime` formats as `mm:ss`; `sourceTimestamp` is an absolute date and showing it where a clock time belongs tells the reader an action came from four minutes into a meeting when it did not. A row without an offset gets a plain `Source` chip and a sentence saying the position was not stored. 🔒 The accessible name keeps its existing value, including that fallback — it names the target rather than claiming a position.
6. **The empty state ships a next action** ("Open the review queue"), where it previously ended the journey.
7. A **client-side filter** narrows what is already loaded. It issues no query, changes no order and mutates nothing — 🔒 ADR-0025's read-only rule is intact, and there is still no complete, snooze, due-date editor or overdue styling.
8. 🔒 **Zero analytics.** This surface handles meeting content and reports nothing; the redesign introduces no tracking here.

**Consequences.** `/design/actions` renders the real screen with an injected loader across loaded, empty and failed states in both themes, including the no-offset row. The suite count moves from 25 files / 258 tests to 26 / 264.

---
## ADR-0059 (design ADR-F) — The report is one document, and the transcript is evidence you open beside a claim

**Status:** Accepted (2026-09-16). Implements DESIGN_SYSTEM.md §6.3 (WP9). Resolves the contradiction the audit recorded between `docs/DESIGN_READAI.md:45-50` and what shipped.

**Context.** The design direction prescribed a single column — header, summary, takeaways, action items, topics, transcript. What shipped was the opposite: a **transcript-primary split pane** with the summary capped at 640px, collapsible, and on narrow windows hidden behind a tab bar. The reader met **two amber Art. 50 disclosures back to back** (the Ask panel's marking, then the draft's review-required banner) before the first sentence of the summary. The editor was pinned to `theme="light"`, so a summary opened as a white sheet inside a near-black window. And opening a report **wrote a model configuration to the database** as a side effect of viewing it.

**Decision.**
1. **One column.** The summary is the report; the split pane, the collapse control and the mobile tab bar are gone.
2. **The transcript moves into an evidence drawer** that opens beside the claim — on a source link, on a deep link naming a segment, on the toolbar's Transcript control, and when the first-run tour anchors on it (a coach-mark needs its anchor **visible**, not merely mounted).
3. **The drawer is not a `Dialog` or `Sheet`.** Three contracts rule that out: the transcript pane must stay 🔒 MOUNTED (unmounting loses its scroll position and breaks jump-to-source, which retries after pagination brings the segment in); focus must **not** be trapped and the report must stay interactive, because the point is to read evidence next to a claim; and the tour anchors on it. So it is a plain `aside` that animates its width and goes `invisible` — out of the tab order — the instant it closes, with Esc closing it and returning focus to the control that opened it. The rejected alternative, a bezier connector drawn from a block to its segment, is unbuildable across a virtualized list whose target row is usually unmounted.
4. **Exactly one Art. 50 banner above the first summary block.** The Ask panel moves **below** the summary. 🔒 Its own unconditional `role="note"` marking stays exactly where it is — directly above its input — and it stays outside the structured-draft branch, so meetings that were never summarised keep it. What changed is the order.
5. **The editor follows the app theme**, resolving `system` to its actual value; the `!important` sizing overrides and the ProseMirror aliases are untouched.
6. **Viewing a report no longer writes a setting.** The old path saved `provider: ollama, model: ''` when the database was empty — a read-only act changing stored configuration the user had never been asked about, and not even the configuration it then generated with. Auto-generation proceeds on the in-memory default; choosing a model stays in Settings.
7. **The playback scrubber is operable.** It stays a native range input — that already carries the slider role and pointer handling — and gains `aria-label="Playback position"`, an `aria-valuetext` a person can understand ("12 minutes 4 seconds of 1 hour 24 minutes" rather than "724" of "5040"), and ←/→ 5s, ⇧←/⇧→ 30s, Home/End.
8. **The fixture stops proving lookalikes.** `/design/report`'s four private copies of the stat tile, the AI label, the source chip and the card chrome are deleted in favour of the shipping primitives.
9. **The marking is only on model output.** Chapters are computed on-device from pauses — deterministic, no model — and the transcript is the evidence a claim is checked against, not a claim. Both carried the label in the fixture; a reader who sees a disclosure everywhere stops reading it anywhere.

**Consequences.** `SummaryPanel` loses its collapse props and its brand-tinted "AI zone" background: the report reads on the page surface now, and the tint was doing the job the per-block markings already do. 🔒 `spellCheck={false}` on that root stays — a red squiggle under a model's wording reads as a correction the app is making to evidence. Everything the register freezes on this screen is unchanged: the Ask panel's roles and strings and its ban on any descendant labelled close or dismiss, the speaker caveat in the primary column, the transcript pane's tour anchor and diarization strings, the URL parameter names and the jump nonce, and the format priority behind the summary view.

---
## ADR-0060 (design ADR-K) — One feedback system, no native dialogs, and sonner's own palette turned off

**Status:** Accepted (2026-09-16). Implements DESIGN_SYSTEM.md §6.8 (WP13, first tranche).

**Context.** Feedback was five competing systems: **twelve native `alert()` / `confirm()` call sites**, sonner toasts, six hand-rolled full-screen pseudo-modals, inline alerts, and a hand-rolled confirmation with no focus trap. A native dialog is drawn by the operating system: it cannot be themed, translated or screenshotted, it freezes the whole webview, and it says nothing a user can act on. Three of them sat on the recording path — including the mount check of the one screen whose job is to start a recording — and one asked "Are you sure you want to delete this meeting?" about a meeting it could not name.

**Decision.**
1. **Zero `alert()` / `confirm()` in `src/`.** Each becomes a message that says what happened and what to do next: "Recording is unavailable — Mityu could not reach the audio engine. Restart the app; if it keeps happening, reinstall." The permission failures name the exact System Settings path and the Re-check control instead of a sentence the user has to translate into clicks.
2. **The destructive confirmation is the `Dialog` primitive.** It supplies the focus trap and Escape, and 🔒 `role="dialog"` + `aria-modal` + `aria-labelledby="delete-confirmation-title"` + `aria-busy` and the Cancel / Delete / "Deleting…" labels are preserved. The content is reordered the way a person reads it: the **name of the thing**, one sentence of consequence, and the full 🔒 ADR-0026 disclosure verbatim behind **"What exactly is removed"**. Focus lands on Cancel — Enter on an unread dialog must not delete a meeting.
3. **The consent gate keeps every frozen string and loses its red button.** 🔒 The title, the description, the consent box heading and paragraph, the reset-on-open checkbox, both button labels, and the rule that X / Escape / outside-click all cancel are unchanged. The confirm becomes `variant="verified"` — red is the *indicator that a recording is running*, not the semantics of a person confirming something, and the verified family's white label clears AA under exactly this sentence where the old dark hover measured 3.82. The amber consent box, three hardcoded values with no dark variant, moves to the warning tokens; in dark mode it had been painting near-black text on near-white inside a near-black dialog.
4. **Onboarding stops reloading the window.** `window.location.reload()` ran from three places and the `onComplete` the flow is handed was never called. The reload was doing real work — it is what made the providers re-read a database that did not exist when they first mounted — so it is replaced by an explicit refetch of the meetings list and the licence anchor, performed **inside** the provider tree, followed by the handover. The white flash that used to end setup goes with it.
5. **Per-row permission state.** One flag disabled both rows, so pressing Enable on the microphone put the system-audio row into "Checking…" and blocked it.
6. **A persisted `current_step === 4` on a non-macOS machine falls back to step 3** instead of rendering nothing at all, which is what it did — a blank setup screen with no way out.
7. **One toast configuration.** 🔒 `position="bottom-center"` and `closeButton` are preserved byte-for-byte — the close control is the only dismiss for an infinite download toast — and the offset reads `--bottom-chrome`, so a toast never lands under the record pill.

**The deliberate deviation.** `richColors` is **dropped**, and the audit records it as part of the shipped configuration. It paints sonner's own Tailwind palette: colours no contrast row in §4.4 covers, from the exact source §11.4 rule 1 forbids everywhere else in this app. Tone now comes from the design tokens — a 3px left rule in `--success` / `--warning` / `--destructive` / `--info` on a `--card` toast — so a message reads the same as every other surface and its contrast is a number someone has checked. This is the only must-preserve entry this redesign departs from, and it is recorded here rather than discovered later.

**Not in this tranche.** The six hand-rolled modals in `app/_components/SettingsModal.tsx` are replaced when the settings work moves `LanguageSelection` out of it; the file is deleted in the cleanup package. The import, update and download-progress surfaces keep their current behaviour and their frozen strings and are not restyled here.

---
## ADR-0061 (design ADR-M) — Settings become URL-addressable sections, and the two settings that existed twice stop pretending to be one

**Status:** Accepted (2026-09-16). Implements DESIGN_SYSTEM.md §6.5 (WP12, first tranche).

**Context.** Settings were six tabs held in `useState`, so **nothing could deep-link to a section**: an empty state that says "choose a model in Settings", a toast, or the tray could only open the page and leave the user to find it. The back control was `router.back()` sitting beside a permanent navigation rail — a browser affordance in an app that never navigated like a browser. And "General" was **nine unrelated cards in one column**: appearance, notifications, the product tour, storage paths, recording consent, redaction, the learning loop and analytics, in that order.

Two settings existed **twice**. The recordings folder was printed in General and again in Recording. Two switches both called themselves notifications while writing different stores — `notification_preferences` in one place and `preferences.json`'s `show_recording_notification` in the other — so turning "notifications" off in one tab left "notifications" on in the other.

**Decision.**
1. **Nine sections, each a URL** (`/settings?section=privacy`): General · Recording · Transcription · Summary · Privacy · Learning · Beta · License · About. The section column lives **inside** the content pane, so navigating settings never opens or closes the meetings pane as a side effect, and the page header replaces the back button.
2. **Privacy is a section, not a scroll position.** Consent, redaction, analytics and the storage/erasure disclosure — everything that decides what leaves this device or what is kept on it — are in one place. 🔒 Every string in them is verbatim, including the erasure disclosure and the consent disclaimer.
3. **The duplicates are resolved by naming, not deletion.** The folder path lives only in Recording, where a person looks for it; General's storage card points there. The two switches now say which is which: **"Meeting start and end notifications"** in General, the **participant reminder** in Recording. Deleting either would have removed a real setting from a real store.
4. **Two primitives** carry the shape: `SettingsCard` (the card chrome nine copies of which were written out by hand) and `SwitchRow` (a real `<label>` bound to the control, with the consequence in a description rather than a `title=` tooltip no keyboard user sees).

**Explicitly not in this tranche, and why.** The 1387-line `ModelSettingsModal` is **not** decomposed, and `ApiKeyField` / `ModelCard` are not extracted: five importers depend on its exact props and its `ModelConfig` type, and splitting it is a change that needs the app running to verify — a summary that silently stops generating is not something a fixture screenshot can catch. `ThemeToggle` does **not** yet call `setTheme` on the native window; that needs a `core:window:allow-set-theme` grant in the inline capabilities and a Rust build to prove, and **the Rust gates cannot run in this environment** (no GTK/WebKitGTK development packages — `cargo clippy` dies in `gdk-sys`). Both are recorded here rather than claimed.

---
## ADR-0062 (design ADR-N) — Approval is legible without colour, and the review controls stop hiding from the keyboard

**Status:** Accepted (2026-09-16). Implements DESIGN_SYSTEM.md §5.10 (WP10). **No string, role or `aria-label` on this surface changed** — only layout, colour and disclosure.

**Context.** `DraftSummaryView` is the product's moat: it is where a person turns model output into a record they will stand behind. Three things about it worked against that.

**The status chips were colour and a word, and the colour came from the raw palette** — twelve hardcoded values with no dark variant beyond a tint. In greyscale, in a printed export, in a screenshot pasted into a dispute file, or to a reader with a colour-vision deficiency, "Approved" and "Rejected" were the same grey pill with different text. That is the one context where this product's output matters.

**The primary action was a guess.** Approve, Edit and Reject were three identical 32px outline buttons, distinguished only by a tick, a pencil and a cross at 16px. Nothing said which one the screen existed for.

**The source link could vanish silently.** `SourceLink` returned `null` when `onJumpToSource` was absent, so a parent that forgot one prop removed the evidence link from every block — and the summary looked exactly as complete as one whose evidence resolved. That is the failure this whole surface exists to prevent.

**Decision.**
1. **Status chips become `StatusPill`:** the same four words, each with an icon, on tokens. Status is never carried by colour alone.
2. **Approve keeps its word** (`Check` + "Approve"), and Edit and Reject reveal on hover or keyboard focus while staying in the DOM **and in the tab order** — `opacity-0`, never `display:none` and never `pointer-events-none`, which is how a reveal-on-hover pattern quietly becomes mouse-only. `@media (pointer: coarse)` makes all three visible where there is no hover at all.
3. **The source chip is always drawn.** A caller without a handler gets the unresolved state, which says so in a sentence, instead of nothing at all. 🔒 The accessible name is unchanged.
4. **The Art. 50 banner is the `Notice` primitive**, which **drops** any element passed as a child rather than rendering it — so "this marking contains no control" is a property of the component instead of a promise a later edit can break. It moves from amber to the AI register: amber is this app's warning colour, a draft is not an error, and a marking that looks like an error is one a reader learns to dismiss. 🔒 `role="note"`, the accessible name, both sentences and its presence in all four states are unchanged.
5. **The edit and reject fields move onto tokens**, including their focus rings; they had been drawing blue and red borders from the palette with `focus:ring` rather than `focus-visible`.

**Consequences.** 🔒 Everything the register pins is intact: the eleven per-item accessible names, the optional never-gating reject reason with Enter submitting blank and Escape cancelling, the whole-summary gate and its five disabled reasons, the export gating and its disclosure, the lock predicate and its unit test, the Original toggle, and `data-tour="summary-approve-block"` on the first draft block. The `/design/hitl` fixture now passes a jump handler, so the source control it was silently omitting is part of the screenshot.
