'use client';

/** Route shell for the Action Center — the screen itself is `components/actions/ActionCenter`. */
import { ActionCenter } from '@/components/actions/ActionCenter';

export default function ActionsPage() {
  return <ActionCenter />;
}
